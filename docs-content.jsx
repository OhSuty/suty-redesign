// AUTO-GENERATED — do not edit. Built by scripts/build-redesign-docs.mjs
// Source: OhSuty/suty-docs synced into suty-tebex/content/docs-external/resources/
//
// To refresh, in the suty-tebex repo root:
//   node scripts/build-redesign-docs.mjs

const DOCS_TREE = [
  {
    "id": "suty-billing",
    "title": "Tap Pay",
    "pages": [
      {
        "id": "suty-billing/README",
        "title": "Overview"
      },
      {
        "id": "suty-billing/installation",
        "title": "Installation"
      },
      {
        "id": "suty-billing/configuration",
        "title": "Configuration"
      },
      {
        "id": "suty-billing/compatibility",
        "title": "Compatibility"
      },
      {
        "id": "suty-billing/troubleshooting",
        "title": "Troubleshooting"
      }
    ]
  },
  {
    "id": "suty-scalemenu",
    "title": "Character Scale Menu",
    "pages": [
      {
        "id": "suty-scalemenu/README",
        "title": "Overview"
      },
      {
        "id": "suty-scalemenu/installation",
        "title": "Installation"
      },
      {
        "id": "suty-scalemenu/configuration",
        "title": "Configuration"
      },
      {
        "id": "suty-scalemenu/commands",
        "title": "Commands"
      },
      {
        "id": "suty-scalemenu/compatibility",
        "title": "Compatibility"
      },
      {
        "id": "suty-scalemenu/troubleshooting",
        "title": "Troubleshooting"
      },
      {
        "id": "suty-scalemenu/usage",
        "title": "Usage"
      }
    ]
  },
  {
    "id": "suty-sv_sync",
    "title": "Community Service",
    "pages": [
      {
        "id": "suty-sv_sync/README",
        "title": "Overview"
      },
      {
        "id": "suty-sv_sync/installation",
        "title": "Installation"
      },
      {
        "id": "suty-sv_sync/configuration",
        "title": "Configuration"
      },
      {
        "id": "suty-sv_sync/commands",
        "title": "Commands"
      },
      {
        "id": "suty-sv_sync/compatibility",
        "title": "Compatibility"
      },
      {
        "id": "suty-sv_sync/troubleshooting",
        "title": "Troubleshooting"
      }
    ]
  },
  {
    "id": "suty-drag",
    "title": "Drag System",
    "pages": [
      {
        "id": "suty-drag/README",
        "title": "Overview"
      },
      {
        "id": "suty-drag/installation",
        "title": "Installation"
      },
      {
        "id": "suty-drag/configuration",
        "title": "Configuration"
      },
      {
        "id": "suty-drag/commands",
        "title": "Commands"
      },
      {
        "id": "suty-drag/compatibility",
        "title": "Compatibility"
      },
      {
        "id": "suty-drag/troubleshooting",
        "title": "Troubleshooting"
      }
    ]
  },
  {
    "id": "suty-drugsell",
    "title": "Drug Sell System",
    "pages": [
      {
        "id": "suty-drugsell/README",
        "title": "Overview"
      },
      {
        "id": "suty-drugsell/installation",
        "title": "Installation"
      },
      {
        "id": "suty-drugsell/configuration",
        "title": "Configuration"
      },
      {
        "id": "suty-drugsell/commands",
        "title": "Commands"
      },
      {
        "id": "suty-drugsell/events",
        "title": "Events"
      },
      {
        "id": "suty-drugsell/compatibility",
        "title": "Compatibility"
      },
      {
        "id": "suty-drugsell/troubleshooting",
        "title": "Troubleshooting"
      }
    ]
  },
  {
    "id": "suty-announce",
    "title": "Job Announcements",
    "pages": [
      {
        "id": "suty-announce/README",
        "title": "Overview"
      },
      {
        "id": "suty-announce/installation",
        "title": "Installation"
      },
      {
        "id": "suty-announce/configuration",
        "title": "Configuration"
      },
      {
        "id": "suty-announce/commands",
        "title": "Commands"
      },
      {
        "id": "suty-announce/compatibility",
        "title": "Compatibility"
      },
      {
        "id": "suty-announce/troubleshooting",
        "title": "Troubleshooting"
      }
    ]
  },
  {
    "id": "suty-moneythrow",
    "title": "Money Throwing",
    "pages": [
      {
        "id": "suty-moneythrow/README",
        "title": "Overview"
      },
      {
        "id": "suty-moneythrow/installation",
        "title": "Installation"
      },
      {
        "id": "suty-moneythrow/configuration",
        "title": "Configuration"
      },
      {
        "id": "suty-moneythrow/commands",
        "title": "Commands"
      },
      {
        "id": "suty-moneythrow/compatibility",
        "title": "Compatibility"
      },
      {
        "id": "suty-moneythrow/troubleshooting",
        "title": "Troubleshooting"
      }
    ]
  },
  {
    "id": "suty-carry",
    "title": "Player Carry",
    "pages": [
      {
        "id": "suty-carry/README",
        "title": "Overview"
      },
      {
        "id": "suty-carry/installation",
        "title": "Installation"
      },
      {
        "id": "suty-carry/configuration",
        "title": "Configuration"
      },
      {
        "id": "suty-carry/commands",
        "title": "Commands"
      },
      {
        "id": "suty-carry/compatibility",
        "title": "Compatibility"
      },
      {
        "id": "suty-carry/troubleshooting",
        "title": "Troubleshooting"
      }
    ]
  },
  {
    "id": "suty-queue",
    "title": "Queue System",
    "pages": [
      {
        "id": "suty-queue/README",
        "title": "Overview"
      },
      {
        "id": "suty-queue/installation",
        "title": "Installation"
      },
      {
        "id": "suty-queue/configuration",
        "title": "Configuration"
      },
      {
        "id": "suty-queue/commands",
        "title": "Commands"
      },
      {
        "id": "suty-queue/compatibility",
        "title": "Compatibility"
      },
      {
        "id": "suty-queue/troubleshooting",
        "title": "Troubleshooting"
      }
    ]
  },
  {
    "id": "suty-reportes",
    "title": "Report System",
    "pages": [
      {
        "id": "suty-reportes/README",
        "title": "Overview"
      },
      {
        "id": "suty-reportes/installation",
        "title": "Installation"
      },
      {
        "id": "suty-reportes/configuration",
        "title": "Configuration"
      },
      {
        "id": "suty-reportes/commands",
        "title": "Commands"
      },
      {
        "id": "suty-reportes/compatibility",
        "title": "Compatibility"
      },
      {
        "id": "suty-reportes/troubleshooting",
        "title": "Troubleshooting"
      }
    ]
  },
  {
    "id": "suty-zonasegura",
    "title": "Safe Zone Creator",
    "pages": [
      {
        "id": "suty-zonasegura/README",
        "title": "Overview"
      },
      {
        "id": "suty-zonasegura/installation",
        "title": "Installation"
      },
      {
        "id": "suty-zonasegura/configuration",
        "title": "Configuration"
      },
      {
        "id": "suty-zonasegura/commands",
        "title": "Commands"
      },
      {
        "id": "suty-zonasegura/compatibility",
        "title": "Compatibility"
      },
      {
        "id": "suty-zonasegura/troubleshooting",
        "title": "Troubleshooting"
      }
    ]
  }
];

const DOCS_CONTENT = {
  "suty-announce/README": {
    "folderTitle": "Job Announcements",
    "pageTitle": "Overview",
    "crumb": [
      "Job Announcements",
      "Overview"
    ],
    "html": "<h1>📣 suty-announce</h1>\n<p>Two announcement systems in one resource:</p>\n<ol>\n<li><strong><code>/anuncio</code></strong> — job-based banner that any business job (police, EMS, bakery, etc.) can send. Players press E to mark the sender&#39;s location on their GPS.</li>\n<li><strong><code>/evento</code></strong> — anonymous admin broadcaster styled as an emergency transmission. Players see the alert but not the admin&#39;s identity. If the admin is in a vehicle when sending, players track the vehicle live.</li>\n</ol>\n<h2>✨ Key Features</h2>\n<ul>\n<li>Per-job branding (label, icon, banner image, accent color, default postal)</li>\n<li>GPS waypoint on hold-E (with optional 3D world marker via <code>sleepless_waypoints</code>)</li>\n<li>Postal-code support (auto-detects 6+ popular postal scripts)</li>\n<li>Anonymous emergency broadcast (<code>/evento</code>) with caution-stripe styling</li>\n<li>Vehicle-follow tracking for anonymous events</li>\n<li>Discord webhook with admin audit trail</li>\n<li>Multi-framework: QBX / QB-Core / ESX</li>\n<li>Cooldown + duration limits per command</li>\n</ul>\n<h2>📦 Requirements</h2>\n<ul>\n<li>ox_lib</li>\n<li>QBX Core / QB-Core / ESX</li>\n<li><strong>Optional:</strong> <a href=\"https://github.com/Sleepless-Solutions/sleepless_waypoints\">sleepless_waypoints</a> — for 3D world markers on GPS-marked locations</li>\n<li><strong>Optional:</strong> any postal script (auto-detects nearest-postal, Chow_Postal, melons_postals, jc-postal, qb-postal)</li>\n</ul>\n<h2>🌐 Compatibility</h2>\n<table>\n<thead>\n<tr>\n<th>Framework</th>\n<th>Status</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>QBX Core (Qbox)</td>\n<td>✅</td>\n</tr>\n<tr>\n<td>QB-Core</td>\n<td>✅</td>\n</tr>\n<tr>\n<td>ESX (es_extended)</td>\n<td>✅</td>\n</tr>\n</tbody></table>\n<h2>🛍️ Get it</h2>\n<p><strong><a href=\"https://suty.dev\">Buy on suty.dev →</a></strong></p>\n<h2>📑 Next steps</h2>\n<ul>\n<li><a href=\"installation.md\">Installation</a></li>\n<li><a href=\"configuration.md\">Configuration</a></li>\n<li><a href=\"commands.md\">Commands</a></li>\n<li><a href=\"troubleshooting.md\">Troubleshooting</a></li>\n</ul>\n"
  },
  "suty-announce/installation": {
    "folderTitle": "Job Announcements",
    "pageTitle": "Installation",
    "crumb": [
      "Job Announcements",
      "Installation"
    ],
    "html": "<h1>Installation</h1>\n<h2>1. Drop the resource</h2>\n<p>Extract <code>suty-announce</code> into <code>resources/[suty]/suty-announce</code>.</p>\n<h2>2. Ensure in <code>server.cfg</code></h2>\n<pre><code class=\"language-cfg\">ensure ox_lib\nensure suty-announce\n</code></pre>\n<p>If you use <code>sleepless_waypoints</code> for 3D world markers, make sure it&#39;s also ensured (start order doesn&#39;t matter — suty-announce probes for it at runtime).</p>\n<h2>3. Set the framework</h2>\n<p>Open <code>config.lua</code> and confirm:</p>\n<pre><code class=\"language-lua\">Config.Framework = &#39;qbx&#39;    -- &#39;qbx&#39; | &#39;qb&#39; | &#39;esx&#39;\n</code></pre>\n<h2>4. Grant admin permission</h2>\n<p>The <code>/evento</code> command requires <code>admin</code> ACE:</p>\n<pre><code class=\"language-cfg\">add_principal identifier.license:YOUR_LICENSE group.admin\n</code></pre>\n<p>The script accepts both <code>admin</code> and <code>group.admin</code> ACE forms.</p>\n<h2>5. Configure your jobs</h2>\n<p>Open <code>config.lua</code> and review <code>Config.AllowedJobs</code>. Each job in this table can use <code>/anuncio</code>. Pre-configured with police, ambulance, barber, pizzeria, tokyo, kushbites, mechanic, bbq, sexywaffle, coffeclub, carwash, abogada, gelato, bowling, cannabis, etc. — add your own following the same pattern.</p>\n<h2>6. (Optional) Discord webhook</h2>\n<p>In <code>config.lua</code>:</p>\n<pre><code class=\"language-lua\">Config.DiscordLogs = {\n    enabled = true,\n    webhook = &#39;https://discord.com/api/webhooks/...&#39;,\n    botName = &#39;HoodLife Announce&#39;,\n    botLogo = &#39;https://your-cdn.com/logo.png&#39;,\n}\n</code></pre>\n<p>Logs include sender identity even for anonymous <code>/evento</code> broadcasts (audit trail).</p>\n<h2>7. Verify</h2>\n<ul>\n<li>As a player with one of the <code>AllowedJobs</code>, run <code>/anuncio</code> — the input dialog opens</li>\n<li>As an admin, run <code>/evento</code> — the emergency-broadcast dialog opens</li>\n</ul>\n"
  },
  "suty-announce/configuration": {
    "folderTitle": "Job Announcements",
    "pageTitle": "Configuration",
    "crumb": [
      "Job Announcements",
      "Configuration"
    ],
    "html": "<h1>Configuration</h1>\n<p><code>config.lua</code> at the resource root. Unencrypted and freely editable.</p>\n<h2>Globals</h2>\n<pre><code class=\"language-lua\">Config.Framework            = &#39;qbx&#39;         -- &#39;qbx&#39; | &#39;qb&#39; | &#39;esx&#39;\nConfig.Command              = &#39;anuncio&#39;     -- job announcement command\nConfig.AdminCommand         = &#39;announce&#39;    -- global admin announce command (NOT /evento, see below)\nConfig.AnnouncementDuration = 15000         -- ms a banner stays on screen\nConfig.AnnouncementCooldown = 10            -- seconds between announcements per player\n</code></pre>\n<h2>Admin groups for the global command</h2>\n<pre><code class=\"language-lua\">Config.AdminGroups = {\n    [&#39;god&#39;]        = true,\n    [&#39;admin&#39;]      = true,\n    [&#39;mod&#39;]        = true,\n    [&#39;moderator&#39;]  = true,\n}\n</code></pre>\n<p>These groups can use <code>Config.AdminCommand</code> (defaults to <code>/announce</code>) which broadcasts as a generic server-wide message.</p>\n<h2>Postal script integration</h2>\n<pre><code class=\"language-lua\">Config.UsePostalScript = true               -- auto-detect popular postal scripts\nConfig.PostalScript    = nil                -- manual override resource name (optional)\nConfig.PostalExport    = nil                -- manual override export name (optional)\n</code></pre>\n<p>Auto-detects: <code>nearest-postal</code>, <code>Chow_Postal</code>, <code>melons_postals</code>, <code>jc-postal</code>, <code>qb-postal</code>.</p>\n<h2>Job-based jobs (<code>Config.AllowedJobs</code>)</h2>\n<p>Each entry is a job that can use <code>/anuncio</code>. Sample:</p>\n<pre><code class=\"language-lua\">police = {\n    label       = &#39;HOOD LIFE POLICE DEPARTMENT&#39;,\n    icon        = &#39;👮&#39;,\n    logo        = false,                    -- &#39;police.png&#39; in html/img/ for a logo (false to skip)\n    banner      = false,                    -- banner image (URL or local file in html/img/)\n    color       = &#39;#0066cc&#39;,                -- accent color (one var theming)\n    postalCode  = &#39;10001&#39;,                  -- default postal shown\n    gps         = vector3(428.46, -981.19, 30.71),  -- GPS waypoint when player presses E\n    permissions = {\n        canSendGlobal = true,               -- true = banner goes to ALL players; false = job-only\n    },\n},\n</code></pre>\n<ul>\n<li><strong><code>postalCode = &#39;RANDOM&#39;</code></strong> uses the postal script&#39;s nearest-postal lookup</li>\n<li><strong><code>logo</code></strong> and <strong><code>banner</code></strong> accept either an <code>https://</code> URL or a filename in <code>html/img/</code></li>\n<li><strong><code>canSendGlobal = false</code></strong> restricts the broadcast to other people in the same job</li>\n</ul>\n<h2>UI / notification</h2>\n<pre><code class=\"language-lua\">Config.NotificationPosition = &#39;top-right&#39;   -- &#39;top-left&#39; | &#39;top-center&#39; | &#39;top-right&#39;\nConfig.UseOxLib             = false         -- true = ox_lib notifications, false = native QB notify\n\nConfig.EnableSounds = true\nConfig.SoundVolume  = 0.3\n</code></pre>\n<h2>GPS marking</h2>\n<pre><code class=\"language-lua\">Config.EnableGPSMarking = true              -- hold E to set waypoint\nConfig.Use3DWaypoint    = true              -- spawn 3D world marker via sleepless_waypoints (soft dep)\n</code></pre>\n<h2>Anonymous event broadcaster (<code>/evento</code>)</h2>\n<pre><code class=\"language-lua\">Config.AnonymousEvent = {\n    Enabled  = true,\n    Command  = &#39;evento&#39;,\n    AceGroup = &#39;admin&#39;,                     -- &#39;admin&#39; OR &#39;group.admin&#39; both accepted at runtime\n\n    DefaultDurationMs = 15000,\n    MinDurationMs     = 5000,\n    MaxDurationMs     = 60000,\n    CooldownSeconds   = 10,\n\n    VehicleFollow = {\n        Enabled        = true,\n        UpdateMs       = 5000,              -- server pushes new coords every X ms\n        DurationMs     = 5 * 60 * 1000,     -- follow up to N ms after broadcast ends\n        ArriveDistance = 25.0,              -- player&#39;s waypoint auto-clears within X m\n    },\n\n    Style = {\n        AlertLabel = &#39;⚠ TRANSMISIÓN ANÓNIMA INTERCEPTADA&#39;,\n        Accent     = &#39;#fcd000&#39;,             -- caution-stripe amber accent\n    },\n}\n</code></pre>\n<h2>Discord webhook</h2>\n<pre><code class=\"language-lua\">Config.DiscordLogs = {\n    enabled = true,\n    webhook = &#39;https://discord.com/api/webhooks/...&#39;,\n    botName = &#39;HoodLife Announce&#39;,\n    botLogo = &#39;https://r2.fivemanage.com/.../suty.png&#39;,\n}\n</code></pre>\n<p>Logs <strong>all</strong> announcements with sender identity — including anonymous <code>/evento</code> (the webhook captures who sent it for audit even though players don&#39;t see).</p>\n"
  },
  "suty-announce/commands": {
    "folderTitle": "Job Announcements",
    "pageTitle": "Commands",
    "crumb": [
      "Job Announcements",
      "Commands"
    ],
    "html": "<h1>Commands</h1>\n<table>\n<thead>\n<tr>\n<th>Command</th>\n<th>Who</th>\n<th>Description</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>/anuncio</code></td>\n<td>Anyone whose job appears in <code>Config.AllowedJobs</code></td>\n<td>Opens the job announcement input. Players see a branded banner with the job&#39;s color/icon.</td>\n</tr>\n<tr>\n<td><code>/announce</code></td>\n<td>Anyone in <code>Config.AdminGroups</code></td>\n<td>Global generic admin announcement (not branded by job).</td>\n</tr>\n<tr>\n<td><code>/evento</code></td>\n<td>Admins (<code>Config.AnonymousEvent.AceGroup</code>)</td>\n<td>Anonymous emergency-broadcast banner with caution-stripe styling. Players cannot see who sent it.</td>\n</tr>\n</tbody></table>\n<h2>Player-side interactions</h2>\n<p><strong>Hold E</strong> while a banner is on screen (configurable via <code>Config.EnableGPSMarking</code>) to:</p>\n<ul>\n<li>Set a yellow flag waypoint on the minimap</li>\n<li>Add a route line to the destination</li>\n<li>If <code>Config.Use3DWaypoint = true</code> AND <code>sleepless_waypoints</code> is started → spawn a tall 3D checkpoint marker visible at long range</li>\n</ul>\n<p>For <strong>anonymous /evento</strong> broadcasts where the sender is in a vehicle, the GPS waypoint <strong>tracks the vehicle live</strong> (updates every <code>VehicleFollow.UpdateMs</code>) until either:</p>\n<ul>\n<li><code>VehicleFollow.DurationMs</code> elapses (default 5 min)</li>\n<li>The player gets within <code>VehicleFollow.ArriveDistance</code> meters (default 25m)</li>\n</ul>\n<h2>Cooldowns</h2>\n<ul>\n<li><code>/anuncio</code> and <code>/announce</code> share <code>Config.AnnouncementCooldown</code> (default 10s per player)</li>\n<li><code>/evento</code> uses <code>Config.AnonymousEvent.CooldownSeconds</code> (default 10s)</li>\n</ul>\n"
  },
  "suty-announce/compatibility": {
    "folderTitle": "Job Announcements",
    "pageTitle": "Compatibility",
    "crumb": [
      "Job Announcements",
      "Compatibility"
    ],
    "html": "<h1>Compatibility</h1>\n<h2>Frameworks</h2>\n<table>\n<thead>\n<tr>\n<th>Framework</th>\n<th>Status</th>\n<th>Notes</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>QBX Core (Qbox)</td>\n<td>✅ Native</td>\n<td>Default. <code>Config.Framework = &#39;qbx&#39;</code></td>\n</tr>\n<tr>\n<td>QB-Core</td>\n<td>✅ Native</td>\n<td><code>Config.Framework = &#39;qb&#39;</code></td>\n</tr>\n<tr>\n<td>ESX (es_extended)</td>\n<td>✅ Native</td>\n<td><code>Config.Framework = &#39;esx&#39;</code></td>\n</tr>\n</tbody></table>\n<h2>Postal scripts (auto-detected)</h2>\n<table>\n<thead>\n<tr>\n<th>Script</th>\n<th>Auto-detect</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>nearest-postal</code></td>\n<td>✅</td>\n</tr>\n<tr>\n<td><code>Chow_Postal</code></td>\n<td>✅</td>\n</tr>\n<tr>\n<td><code>melons_postals</code></td>\n<td>✅</td>\n</tr>\n<tr>\n<td><code>jc-postal</code></td>\n<td>✅</td>\n</tr>\n<tr>\n<td><code>qb-postal</code></td>\n<td>✅</td>\n</tr>\n</tbody></table>\n<p>You can also point at a custom postal via <code>Config.PostalScript</code> + <code>Config.PostalExport</code>.</p>\n<h2>3D world markers (optional)</h2>\n<ul>\n<li><strong><code>sleepless_waypoints</code></strong> — when started AND <code>Config.Use3DWaypoint = true</code>, hold-E spawns a tall 3D checkpoint marker visible from far away</li>\n<li>Soft dependency — silently skipped if not started</li>\n</ul>\n<h2>Notification systems</h2>\n<pre><code class=\"language-lua\">Config.UseOxLib = false                     -- true = ox_lib, false = native QB notify\n</code></pre>\n<p>ESX users should set <code>UseOxLib = true</code> since native QB notify isn&#39;t available.</p>\n<h2>Other suty resources</h2>\n<table>\n<thead>\n<tr>\n<th>Resource</th>\n<th>Interaction</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>All other suty-</td>\n<td>No conflicts</td>\n</tr>\n</tbody></table>\n<h2>Known incompatibilities</h2>\n<ul>\n<li>Older versions of <code>qb-core</code> may not expose <code>QBCore.Functions.Notify</code> in the format expected. Set <code>UseOxLib = true</code> if you hit notify errors.</li>\n</ul>\n<p>Open a ticket on <a href=\"https://discord.gg/c2RFDBU9pc\">Discord</a> if you find a compatibility issue.</p>\n"
  },
  "suty-announce/troubleshooting": {
    "folderTitle": "Job Announcements",
    "pageTitle": "Troubleshooting",
    "crumb": [
      "Job Announcements",
      "Troubleshooting"
    ],
    "html": "<h1>Troubleshooting</h1>\n<h2>&quot;You don&#39;t have permission&quot; when running <code>/anuncio</code></h2>\n<p>Your current job isn&#39;t in <code>Config.AllowedJobs</code>. Either switch jobs or add yours to the config. The job NAME must match exactly (case-sensitive).</p>\n<h2><code>/evento</code> says &quot;no permission&quot; even as admin</h2>\n<p>The ACE check accepts both <code>admin</code> and <code>group.admin</code> ACE forms. Make sure you have one of them:</p>\n<pre><code class=\"language-cfg\">add_principal identifier.license:YOUR_LICENSE group.admin\n</code></pre>\n<h2>Banner doesn&#39;t show but command runs fine</h2>\n<p>Two checks:</p>\n<ol>\n<li><code>Config.AnnouncementDuration &gt; 0</code> — must be positive ms</li>\n<li>If the banner appears on YOUR screen but not others, <code>permissions.canSendGlobal</code> may be <code>false</code> for your job — only same-job players see it</li>\n</ol>\n<h2>Postal code shows &quot;RANDOM&quot;</h2>\n<p><code>Config.UsePostalScript = true</code> but no postal script is detected or started. Either:</p>\n<ul>\n<li>Install a supported postal script (<code>nearest-postal</code>, <code>qb-postal</code>, etc.)</li>\n<li>Set the actual postal in each job&#39;s config: <code>postalCode = &#39;10001&#39;</code> instead of <code>&#39;RANDOM&#39;</code></li>\n</ul>\n<h2>GPS waypoint doesn&#39;t appear on hold-E</h2>\n<ul>\n<li>Confirm <code>Config.EnableGPSMarking = true</code></li>\n<li>Confirm the job has a real <code>gps = vector3(...)</code> value (not <code>nil</code>)</li>\n<li>For 3D markers: <code>Config.Use3DWaypoint = true</code> AND <code>sleepless_waypoints</code> must be started</li>\n</ul>\n<h2>Anonymous event reveals my admin name</h2>\n<p>The PLAYER UI never shows who sent it — only the <strong>Discord webhook</strong> logs the identity (for audit). If your players can see the sender, you&#39;re using <code>/anuncio</code> or <code>/announce</code> instead of <code>/evento</code>.</p>\n<h2>Vehicle-follow doesn&#39;t track</h2>\n<p>In <code>config.lua</code>:</p>\n<pre><code class=\"language-lua\">Config.AnonymousEvent.VehicleFollow.Enabled = true\n</code></pre>\n<p>Also confirm the admin was actually inside a vehicle when sending. If they leave the car mid-broadcast, the waypoint stays on the abandoned vehicle.</p>\n<h2>Discord logs don&#39;t fire</h2>\n<ul>\n<li><code>Config.DiscordLogs.enabled = true</code></li>\n<li><code>webhook</code> is a valid Discord webhook URL (paste in a browser → you should see a JSON response, not an error)</li>\n</ul>\n<h2>Still stuck?</h2>\n<p><a href=\"https://discord.gg/c2RFDBU9pc\">Discord</a> — include your <code>Config.Framework</code> and which command is failing.</p>\n"
  },
  "suty-billing/README": {
    "folderTitle": "Tap Pay",
    "pageTitle": "Overview",
    "crumb": [
      "Tap Pay",
      "Overview"
    ],
    "html": "<h1>💳 suty-billing</h1>\n<p><code>Tap Pay</code> — a contactless payment terminal for FiveM businesses. Merchants on an allowed job pull out a tablet (real ped prop), customers walk up and tap to pay from a native lb-phone app. Cash moves bank → bank. No SQL, no menus, no typing player IDs.</p>\n<h2>✨ Key Features</h2>\n<ul>\n<li><strong>Real merchant tablet</strong> — <code>prop_cs_tablet</code> attached to the merchant&#39;s hand with a proper hold pose, walkable</li>\n<li><strong>Native lb-phone app</strong> — customers pay from their own phone, no extra UI to learn</li>\n<li><strong>Range-based discovery</strong> — customer&#39;s app finds the nearest active terminal within 2 m (configurable)</li>\n<li><strong>Multi-framework</strong> — QBX Core / QB-Core / ESX Legacy, auto-detected or forced via config</li>\n<li><strong>Multi-job out of the box</strong> — <code>Config.AllowedJobs = &#39;all&#39;</code> accepts every job registered in the active framework, with an exclude list for non-businesses</li>\n<li><strong>Server-side validation</strong> — job check, range check, balance check all enforced on the server</li>\n<li><strong>Zero database</strong> — no tables, no migrations, no persistent state</li>\n</ul>\n<h2>📦 Requirements</h2>\n<ul>\n<li><a href=\"https://github.com/overextended/ox_lib\">ox_lib</a></li>\n<li><a href=\"https://github.com/overextended/ox_inventory\">ox_inventory</a></li>\n<li><a href=\"https://docs.lbphone.com\">lb-phone</a> — required (customer app runs inside lb-phone, not compatible with qb-phone / qs-smartphone)</li>\n<li>One of: <a href=\"https://github.com/Qbox-project/qbx_core\">qbx_core</a> / qb-core / es_extended</li>\n</ul>\n<h2>🌐 Compatibility</h2>\n<table>\n<thead>\n<tr>\n<th>Framework</th>\n<th>Status</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>QBX Core (Qbox)</td>\n<td>✅ Native</td>\n</tr>\n<tr>\n<td>QB-Core</td>\n<td>✅ Native</td>\n</tr>\n<tr>\n<td>ESX Legacy 1.10+</td>\n<td>✅ Native</td>\n</tr>\n</tbody></table>\n<h2>🛍️ Get it</h2>\n<p><strong><a href=\"https://suty.dev\">Buy on suty.dev →</a></strong></p>\n<h2>📑 Next steps</h2>\n<ul>\n<li><a href=\"installation.md\">Installation</a></li>\n<li><a href=\"configuration.md\">Configuration</a></li>\n<li><a href=\"compatibility.md\">Compatibility</a></li>\n<li><a href=\"troubleshooting.md\">Troubleshooting</a></li>\n</ul>\n"
  },
  "suty-billing/installation": {
    "folderTitle": "Tap Pay",
    "pageTitle": "Installation",
    "crumb": [
      "Tap Pay",
      "Installation"
    ],
    "html": "<h1>Installation</h1>\n<h2>1. Drop the resource</h2>\n<p>Extract <code>suty-billing</code> into <code>resources/[suty]/suty-billing</code>.</p>\n<h2>2. Ensure dependencies and the resource</h2>\n<p><code>server.cfg</code>:</p>\n<pre><code class=\"language-cfg\">ensure ox_lib\nensure ox_inventory\nensure lb-phone\nensure qbx_core          # or qb-core, or es_extended\nensure suty-billing\n</code></pre>\n<div class=\"docs-hint docs-hint-warning\"><p><code>lb-phone</code> MUST start before <code>suty-billing</code> — the Tap Pay app registers during the latter&#39;s boot and won&#39;t show up if lb-phone isn&#39;t loaded yet.</p>\n</div><h2>3. Register the item in ox_inventory</h2>\n<p>Add this entry to <code>ox_inventory/data/items.lua</code> (also in <code>install/items.lua</code>):</p>\n<pre><code class=\"language-lua\">[&#39;payment_tablet&#39;] = {\n    label = &#39;Datafono&#39;,\n    weight = 500,\n    stack = false,\n    close = true,\n    description = &#39;Terminal de pago contactless para cobrar a clientes&#39;,\n    client = {\n        export = &#39;suty-billing.useTablet&#39;,\n    },\n},\n</code></pre>\n<p>Rename the item key (<code>payment_tablet</code>) freely — the resource never reads the name. The only thing it needs is <code>client.export = &#39;suty-billing.useTablet&#39;</code>, which must stay as written.</p>\n<p>Restart <code>ox_inventory</code> after saving.</p>\n<h2>4. Give yourself a test item</h2>\n<pre><code>giveitem 1 payment_tablet 1\n</code></pre>\n<h2>5. Verify</h2>\n<p><strong>As a merchant</strong> (player with an allowed job):</p>\n<ol>\n<li>Use the <code>payment_tablet</code> item → tablet appears in your hand with a hold pose, on-screen terminal NUI activates.</li>\n<li>Leave it active.</li>\n</ol>\n<p><strong>As a customer</strong> (on a separate test account or alt):</p>\n<ol>\n<li>Walk within 2 m of the merchant.</li>\n<li>Open your phone → open the <strong>Tap Pay</strong> app.</li>\n<li>Type an amount → tap <strong>Pay</strong>.</li>\n<li>App finds the nearby active terminal, server validates funds and transfers — bank → bank.</li>\n<li>The merchant sees a &quot;payment received&quot; notification + the terminal UI updates.</li>\n</ol>\n<div class=\"docs-hint docs-hint-info\"><p>The customer drives the payment from their own phone. The merchant doesn&#39;t enter the customer&#39;s ID — proximity is what links them.</p>\n</div><h2>6. (Optional) Restrict who can use it</h2>\n<p>Open <code>config.lua</code>. By default every job registered in your framework can use the tablet except those in <code>Config.ExcludeJobs</code>. To use an explicit whitelist instead, see <a href=\"configuration.md#allowedjobs\">Configuration → AllowedJobs</a>.</p>\n<h2>7. Tebex Escrow</h2>\n<p>Customers buying this through Tebex retain edit access to <code>config.lua</code>, <code>INSTALLATION.md</code>, and <code>install/items.lua</code> (already in <code>escrow_ignore</code>). They can rename the item, change job permissions, swap framework, and tune limits without source access.</p>\n"
  },
  "suty-billing/configuration": {
    "folderTitle": "Tap Pay",
    "pageTitle": "Configuration",
    "crumb": [
      "Tap Pay",
      "Configuration"
    ],
    "html": "<h1>Configuration</h1>\n<p>All settings live in <code>config.lua</code>. Edit, save, restart the resource. Everything below is documented in load order.</p>\n<h2>Terminal</h2>\n<pre><code class=\"language-lua\">Config.TerminalProp  = &#39;prop_cs_tablet&#39;\nConfig.TerminalRange = 2.0\n</code></pre>\n<table>\n<thead>\n<tr>\n<th>Key</th>\n<th>Default</th>\n<th>What it does</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>Config.TerminalProp</code></td>\n<td><code>&#39;prop_cs_tablet&#39;</code></td>\n<td>The prop attached to the merchant&#39;s right hand when the tablet is active. Swap to any other handheld prop hash.</td>\n</tr>\n<tr>\n<td><code>Config.TerminalRange</code></td>\n<td><code>2.0</code></td>\n<td>Meters within which a customer&#39;s lb-phone app will discover this merchant&#39;s terminal.</td>\n</tr>\n</tbody></table>\n<h2>Framework</h2>\n<pre><code class=\"language-lua\">Config.Framework = &#39;auto&#39;\n</code></pre>\n<table>\n<thead>\n<tr>\n<th>Value</th>\n<th>Effect</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>&#39;auto&#39;</code></td>\n<td>Detect at boot. Priority: <strong>qbx_core → qb-core → es_extended</strong>.</td>\n</tr>\n<tr>\n<td><code>&#39;qbx&#39;</code></td>\n<td>Force Qbox. Logs a warning + falls back to auto if <code>qbx_core</code> isn&#39;t started.</td>\n</tr>\n<tr>\n<td><code>&#39;qb&#39;</code></td>\n<td>Force QB-Core. Same fallback rule.</td>\n</tr>\n<tr>\n<td><code>&#39;esx&#39;</code></td>\n<td>Force ESX Legacy. Same fallback rule.</td>\n</tr>\n</tbody></table>\n<p>Boot log shows which one was picked:</p>\n<pre><code>[suty-billing] Bridge initialized — framework: qbx (auto)\n[suty-billing] Bridge initialized — framework: qbx (manual)\n</code></pre>\n<h2>AllowedJobs</h2>\n<pre><code class=\"language-lua\">Config.AllowedJobs = &#39;all&#39;\n\nConfig.ExcludeJobs = {\n    &#39;unemployed&#39;,\n}\n</code></pre>\n<p>Two modes:</p>\n<table>\n<thead>\n<tr>\n<th><code>Config.AllowedJobs</code> value</th>\n<th>Behavior</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>&#39;all&#39;</code></td>\n<td>Every job registered in the active framework is allowed, minus anything in <code>Config.ExcludeJobs</code>. New jobs you add to your framework later work automatically with no config change here.</td>\n</tr>\n<tr>\n<td><code>{ &#39;police&#39;, &#39;mechanic&#39;, ... }</code></td>\n<td>Legacy whitelist — only jobs in the array are allowed.</td>\n</tr>\n</tbody></table>\n<p>Source of &quot;all&quot; per framework:</p>\n<ul>\n<li><strong>QBX</strong> → <code>exports.qbx_core:GetJobs()</code></li>\n<li><strong>QB-Core</strong> → <code>QBCore.Shared.Jobs</code></li>\n<li><strong>ESX</strong> → <code>ESX.GetJobs()</code> <em>(ESX Legacy 1.10+)</em></li>\n</ul>\n<h3>Switching to a manual whitelist</h3>\n<p>Comment out the <code>&#39;all&#39;</code> line and uncomment the manual array template:</p>\n<pre><code class=\"language-lua\">Config.AllowedJobs = {\n    &#39;police&#39;, &#39;ambulance&#39;, &#39;mechanic&#39;, &#39;pizzeria&#39;, &#39;taco&#39;, &#39;bahamas&#39;,\n    -- … etc\n}\n</code></pre>\n<h2>Payment caps</h2>\n<pre><code class=\"language-lua\">Config.MinAmount = 1\nConfig.MaxAmount = 100000\n</code></pre>\n<p>Customer attempts outside this range are rejected by the server.</p>\n<h2>Notifications (Spanish)</h2>\n<pre><code class=\"language-lua\">Config.Notifications = {\n    terminalActive    = &#39;Terminal de pago activa&#39;,\n    terminalClosed    = &#39;Terminal de pago cerrada&#39;,\n    paymentSent       = &#39;Pago de $%s enviado exitosamente&#39;,\n    paymentReceived   = &#39;Pago de $%s recibido&#39;,\n    noTerminalNearby  = &#39;No hay terminal de pago cerca&#39;,\n    insufficientFunds = &#39;Fondos insuficientes&#39;,\n    invalidAmount     = &#39;Monto invalido&#39;,\n}\n</code></pre>\n<p>Translate the strings as needed. The <code>%s</code> in <code>paymentSent</code> and <code>paymentReceived</code> is required — it&#39;s where the amount is interpolated.</p>\n"
  },
  "suty-billing/compatibility": {
    "folderTitle": "Tap Pay",
    "pageTitle": "Compatibility",
    "crumb": [
      "Tap Pay",
      "Compatibility"
    ],
    "html": "<h1>Compatibility</h1>\n<h2>Frameworks</h2>\n<table>\n<thead>\n<tr>\n<th>Framework</th>\n<th>Status</th>\n<th>Notes</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>QBX Core (Qbox)</td>\n<td>✅ Native</td>\n<td>Default detection priority.</td>\n</tr>\n<tr>\n<td>QB-Core</td>\n<td>✅ Native</td>\n<td>Detected after QBX.</td>\n</tr>\n<tr>\n<td>ESX Legacy 1.10+</td>\n<td>✅ Native</td>\n<td>Uses <code>getSharedObject</code> export style + <code>ESX.GetJobs()</code> for the <code>&#39;all&#39;</code> allowlist mode.</td>\n</tr>\n<tr>\n<td>Older ESX (pre-1.10)</td>\n<td>⚠️ Partial</td>\n<td><code>ESX.GetJobs()</code> may not exist — use the manual whitelist form of <code>Config.AllowedJobs</code> instead of <code>&#39;all&#39;</code>.</td>\n</tr>\n</tbody></table>\n<p>Auto-detection runs at boot in priority order <strong>qbx_core → qb-core → es_extended</strong>. Override with <code>Config.Framework = &#39;qbx&#39; / &#39;qb&#39; / &#39;esx&#39;</code> if you run multiple side-by-side.</p>\n<h2>Inventory</h2>\n<ul>\n<li><strong>Required:</strong> <a href=\"https://github.com/overextended/ox_inventory\">ox_inventory</a></li>\n<li>The merchant tablet item is registered via <code>ox_inventory/data/items.lua</code> with <code>client.export = &#39;suty-billing.useTablet&#39;</code>. The resource will not start without <code>ox_inventory</code> in the started resources list.</li>\n<li>Not currently compatible with qb-inventory, qs-inventory, or ESX&#39;s native <code>items</code> SQL table.</li>\n</ul>\n<h2>Phone</h2>\n<ul>\n<li><strong>Required:</strong> <a href=\"https://docs.lbphone.com\">lb-phone</a></li>\n<li>The customer-facing &quot;Tap Pay&quot; app is registered as a native lb-phone custom app during the resource&#39;s boot. lb-phone must start <strong>before</strong> suty-billing.</li>\n<li>Not compatible with qb-phone, qs-smartphone, gksphone, or any non-lb-phone system.</li>\n</ul>\n<h2>Database</h2>\n<p>No tables are required. The resource keeps no persistent state — active terminals live in memory and clear on resource stop or player disconnect.</p>\n<h2>Bank account</h2>\n<p>Cash moves between <code>bank</code> accounts on both sides. If your server uses a different account name (e.g. <code>crypto</code>, <code>savings</code>), it isn&#39;t currently configurable — open a ticket if you need that.</p>\n"
  },
  "suty-billing/troubleshooting": {
    "folderTitle": "Tap Pay",
    "pageTitle": "Troubleshooting",
    "crumb": [
      "Tap Pay",
      "Troubleshooting"
    ],
    "html": "<h1>Troubleshooting</h1>\n<h2>Common issues</h2>\n<table>\n<thead>\n<tr>\n<th>Symptom</th>\n<th>Likely cause</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>Tablet doesn&#39;t open when the merchant uses the item</td>\n<td><code>lb-phone</code> not started, or the items.lua entry&#39;s <code>client.export</code> doesn&#39;t equal <code>&#39;suty-billing.useTablet&#39;</code></td>\n</tr>\n<tr>\n<td>Notification &quot;No tienes un negocio para usar esto&quot;</td>\n<td>Player&#39;s job isn&#39;t allowed by <code>Config.AllowedJobs</code> (or is in <code>Config.ExcludeJobs</code>)</td>\n</tr>\n<tr>\n<td>Customer&#39;s app says &quot;No hay terminal de pago cerca&quot;</td>\n<td>No merchant within <code>Config.TerminalRange</code> (default 2 m) has an active terminal — or the merchant is on a different routing bucket</td>\n</tr>\n<tr>\n<td>Customer&#39;s bank doesn&#39;t change after payment</td>\n<td>Confirm <code>qbx_core</code> / <code>qb-core</code> / <code>es_extended</code> is actually started and detected. Check the F8 / server console — should print <code>[suty-billing] Bridge initialized — framework: ...</code> at boot.</td>\n</tr>\n<tr>\n<td><code>attempt to index a nil value (global &#39;QBCore&#39;)</code></td>\n<td>Your framework resource isn&#39;t <code>ensure</code>d before <code>suty-billing</code> in <code>server.cfg</code></td>\n</tr>\n<tr>\n<td>Console prints <code>Bridge: no supported framework detected</code></td>\n<td>None of <code>qbx_core</code> / <code>qb-core</code> / <code>es_extended</code> was started before <code>suty-billing</code></td>\n</tr>\n<tr>\n<td>ESX + <code>&#39;all&#39;</code> mode rejects every job</td>\n<td>Your ESX build doesn&#39;t expose <code>ESX.GetJobs()</code> — switch <code>Config.AllowedJobs</code> to the manual array form</td>\n</tr>\n<tr>\n<td><code>Bridge: Config.Framework = &quot;qbx&quot; but &quot;qbx_core&quot; is not started</code></td>\n<td>You forced a framework that isn&#39;t running. Either start the matching framework or set <code>Config.Framework = &#39;auto&#39;</code></td>\n</tr>\n</tbody></table>\n<h2>Verifying the install</h2>\n<p>Run these checks in order — each one should print the expected line in F8 / server console after a clean restart of <code>suty-billing</code>:</p>\n<ol>\n<li><p><strong>Bridge boot</strong></p>\n<pre><code>[suty-billing] Bridge initialized — framework: qbx (auto)\n</code></pre>\n<p>Means the framework was found and the resource is ready.</p>\n</li>\n<li><p><strong>Item use</strong></p>\n<pre><code>(merchant uses payment_tablet → tablet appears in hand, terminal NUI opens)\n</code></pre>\n</li>\n<li><p><strong>lb-phone app present</strong>\nThe Tap Pay icon shows on the phone home screen. If it doesn&#39;t, lb-phone was started after suty-billing — restart suty-billing.</p>\n</li>\n</ol>\n<h2>Reporting a bug</h2>\n<p>If something&#39;s still broken after the checks above, open a ticket in our Discord with:</p>\n<ul>\n<li>Your server framework and version (QBX vs QB vs ESX, plus build number)</li>\n<li>The first 20 lines printed by suty-billing on resource start</li>\n<li>Whether <code>lb-phone</code> and <code>ox_inventory</code> are started before suty-billing in <code>server.cfg</code></li>\n<li>Exact reproduction steps</li>\n</ul>\n"
  },
  "suty-carry/README": {
    "folderTitle": "Player Carry",
    "pageTitle": "Overview",
    "crumb": [
      "Player Carry",
      "Overview"
    ],
    "html": "<h1>🤲 suty-carry</h1>\n<p>Carry players over the shoulder, in your arms, or cradle small ped models like a baby. Smooth attachment with custom animation dictionaries; auto-detaches on damage or sprint.</p>\n<h2>✨ Key Features</h2>\n<ul>\n<li><strong>3 carry styles</strong> — Fireman, Arms, Baby (with ped-model restriction)</li>\n<li><strong>Custom animation dicts</strong> OR default GTA animations (configurable)</li>\n<li><strong>Movement speed reduction</strong> while carrying (default 70% of normal)</li>\n<li><strong>Auto-detach on damage / sprint / jump</strong> (each configurable)</li>\n<li><strong>Carry request flow</strong> — sender asks, receiver accepts (mutual consent)</li>\n<li><strong>qbx_peds integration</strong> — baby-carry style restricted to toddler/kid models</li>\n<li><strong>Suty-emotes shared animation library</strong> — baby-carry uses <code>suty@parent2</code> / <code>suty@child2</code> dicts</li>\n<li><strong>Multi-notification systems</strong> — qb / esx / ox_lib / mythic / okok / custom</li>\n<li><strong>Optional job restrictions</strong></li>\n</ul>\n<h2>📦 Requirements</h2>\n<ul>\n<li>QBX Core / QB-Core / ESX</li>\n<li><strong>Optional:</strong> <a href=\"https://github.com/overextended/ox_lib\">ox_lib</a> — if <code>NotificationSystem = &quot;ox_lib&quot;</code></li>\n<li><strong>Optional:</strong> suty-emotes — provides shared animation dictionaries for the baby-carry style</li>\n<li><strong>Optional:</strong> qbx_peds — for the baby-carry ped-model restriction</li>\n</ul>\n<h2>🌐 Compatibility</h2>\n<table>\n<thead>\n<tr>\n<th>Framework</th>\n<th>Status</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>QBX Core (Qbox)</td>\n<td>✅</td>\n</tr>\n<tr>\n<td>QB-Core</td>\n<td>✅</td>\n</tr>\n<tr>\n<td>ESX (es_extended)</td>\n<td>✅</td>\n</tr>\n</tbody></table>\n<h2>🛍️ Get it</h2>\n<p><strong><a href=\"https://suty.dev\">Buy on suty.dev →</a></strong></p>\n<h2>📑 Next steps</h2>\n<ul>\n<li><a href=\"installation.md\">Installation</a></li>\n<li><a href=\"configuration.md\">Configuration</a></li>\n<li><a href=\"commands.md\">Commands</a></li>\n<li><a href=\"troubleshooting.md\">Troubleshooting</a></li>\n</ul>\n"
  },
  "suty-carry/installation": {
    "folderTitle": "Player Carry",
    "pageTitle": "Installation",
    "crumb": [
      "Player Carry",
      "Installation"
    ],
    "html": "<h1>Installation</h1>\n<h2>1. Drop the resource</h2>\n<p>Extract <code>suty-carry</code> into <code>resources/[suty]/suty-carry</code>.</p>\n<h2>2. Ensure in <code>server.cfg</code></h2>\n<pre><code class=\"language-cfg\">ensure suty-carry\n</code></pre>\n<p>That&#39;s it — no hard dependencies. If you want ox_lib notifications, also <code>ensure ox_lib</code>.</p>\n<h2>3. (Custom animations only) stream the animation files</h2>\n<p>If <code>Config.AnimationType = &quot;custom&quot;</code> (default), the custom dicts (<code>pcarryd1@animations</code>, <code>pcarryd2@animations</code>, <code>pcarryzt1@animations</code>, <code>pcarryzt2@animations</code>) must be streamed somewhere on your server.</p>\n<p>These dicts ship with suty-carry in the <code>stream/</code> folder. They auto-stream when the resource starts — no extra config needed.</p>\n<p>For the <strong>baby-carry</strong> style, the <code>suty@parent2</code> and <code>suty@child2</code> dicts come from <strong>suty-emotes</strong>. If you don&#39;t have suty-emotes, remove the <code>brazos</code> style from <code>config/carry.lua</code> or it&#39;ll silently fail.</p>\n<h2>4. Configure your notification system</h2>\n<p><code>config/carry.lua</code>:</p>\n<pre><code class=\"language-lua\">Config.NotificationSystem = &quot;qb&quot;    -- &quot;qb&quot; | &quot;esx&quot; | &quot;ox_lib&quot; | &quot;mythic&quot; | &quot;okokNotify&quot; | &quot;custom&quot;\n</code></pre>\n<p>For <code>&quot;custom&quot;</code>, edit the <code>Notify</code> function in <code>cl_carry.lua</code>.</p>\n<h2>5. Verify</h2>\n<p>Walk up to another player and run <code>/carry</code>. They&#39;ll get a request prompt. On accept, you pick up shoulder-style.</p>\n"
  },
  "suty-carry/configuration": {
    "folderTitle": "Player Carry",
    "pageTitle": "Configuration",
    "crumb": [
      "Player Carry",
      "Configuration"
    ],
    "html": "<h1>Configuration</h1>\n<p><code>config/carry.lua</code> at the resource root. Unencrypted and freely editable.</p>\n<h2>Notification + command</h2>\n<pre><code class=\"language-lua\">Config.NotificationSystem = &quot;qb&quot;            -- &quot;qb&quot; | &quot;esx&quot; | &quot;ox_lib&quot; | &quot;mythic&quot; | &quot;okokNotify&quot; | &quot;custom&quot;\nConfig.CarryCommand       = &quot;carry&quot;         -- /carry — open the carry request\n\nConfig.MaxCarryDistance         = 3.0\nConfig.MovementSpeedReduction   = 0.7       -- 70% of normal speed\nConfig.DisableSprintWhileCarrying = true\nConfig.DisableJumpWhileCarrying   = true\n\nConfig.AnimationCheckInterval = 1000        -- ms between animation health checks\n</code></pre>\n<h2>Animation backend</h2>\n<pre><code class=\"language-lua\">Config.AnimationType = &quot;custom&quot;             -- &quot;custom&quot; (streamed dicts) | &quot;default&quot; (GTA defaults)\n</code></pre>\n<ul>\n<li><strong><code>&quot;custom&quot;</code></strong> uses the high-quality animation dicts streamed with the resource (default).</li>\n<li><strong><code>&quot;default&quot;</code></strong> falls back to vanilla GTA animation dicts — no custom assets needed.</li>\n</ul>\n<p>The script auto-picks <code>Config.CarryStyles</code> based on this flag:</p>\n<pre><code class=\"language-lua\">Config.CarryStyles = Config.AnimationType == &quot;custom&quot;\n    and Config.CustomCarryStyles\n    or  Config.DefaultCarryStyles\n</code></pre>\n<h2>Carry styles</h2>\n<p>Each entry under <code>Config.CustomCarryStyles</code> (or <code>Config.DefaultCarryStyles</code>) is a named style with two pose definitions:</p>\n<pre><code class=\"language-lua\">arms = {\n    label = &quot;Arms Carry&quot;,\n    icon  = &quot;👐&quot;,\n    personCarrying = {\n        animDict = &quot;pcarryd1@animations&quot;,\n        anim     = &quot;pcarryd1clip&quot;,\n        flag     = 49,                      -- looping animation flag\n    },\n    personCarried = {\n        animDict = &quot;pcarryd2@animations&quot;,\n        anim     = &quot;pcarryd2clip&quot;,\n        attachX = -0.120, attachY = -0.150, attachZ = -0.050,\n        rotX = 0.0, rotY = 0.0, rotZ = -3.0,\n        boneIndex = 24818,                  -- spine bone for custom animations\n        flag = 33,\n    },\n},\n</code></pre>\n<p>Three styles ship by default:</p>\n<table>\n<thead>\n<tr>\n<th>Style</th>\n<th>Use case</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>arms</code></td>\n<td>Standard arms-around carry</td>\n</tr>\n<tr>\n<td><code>fireman</code></td>\n<td>Shoulder carry (over the shoulder)</td>\n</tr>\n<tr>\n<td><code>brazos</code></td>\n<td>Baby-cradle (locked to qbx_peds toddler models — <code>restrictToQbxPeds = true</code>)</td>\n</tr>\n</tbody></table>\n<h2>Permissions / job restrictions (optional)</h2>\n<pre><code class=\"language-lua\">Config.UsePermissions = false               -- master toggle for the below\nConfig.RestrictedJobs = {}                  -- jobs that CAN&#39;T carry (e.g. { &quot;prisoner&quot; })\nConfig.RequireJob     = false               -- true = only allowed jobs can carry\nConfig.AllowedJobs    = { &quot;police&quot;, &quot;ambulance&quot; }  -- only used when RequireJob = true\n</code></pre>\n<h2>Messages</h2>\n<pre><code class=\"language-lua\">Config.Messages = {\n    noOneNearby      = &quot;No hay nadie cerca para cargar!&quot;,\n    carryRequest     = &quot;Alguien quiere cargarte. Aceptar?&quot;,\n    requestSent      = &quot;Solicitud de carga enviada...&quot;,\n    requestDenied    = &quot;La solicitud fue rechazada&quot;,\n    requestAccepted  = &quot;Solicitud aceptada!&quot;,\n    tooFarAway       = &quot;El objetivo esta muy lejos!&quot;,\n    stoppedCarrying  = &quot;Dejaste de cargar&quot;,\n    beingCarried     = &quot;Te estan cargando&quot;,\n    carrying         = &quot;Estas cargando a alguien&quot;,\n    cantDoThat       = &quot;No puedes hacer eso mientras cargas a alguien!&quot;,\n}\n</code></pre>\n"
  },
  "suty-carry/commands": {
    "folderTitle": "Player Carry",
    "pageTitle": "Commands",
    "crumb": [
      "Player Carry",
      "Commands"
    ],
    "html": "<h1>Commands</h1>\n<table>\n<thead>\n<tr>\n<th>Command</th>\n<th>Description</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>/carry</code></td>\n<td>Open the carry request — sends a prompt to the nearest player within <code>Config.MaxCarryDistance</code>. They accept or deny.</td>\n</tr>\n<tr>\n<td><code>/carry</code> (while carrying)</td>\n<td>Drop the player you&#39;re currently carrying.</td>\n</tr>\n</tbody></table>\n<p>The command works as a toggle / contextual action — same command does pick-up / drop based on your current state.</p>\n<h2>Flow</h2>\n<ol>\n<li><strong>You run <code>/carry</code></strong> while standing next to another player</li>\n<li><strong>They get a prompt:</strong> &quot;Alguien quiere cargarte. Aceptar?&quot;</li>\n<li><strong>On accept:</strong> the script picks a carry style (arms / fireman / brazos) and attaches them to your ped</li>\n<li><strong>On deny:</strong> request closes, no carry happens</li>\n</ol>\n<h2>Auto-detach triggers</h2>\n<p>The script auto-drops the carried player when:</p>\n<ul>\n<li>You take damage (<code>Config.BreakOnDamage = true</code> is the default — though not in config, hard-coded behavior)</li>\n<li>You try to sprint (<code>Config.DisableSprintWhileCarrying = true</code>)</li>\n<li>You try to jump (<code>Config.DisableJumpWhileCarrying = true</code>)</li>\n<li>You run <code>/carry</code> again</li>\n</ul>\n<h2>Style selection</h2>\n<p>By default the script picks based on the carried ped&#39;s model:</p>\n<ul>\n<li><code>brazos</code> (baby cradle) — only if the carried ped is a qbx_peds toddler/kid model AND <code>restrictToQbxPeds = true</code></li>\n<li><code>arms</code> or <code>fireman</code> — otherwise (configurable in code)</li>\n</ul>\n<p>To change the default pick order, edit the carry-style selector in <code>cl_carry.lua</code>.</p>\n"
  },
  "suty-carry/compatibility": {
    "folderTitle": "Player Carry",
    "pageTitle": "Compatibility",
    "crumb": [
      "Player Carry",
      "Compatibility"
    ],
    "html": "<h1>Compatibility</h1>\n<h2>Frameworks</h2>\n<table>\n<thead>\n<tr>\n<th>Framework</th>\n<th>Status</th>\n<th>Notes</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>QBX Core (Qbox)</td>\n<td>✅</td>\n<td>qbx_peds integration unlocks the <code>brazos</code> (baby) style</td>\n</tr>\n<tr>\n<td>QB-Core</td>\n<td>✅</td>\n<td></td>\n</tr>\n<tr>\n<td>ESX (es_extended)</td>\n<td>✅</td>\n<td></td>\n</tr>\n</tbody></table>\n<h2>Animation backends</h2>\n<table>\n<thead>\n<tr>\n<th>Mode</th>\n<th>Requires</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>Config.AnimationType = &quot;custom&quot;</code></td>\n<td>Custom dicts shipped in <code>stream/</code>. Best visual quality.</td>\n</tr>\n<tr>\n<td><code>Config.AnimationType = &quot;default&quot;</code></td>\n<td>Vanilla GTA dicts — no streaming. Simpler / lighter.</td>\n</tr>\n</tbody></table>\n<h2>Notification systems</h2>\n<table>\n<thead>\n<tr>\n<th>Value</th>\n<th>Status</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>&quot;qb&quot;</code></td>\n<td>✅ (QB / QBX <code>Notify</code>)</td>\n</tr>\n<tr>\n<td><code>&quot;esx&quot;</code></td>\n<td>✅</td>\n</tr>\n<tr>\n<td><code>&quot;ox_lib&quot;</code></td>\n<td>✅ Recommended</td>\n</tr>\n<tr>\n<td><code>&quot;mythic&quot;</code></td>\n<td>✅</td>\n</tr>\n<tr>\n<td><code>&quot;okokNotify&quot;</code></td>\n<td>✅</td>\n</tr>\n<tr>\n<td><code>&quot;custom&quot;</code></td>\n<td>Edit <code>Notify</code> function in <code>cl_carry.lua</code></td>\n</tr>\n</tbody></table>\n<h2>Optional integrations</h2>\n<table>\n<thead>\n<tr>\n<th>Resource</th>\n<th>Effect</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><strong>suty-emotes</strong></td>\n<td>Provides <code>suty@parent2</code> / <code>suty@child2</code> dicts for the <code>brazos</code> style — required if you keep that style</td>\n</tr>\n<tr>\n<td><strong>qbx_peds</strong></td>\n<td>When carrying a qbx_peds toddler ped, <code>brazos</code> style activates automatically</td>\n</tr>\n</tbody></table>\n<h2>Other suty resources</h2>\n<table>\n<thead>\n<tr>\n<th>Resource</th>\n<th>Interaction</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>suty-emotes</td>\n<td>Shared animation library (baby-carry pose)</td>\n</tr>\n<tr>\n<td>suty-drag (carry handcuffed)</td>\n<td><strong>Different resource</strong> — that one&#39;s for dragging handcuffed players</td>\n</tr>\n<tr>\n<td>All other suty-</td>\n<td>No conflicts</td>\n</tr>\n</tbody></table>\n<h2>Known incompatibilities</h2>\n<ul>\n<li>Custom anti-cheats that flag <code>AttachEntityToEntity</code> calls may block the carry attach. Whitelist <code>suty-carry</code> in your anti-cheat&#39;s resource list.</li>\n</ul>\n<p>Report compatibility issues on <a href=\"https://discord.gg/c2RFDBU9pc\">Discord</a>.</p>\n"
  },
  "suty-carry/troubleshooting": {
    "folderTitle": "Player Carry",
    "pageTitle": "Troubleshooting",
    "crumb": [
      "Player Carry",
      "Troubleshooting"
    ],
    "html": "<h1>Troubleshooting</h1>\n<h2>Carried player floats / is in the wrong pose</h2>\n<p>The attach offsets in <code>config/carry.lua</code> are tuned per animation dict. If you switch <code>Config.AnimationType</code> from <code>&quot;custom&quot;</code> to <code>&quot;default&quot;</code> (or vice versa), the attach offsets won&#39;t match the new anim — adjust <code>attachX/Y/Z</code> and <code>rotX/Y/Z</code> for the new dict.</p>\n<h2>&quot;Baby Carry&quot; style does nothing on adult peds</h2>\n<p>By design — <code>brazos</code> has <code>restrictToQbxPeds = true</code>, so it only triggers on qbx_peds toddler/kid models. To allow adults: set <code>restrictToQbxPeds = false</code> for the <code>brazos</code> entry. The pose will look ridiculous on adults but it&#39;ll work.</p>\n<h2>Animation doesn&#39;t play / &quot;Animation failed to load&quot;</h2>\n<ul>\n<li>The required animation dict failed to stream. For custom dicts, check that <code>stream/</code> ships them and the resource started cleanly.</li>\n<li>For the <code>brazos</code> style: <code>suty-emotes</code> must be installed (provides <code>suty@parent2</code> / <code>suty@child2</code>).</li>\n</ul>\n<h2>Movement speed isn&#39;t reduced</h2>\n<p><code>Config.MovementSpeedReduction = 0.7</code> should slow you to 70%. If it doesn&#39;t work:</p>\n<ul>\n<li>Other resources may be overriding <code>SetPlayerMaxStamina</code> or <code>SetPedMaxMoveBlendRatio</code></li>\n<li>Check <code>Config.AnimationCheckInterval</code> — default 1000ms; the speed clamp re-applies on each tick</li>\n</ul>\n<h2>Player keeps being able to sprint / jump while carrying</h2>\n<ul>\n<li><code>Config.DisableSprintWhileCarrying = true</code></li>\n<li><code>Config.DisableJumpWhileCarrying = true</code></li>\n</ul>\n<p>If you&#39;ve added a custom keybind script that fires <code>+sprint</code> directly, it can bypass the disable. Standard SHIFT-to-sprint is correctly blocked.</p>\n<h2>&quot;Cannot do that while carrying&quot;</h2>\n<p>This is the <code>Config.Messages.cantDoThat</code> message. You&#39;re trying to interact with something (use weapons, open inventory, drive) while still carrying. Drop first (<code>/carry</code> again), then do the action.</p>\n<h2>Notification system shows wrong messages</h2>\n<p><code>Config.NotificationSystem</code> doesn&#39;t match what&#39;s installed. Confirm with <code>start</code> console — does the listed notify resource actually appear in the started list?</p>\n<h2>Still stuck?</h2>\n<p><a href=\"https://discord.gg/c2RFDBU9pc\">Discord</a> — include <code>Config.AnimationType</code>, <code>Config.NotificationSystem</code>, and which carry style you were attempting.</p>\n"
  },
  "suty-drag/README": {
    "folderTitle": "Drag System",
    "pageTitle": "Overview",
    "crumb": [
      "Drag System",
      "Overview"
    ],
    "html": "<h1>🏁 suty-drag</h1>\n<p>Full drag-racing toolkit: installable nitro kits with engine-blow risk, a mechanic-only ECU tablet for tuning vehicles (boost / ignition / fuel / timing), tracked usage that punishes spam, and visual effects for the perfect launch.</p>\n<h2>✨ Key Features</h2>\n<ul>\n<li><strong>Nitro kits</strong> — installable per vehicle via item, configurable safe-use count before engine damage starts</li>\n<li><strong>ECU tablet</strong> — mechanic-only tablet for scanning + tuning vehicles. Realistic scan duration, individual tunable parameters</li>\n<li><strong>Engine blow mechanics</strong> — push the nitro too hard and the engine fails dramatically</li>\n<li><strong>jg-mechanic integration</strong> (optional) — tablet shows real maintenance data</li>\n<li><strong>lc_fuel integration</strong> (optional) — aggressive tunes burn extra fuel</li>\n<li><strong>Multi-framework</strong> — auto-detect QBX / QB-Core / ESX</li>\n<li><strong>Spanish + English locales</strong> — switch via single config option</li>\n</ul>\n<h2>📦 Requirements</h2>\n<ul>\n<li><a href=\"https://github.com/overextended/ox_lib\">ox_lib</a></li>\n<li><a href=\"https://github.com/overextended/ox_inventory\">ox_inventory</a></li>\n<li><a href=\"https://github.com/overextended/ox_target\">ox_target</a></li>\n<li>QBX Core / QB-Core / ESX</li>\n<li><strong>Optional:</strong> <a href=\"https://jgscripts.com/\">jg-mechanic</a> — for live maintenance data in the ECU tablet</li>\n<li><strong>Optional:</strong> <a href=\"https://docs.loaf-scripts.com/\">lc_fuel</a> — for tune-based fuel consumption</li>\n</ul>\n<h2>🌐 Compatibility</h2>\n<table>\n<thead>\n<tr>\n<th>Framework</th>\n<th>Status</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>QBX Core (Qbox)</td>\n<td>✅</td>\n</tr>\n<tr>\n<td>QB-Core</td>\n<td>✅</td>\n</tr>\n<tr>\n<td>ESX (es_extended)</td>\n<td>✅</td>\n</tr>\n</tbody></table>\n<h2>🛍️ Get it</h2>\n<p><strong><a href=\"https://suty.dev\">Buy on suty.dev →</a></strong></p>\n<h2>📑 Next steps</h2>\n<ul>\n<li><a href=\"installation.md\">Installation</a></li>\n<li><a href=\"configuration.md\">Configuration</a></li>\n<li><a href=\"commands.md\">Commands</a></li>\n<li><a href=\"troubleshooting.md\">Troubleshooting</a></li>\n</ul>\n"
  },
  "suty-drag/installation": {
    "folderTitle": "Drag System",
    "pageTitle": "Installation",
    "crumb": [
      "Drag System",
      "Installation"
    ],
    "html": "<h1>Installation</h1>\n<h2>1. Drop the resource</h2>\n<p>Extract <code>suty-drag</code> into <code>resources/[suty]/suty-drag</code>.</p>\n<h2>2. Ensure dependencies</h2>\n<p><code>server.cfg</code>:</p>\n<pre><code class=\"language-cfg\">ensure ox_lib\nensure ox_inventory\nensure ox_target\nensure suty-drag\n</code></pre>\n<h2>3. Install SQL</h2>\n<p>The resource folder contains <code>[install]/</code> with a SQL file. Run it once against your database to create the tuning/state tables (the file is included separately so it doesn&#39;t auto-run — gives you control over when schema changes hit prod).</p>\n<pre><code class=\"language-bash\">mysql -u root -p YOUR_DB_NAME &lt; &quot;resources/[suty]/suty-drag/[install]/install.sql&quot;\n</code></pre>\n<p>Or paste the contents into HeidiSQL / phpMyAdmin.</p>\n<h2>4. Items</h2>\n<p><code>suty-drag</code> ships three items that you need to register in your inventory before they&#39;ll work in-game:</p>\n<table>\n<thead>\n<tr>\n<th>Item key</th>\n<th>Name</th>\n<th>Purpose</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>drag_tablet</code></td>\n<td>Drag Tuning Tablet</td>\n<td>Open the tuning UI to modify engine, top speed, traction. Use near your drag car.</td>\n</tr>\n<tr>\n<td><code>drag_nitro</code></td>\n<td>Kit de Nitro</td>\n<td>Adds a nitrous kit to a drag car. Permanent until consumed. Overuse blows the engine.</td>\n</tr>\n<tr>\n<td><code>drag_engine_kit</code></td>\n<td>Kit de Reparación de Motor</td>\n<td>Mechanic-only. Repairs blown drag engines via skillcheck.</td>\n</tr>\n</tbody></table>\n<p>Pick the tab that matches your inventory and paste the snippet into the listed file. <strong>You only need one option</strong> — the script auto-detects which framework is running.</p>\n<h4>ox_inventory</h4>\n<p>Add to <code>ox_inventory/data/items.lua</code>:</p>\n<pre><code class=\"language-lua\">[&quot;drag_tablet&quot;] = {\n    label = &quot;Drag Tuning Tablet&quot;,\n    weight = 5,\n    stack = false,\n    close = true,\n    consume = 0,\n    description = &quot;Professional tablet for drag racing tuning. Modify engine performance, top speed, traction, and more. Stand near your drag car to use.&quot;,\n    client = {\n        image = &quot;drag_tablet.png&quot;,\n        export = &quot;suty-drag.useTablet&quot;,\n    },\n},\n\n[&quot;drag_nitro&quot;] = {\n    label = &quot;Kit de Nitro&quot;,\n    weight = 5000,\n    stack = true,\n    close = true,\n    consume = 0,\n    description = &quot;Sistema de óxido nitroso para autos de carreras. Instálalo en tu auto drag para obtener impulso extra. El nitro permanece en el vehículo hasta que se agote o se remueva. ADVERTENCIA: el uso excesivo puede explotar el motor.&quot;,\n    client = {\n        image = &quot;nitrous_bottle.png&quot;,\n        export = &quot;suty-drag.drag_nitro&quot;,\n    },\n},\n\n[&quot;drag_engine_kit&quot;] = {\n    label = &quot;Kit de Reparación de Motor&quot;,\n    weight = 5000,\n    stack = false,\n    close = true,\n    consume = 0,\n    description = &quot;Kit profesional para reparar motores fundidos de autos drag. Solo puede ser usado por mecánicos. Requiere completar un skillcheck para reparar exitosamente.&quot;,\n},\n</code></pre>\n<h4>qb-core / qbx</h4>\n<p>Add to <code>qb-core/shared/items.lua</code> (also works for <code>qbx_core</code> shared items):</p>\n<pre><code class=\"language-lua\">drag_tablet = {\n    name = &quot;drag_tablet&quot;,\n    label = &quot;Drag Tuning Tablet&quot;,\n    weight = 5,\n    type = &quot;item&quot;,\n    image = &quot;drag_tablet.png&quot;,\n    unique = true,\n    useable = true,\n    shouldClose = true,\n    description = &quot;Professional tablet for drag racing tuning. Modify engine performance, top speed, traction, and more. Stand near your drag car to use.&quot;,\n},\n\ndrag_nitro = {\n    name = &quot;drag_nitro&quot;,\n    label = &quot;Kit de Nitro&quot;,\n    weight = 5000,\n    type = &quot;item&quot;,\n    image = &quot;nitrous_bottle.png&quot;,\n    unique = false,\n    useable = true,\n    shouldClose = true,\n    description = &quot;Sistema de óxido nitroso para autos de carreras. Instálalo en tu auto drag para obtener impulso extra. ADVERTENCIA: el uso excesivo puede explotar el motor.&quot;,\n},\n\ndrag_engine_kit = {\n    name = &quot;drag_engine_kit&quot;,\n    label = &quot;Kit de Reparación de Motor&quot;,\n    weight = 5000,\n    type = &quot;item&quot;,\n    image = &quot;drag_engine_kit.png&quot;,\n    unique = true,\n    useable = true,\n    shouldClose = true,\n    description = &quot;Kit profesional para reparar motores fundidos de autos drag. Solo puede ser usado por mecánicos.&quot;,\n},\n</code></pre>\n<h4>ESX (default inventory)</h4>\n<div class=\"docs-hint docs-hint-info\"><p>If you&#39;re running ESX <strong>with</strong> <code>ox_inventory</code>, use the <strong>ox_inventory</strong> tab instead. The SQL approach below is only for ESX&#39;s default inventory.</p>\n</div><p>Run this SQL once against your server&#39;s database:</p>\n<pre><code class=\"language-sql\">INSERT INTO `items` (`name`, `label`, `weight`, `rare`, `can_remove`) VALUES\n    (&#39;drag_tablet&#39;,     &#39;Drag Tuning Tablet&#39;,          5,    0, 1),\n    (&#39;drag_nitro&#39;,      &#39;Kit de Nitro&#39;,                5000, 0, 1),\n    (&#39;drag_engine_kit&#39;, &#39;Kit de Reparación de Motor&#39;,  5000, 0, 1);\n</code></pre>\n<p>ESX item registration is then handled automatically in <code>server/main.lua</code> via <code>ESX.RegisterUsableItem()</code> — no additional exports needed.</p>\n<div class=\"docs-hint docs-hint-warning\"><p>Make sure the <strong>image filenames</strong> (<code>drag_tablet.png</code>, <code>nitrous_bottle.png</code>, <code>drag_engine_kit.png</code>) exist in your inventory&#39;s image folder. If you renamed them, update the <code>image</code> field in the snippets accordingly.</p>\n</div><h2>5. Mechanic job &amp; ECU tablet access</h2>\n<p>The ECU tablet is restricted to a job + grade. Defaults to <code>mechanic</code> grade <code>3+</code>. Adjust in <code>config/tablet.lua</code>:</p>\n<pre><code class=\"language-lua\">Config.Tablet.jobRestriction = {\n    job      = &#39;mechanic&#39;,\n    minGrade = 3,\n}\n</code></pre>\n<p>Set <code>job = false</code> to allow anyone.</p>\n<h2>6. Verify</h2>\n<ul>\n<li>As an allowed mechanic, you should be able to use the tablet item to scan a drag vehicle near you.</li>\n<li>As a regular player, use the nitro kit item near your drag car — installation animation runs and the kit is consumed.</li>\n<li>Drive the car and press the configured key (default LSHIFT) to fire nitro.</li>\n</ul>\n"
  },
  "suty-drag/configuration": {
    "folderTitle": "Drag System",
    "pageTitle": "Configuration",
    "crumb": [
      "Drag System",
      "Configuration"
    ],
    "html": "<h1>Configuration</h1>\n<p>Configuration is split across <strong>6 files</strong> in <code>config/</code>, all unencrypted and freely editable.</p>\n<table>\n<thead>\n<tr>\n<th>File</th>\n<th>Owns</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>config/main.lua</code></td>\n<td>Framework, locale, debug</td>\n</tr>\n<tr>\n<td><code>config/nitro.lua</code></td>\n<td>Nitro kit, control key, safe-use count, engine-blow risk</td>\n</tr>\n<tr>\n<td><code>config/tablet.lua</code></td>\n<td>ECU tablet job restriction, scan duration, integrations</td>\n</tr>\n<tr>\n<td><code>config/vehicles.lua</code></td>\n<td>Vehicle whitelist (which cars are &quot;drag cars&quot;)</td>\n</tr>\n<tr>\n<td><code>config/effects.lua</code></td>\n<td>Visual effects (boost particles, exhaust flames)</td>\n</tr>\n<tr>\n<td><code>config/ui.lua</code></td>\n<td>Tablet UI styling, themes, accent colors</td>\n</tr>\n</tbody></table>\n<h2><code>main.lua</code> — globals</h2>\n<pre><code class=\"language-lua\">Config.Framework = &#39;qbx&#39;        -- &#39;auto&#39; | &#39;qbx&#39; | &#39;qb&#39; | &#39;esx&#39;\nConfig.Locale    = &#39;es&#39;         -- &#39;es&#39; | &#39;en&#39;\nConfig.Debug     = false        -- F8 console debug spam\n</code></pre>\n<h2><code>nitro.lua</code> — kit + risk</h2>\n<pre><code class=\"language-lua\">Config.Nitro = {\n    enabled  = true,\n    itemName = &#39;drag_nitro&#39;,                  -- ox_inventory item\n\n    integration = {\n        useJGMechanic = false,                -- jg-mechanic native detection\n        controlKey    = 21,                   -- 21 = LSHIFT\n    },\n\n    installation = {\n        installTime     = 10000,              -- ms to install\n        installDistance = 2.0,                -- meters from vehicle\n        checkOwnership  = false,              -- require vehicle keys to install\n    },\n\n    safeUses    = 10,                         -- uses before damage risk starts\n    checkInterval = 100,                      -- ms between active-use checks\n}\n</code></pre>\n<p>After <code>safeUses</code>, every additional firing rolls a damage chance — push too hard and you blow the engine.</p>\n<h2><code>tablet.lua</code> — ECU scanner</h2>\n<pre><code class=\"language-lua\">Config.Tablet = {\n    jobRestriction = {\n        job      = &#39;mechanic&#39;,                -- false = anyone\n        minGrade = 3,                         -- 0 = any grade\n    },\n\n    scanDuration = 25,                        -- seconds (5-120 range)\n}\n</code></pre>\n<p>The tablet is its own ox_inventory item — its name is also configurable in <code>tablet.lua</code>. When used, the player aims at a vehicle and a scan animation runs for the duration above, then the tuning UI opens.</p>\n<h2><code>vehicles.lua</code> — allowed cars</h2>\n<p>A whitelist of vehicle models that the resource treats as drag cars. The nitro kit refuses to install on vehicles outside this list (so players can&#39;t put nitro on a city bus).</p>\n<h2><code>effects.lua</code> — visuals</h2>\n<p>Boost cloud, exhaust flame, screen-shake intensity. Each effect is toggleable individually.</p>\n<h2><code>ui.lua</code> — tablet styling</h2>\n<p>CSS-variable equivalents for the tablet&#39;s NUI — accent color, font, layout density.</p>\n"
  },
  "suty-drag/commands": {
    "folderTitle": "Drag System",
    "pageTitle": "Commands",
    "crumb": [
      "Drag System",
      "Commands"
    ],
    "html": "<h1>Commands</h1>\n<p>This resource is mostly <strong>item-driven</strong> — most interactions go through <code>ox_inventory</code> items + <code>ox_target</code> prompts rather than chat commands.</p>\n<h2>Items</h2>\n<table>\n<thead>\n<tr>\n<th>Item</th>\n<th>Used by</th>\n<th>Action</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>drag_nitro</code></td>\n<td>Anyone</td>\n<td>Installs a nitro kit on the nearest drag-class vehicle (within <code>Config.Nitro.installation.installDistance</code>)</td>\n</tr>\n<tr>\n<td>ECU Tablet</td>\n<td>Mechanic (grade ≥ 3 by default)</td>\n<td>Aim at a drag vehicle → scan animation runs → tuning UI opens</td>\n</tr>\n</tbody></table>\n<p>The actual item name for the ECU tablet is set in <code>config/tablet.lua</code>.</p>\n<h2>In-vehicle controls</h2>\n<table>\n<thead>\n<tr>\n<th>Input</th>\n<th>Action</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><strong>LSHIFT</strong> (default, see <code>controlKey</code> in <code>nitro.lua</code>)</td>\n<td>Fire nitro</td>\n</tr>\n</tbody></table>\n<p>Nitro fires only while the player is in a drag-class vehicle that has a kit installed AND is below the engine-damage threshold.</p>\n<h2>Tablet workflow</h2>\n<ol>\n<li>Mechanic uses the tablet item</li>\n<li>Aim at a vehicle within range</li>\n<li>Scan animation runs for <code>Config.Tablet.scanDuration</code> seconds</li>\n<li>UI opens with tunable parameters (boost / ignition / fuel / timing)</li>\n<li>If <code>useJGMechanic = true</code> and jg-mechanic is started, maintenance data is shown</li>\n<li>Apply tune → vehicle&#39;s state is updated and persisted to the DB</li>\n</ol>\n<h2>Aggressive tunes &amp; lc_fuel</h2>\n<p>If <code>lc_fuel</code> is detected, aggressive tune values multiply base fuel consumption (up to ~2.5x at extreme settings). Set the multiplier in <code>config/tablet.lua</code> if you want to scale this.</p>\n"
  },
  "suty-drag/compatibility": {
    "folderTitle": "Drag System",
    "pageTitle": "Compatibility",
    "crumb": [
      "Drag System",
      "Compatibility"
    ],
    "html": "<h1>Compatibility</h1>\n<h2>Frameworks</h2>\n<table>\n<thead>\n<tr>\n<th>Framework</th>\n<th>Status</th>\n<th>Notes</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>QBX Core (Qbox)</td>\n<td>✅ Native</td>\n<td>Default</td>\n</tr>\n<tr>\n<td>QB-Core</td>\n<td>✅ Native</td>\n<td>Set <code>Config.Framework = &#39;qb&#39;</code></td>\n</tr>\n<tr>\n<td>ESX (es_extended)</td>\n<td>✅ Native</td>\n<td>Set <code>Config.Framework = &#39;esx&#39;</code></td>\n</tr>\n<tr>\n<td>Auto-detect</td>\n<td>✅</td>\n<td><code>Config.Framework = &#39;auto&#39;</code></td>\n</tr>\n</tbody></table>\n<h2>Optional integrations</h2>\n<table>\n<thead>\n<tr>\n<th>Resource</th>\n<th>Toggle</th>\n<th>Effect</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><a href=\"https://jgscripts.com/\">jg-mechanic</a></td>\n<td><code>Config.Nitro.integration.useJGMechanic = true</code></td>\n<td>ECU tablet shows live maintenance data (oil, brakes, tires) during scans</td>\n</tr>\n<tr>\n<td><a href=\"https://docs.loaf-scripts.com/\">lc_fuel</a></td>\n<td>Auto-detected if started</td>\n<td>Aggressive tunes burn extra fuel (up to ~2.5x at extreme settings)</td>\n</tr>\n</tbody></table>\n<h2>Inventory</h2>\n<p>Required: <strong>ox_inventory</strong>. The nitro kit and ECU tablet are both ox_inventory items. Not compatible with qb-inventory or qs-inventory out of the box.</p>\n<h2>Vehicle systems</h2>\n<p>Compatible with any vehicle persistence layer that uses stable plate keys (qbx_vehicles, qb-garage, esx_advancedgarage, etc.). Tunes persist via vehicle plate — if your garage system rotates plates, tunes won&#39;t survive a session.</p>\n<h2>Anti-cheat</h2>\n<p>The nitro applies a temporary power boost via <code>ApplyForceToEntity</code> and modifies max speed natively. Most anti-cheats accept this if the script is in their whitelist:</p>\n<ul>\n<li><strong>FiveGuard</strong> — whitelist <code>suty-drag</code> in its resource policy</li>\n<li><strong>Badger</strong> — usually no action needed</li>\n<li><strong>Eulen-detect</strong> — whitelist <code>suty-drag</code> for vehicle speed/force checks</li>\n</ul>\n<h2>Other suty resources</h2>\n<table>\n<thead>\n<tr>\n<th>Resource</th>\n<th>Interaction</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>All other suty-</td>\n<td>No conflicts</td>\n</tr>\n</tbody></table>\n<h2>Known incompatibilities</h2>\n<ul>\n<li>Some custom handling scripts that re-apply per-frame <code>SetVehicleEnginePower</code> will fight nitro&#39;s boost. Disable handling overrides on drag vehicles or use a vehicle-class exception.</li>\n</ul>\n<p>Report issues on <a href=\"https://discord.gg/c2RFDBU9pc\">Discord</a>.</p>\n"
  },
  "suty-drag/troubleshooting": {
    "folderTitle": "Drag System",
    "pageTitle": "Troubleshooting",
    "crumb": [
      "Drag System",
      "Troubleshooting"
    ],
    "html": "<h1>Troubleshooting</h1>\n<h2>&quot;Este vehículo no es un auto de carreras drag&quot;</h2>\n<p>The vehicle isn&#39;t in the whitelist. Open <code>config/vehicles.lua</code> and add the model name (the spawn name, lowercase). Restart <code>suty-drag</code> after editing.</p>\n<h2>&quot;Debes estar cerca de tu vehículo&quot;</h2>\n<p>The install distance is <code>Config.Nitro.installation.installDistance</code> meters (default 2.0). Get closer or bump it in <code>config/nitro.lua</code>.</p>\n<h2>Nitro key doesn&#39;t fire</h2>\n<p>Three things:</p>\n<ol>\n<li><code>Config.Nitro.integration.controlKey</code> — make sure it matches the FiveM control index you intend. Default <code>21</code> = LSHIFT.</li>\n<li><code>Config.Nitro.enabled = true</code> — flag must be on.</li>\n<li>The vehicle must have a kit installed AND not be past the engine-damage threshold. Check F8 console with <code>Config.Debug = true</code>.</li>\n</ol>\n<h2>Engine blows after one nitro use</h2>\n<p>You&#39;re firing the kit too aggressively or <code>safeUses</code> is set too low. Open <code>config/nitro.lua</code>:</p>\n<pre><code class=\"language-lua\">safeUses = 10,    -- increase if you want more grace\n</code></pre>\n<h2>ECU tablet won&#39;t open / &quot;No tienes permisos&quot;</h2>\n<p>The job restriction in <code>config/tablet.lua</code> is blocking you. Either:</p>\n<ul>\n<li>Make yourself the required job + grade (<code>/setjob &lt;id&gt; mechanic 3</code>)</li>\n<li>Lower the requirement: <code>Config.Tablet.jobRestriction.minGrade = 0</code></li>\n<li>Disable entirely: <code>Config.Tablet.jobRestriction.job = false</code></li>\n</ul>\n<h2>Tablet shows &quot;Maintenance data unavailable&quot;</h2>\n<p>You have <code>useJGMechanic = true</code> but jg-mechanic isn&#39;t started, or the vehicle was never serviced. Either start jg-mechanic, or set <code>useJGMechanic = false</code> to hide the section.</p>\n<h2>Tune values don&#39;t persist between sessions</h2>\n<p>The vehicle state is keyed by plate. If your server wipes plates on restart (some custom vehicle systems do), tunes won&#39;t persist. Check that vehicle plates are stable.</p>\n<h2>Still stuck?</h2>\n<p>Open a ticket on <a href=\"https://discord.gg/c2RFDBU9pc\">Discord</a> with:</p>\n<ul>\n<li>Your <code>Config.Framework</code> value</li>\n<li>Whether the issue is nitro, tablet, or persistence</li>\n<li>F8 console output (with <code>Config.Debug = true</code>)</li>\n</ul>\n"
  },
  "suty-drugsell/README": {
    "folderTitle": "Drug Sell System",
    "pageTitle": "Overview",
    "crumb": [
      "Drug Sell System",
      "Overview"
    ],
    "html": "<h1>💊 suty-drugsell</h1>\n<p>Full street-level drug-selling economy. Players sell to AI customers inside admin-drawn polygon zones; customers negotiate, rob, or snitch based on their behavior weights. Cops get silent dispatch alerts. Trap-phone app on lb-phone for managing inventory and territory.</p>\n<h2>✨ Key Features</h2>\n<ul>\n<li><strong>Polygon zone creator</strong> — 2D Leaflet map or 3D in-world freecam (same UI family as suty-zonasegura)</li>\n<li><strong>Drug catalog</strong> — configure name, item, price, addiction rate per drug</li>\n<li><strong>Customer AI</strong> — three behavior types per spawn: friendly, robber, snitch</li>\n<li><strong>Police dispatch integration</strong> — silent alerts on snitch / failed sale</li>\n<li><strong>lb-phone trap-phone app</strong> — inventory, territory map, heat tracking</li>\n<li><strong>OP-Crime / OP-Gangs turf integration</strong> (optional)</li>\n<li><strong>Multi-language</strong> — Spanish and English locales</li>\n<li><strong>Multi-framework</strong> — QBX Core / QB-Core / ESX</li>\n</ul>\n<h2>📦 Requirements</h2>\n<ul>\n<li><a href=\"https://github.com/overextended/ox_lib\">ox_lib</a></li>\n<li><a href=\"https://github.com/overextended/oxmysql\">oxmysql</a></li>\n<li><a href=\"https://github.com/overextended/ox_target\">ox_target</a></li>\n<li><a href=\"https://github.com/overextended/ox_inventory\">ox_inventory</a></li>\n<li><a href=\"https://docs.lbphone.com\">lb-phone</a> — for the trap-phone app</li>\n<li>MariaDB / MySQL</li>\n</ul>\n<h2>🌐 Compatibility</h2>\n<table>\n<thead>\n<tr>\n<th>Framework</th>\n<th>Status</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>QBX Core (Qbox)</td>\n<td>✅</td>\n</tr>\n<tr>\n<td>QB-Core</td>\n<td>✅</td>\n</tr>\n<tr>\n<td>ESX (es_extended)</td>\n<td>✅</td>\n</tr>\n</tbody></table>\n<h2>🛍️ Get it</h2>\n<p><strong><a href=\"https://suty.dev\">Buy on suty.dev →</a></strong></p>\n<h2>📑 Next steps</h2>\n<ul>\n<li><a href=\"installation.md\">Installation</a></li>\n<li><a href=\"configuration.md\">Configuration</a></li>\n<li><a href=\"commands.md\">Commands</a></li>\n<li><a href=\"events.md\">Events &amp; Handlers</a> — hook drug sales into XP / loyalty / Discord systems</li>\n<li><a href=\"troubleshooting.md\">Troubleshooting</a></li>\n</ul>\n"
  },
  "suty-drugsell/installation": {
    "folderTitle": "Drug Sell System",
    "pageTitle": "Installation",
    "crumb": [
      "Drug Sell System",
      "Installation"
    ],
    "html": "<h1>Installation</h1>\n<h2>1. Drop the resource</h2>\n<p>Extract <code>suty-drugsell</code> into <code>resources/[suty]/suty-drugsell</code>.</p>\n<h2>2. Ensure dependencies and the resource</h2>\n<p><code>server.cfg</code>:</p>\n<pre><code class=\"language-cfg\">ensure ox_lib\nensure oxmysql\nensure ox_target\nensure ox_inventory\nensure lb-phone           # must boot BEFORE suty-drugsell so the trap-phone app registers\nensure suty-drugsell\n</code></pre>\n<div class=\"docs-hint docs-hint-warning\"><p><code>lb-phone</code> MUST start before <code>suty-drugsell</code> — the trap-phone app registers during the latter&#39;s boot and silently no-ops if lb-phone isn&#39;t loaded yet.</p>\n</div><h2>3. Database</h2>\n<p>All tables are created automatically on first start. Verify with:</p>\n<pre><code class=\"language-sql\">SHOW TABLES LIKE &#39;suty_drugsell_%&#39;;\n</code></pre>\n<p>You should see at least: <code>suty_drugsell_zones</code>, <code>suty_drugsell_dealers</code>, <code>suty_drugsell_sales</code>.</p>\n<h2>4. Items (ox_inventory)</h2>\n<p>The drug items used by the script live in <code>config/drugs.lua</code>. Each entry references an <code>item</code> name that <strong>must exist in <code>ox_inventory/data/items.lua</code></strong>. If you&#39;re using vanilla drug items (<code>coke_small</code>, <code>weed_bag</code>, etc.) you&#39;re set. For custom items, add them to ox_inventory first.</p>\n<h2>5. Grant admin permission</h2>\n<p>The zone creator command requires <code>group.admin</code>:</p>\n<pre><code class=\"language-cfg\">add_principal identifier.license:YOUR_LICENSE group.admin\n</code></pre>\n<h2>6. Create your first zone</h2>\n<p>As admin, in-game:</p>\n<pre><code>/creardrugzone\n</code></pre>\n<p>The same zone creator UI as suty-zonasegura opens. Draw a polygon, name it, save.</p>\n<h2>7. Verify</h2>\n<p>Walk into the saved zone carrying a drug item that matches one in <code>config/drugs.lua</code>. Customer NPCs should start spawning according to the <code>Config.SpawnIntervalMs</code> setting.</p>\n<h2>8. (Optional) OP-Crime / OP-Gangs</h2>\n<p>If you have <code>op-crime</code> or <code>op-gangs</code> installed and want turf-based drug economics, open <code>config/main.lua</code> and set:</p>\n<pre><code class=\"language-lua\">Config.AdditionalScripts = {\n    op_Gangs = true,\n}\n</code></pre>\n<p>See <code>OP-CRIME-INTEGRATION.md</code> in the resource folder for the full integration matrix.</p>\n"
  },
  "suty-drugsell/configuration": {
    "folderTitle": "Drug Sell System",
    "pageTitle": "Configuration",
    "crumb": [
      "Drug Sell System",
      "Configuration"
    ],
    "html": "<h1>Configuration</h1>\n<p>Configuration is split across <strong>5 files</strong> in <code>config/</code> — all unencrypted and freely editable.</p>\n<table>\n<thead>\n<tr>\n<th>File</th>\n<th>Owns</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>config/main.lua</code></td>\n<td>Framework, commands, debug, interaction, zone toggle</td>\n</tr>\n<tr>\n<td><code>config/drugs.lua</code></td>\n<td>Drug catalog (item, price, addiction, demand)</td>\n</tr>\n<tr>\n<td><code>config/customers.lua</code></td>\n<td>Customer AI: spawn rate, behavior weights, ped models</td>\n</tr>\n<tr>\n<td><code>config/police.lua</code></td>\n<td>Cop dispatch chance, alert text, integrations</td>\n</tr>\n<tr>\n<td><code>config/trapphone.lua</code></td>\n<td>lb-phone app config (icon, theme, allowed jobs)</td>\n</tr>\n</tbody></table>\n<h2><code>main.lua</code> — global flags</h2>\n<pre><code class=\"language-lua\">Config.Framework = &#39;qbx&#39;              -- &#39;qbx&#39; | &#39;qb&#39; | &#39;esx&#39;\nConfig.Locale    = &#39;es&#39;               -- &#39;es&#39; | &#39;en&#39;\nConfig.Debug     = false              -- console debug spam\n\nConfig.Notification = {\n    Type = &#39;ox&#39;,                      -- &#39;ox&#39; | &#39;qbcore&#39; | &#39;okok&#39; | &#39;mythic&#39; | &#39;custom&#39;\n}\n\nConfig.Interaction = {\n    UseTarget = true,                 -- true: ox_target | false: drawtext+E\n    Distance  = 2.5,                  -- drawtext distance (UseTarget=false only)\n}\n\nConfig.Commands = {\n    Start = &#39;venderdrogas&#39;,           -- player command to start selling\n    Stop  = &#39;pararventa&#39;,             -- player command to stop\n}\n\nConfig.AdminCommands = {\n    CreateZone = &#39;creardrugzone&#39;,     -- opens zone creator\n    ToggleZone = &#39;toggledrugzone&#39;,    -- /toggledrugzone &lt;id&gt;\n}\n\nConfig.DefaultCreator   = &#39;map&#39;       -- &#39;map&#39; (Leaflet) | &#39;freecam&#39; (in-world)\nConfig.RestrictToZones  = true        -- false = sell anywhere\n\nConfig.AdditionalScripts = {\n    op_Gangs = false,                 -- OP-Crime/OP-Gangs turf integration\n}\n</code></pre>\n<h2><code>drugs.lua</code> — drug catalog</h2>\n<p>Each drug entry describes ONE saleable substance:</p>\n<pre><code class=\"language-lua\">Config.Drugs[&#39;weed&#39;] = {\n    label             = &#39;Marihuana&#39;,\n    item              = &#39;weed_bag&#39;,         -- must match ox_inventory item name\n    basePrice         = 50,                 -- base $ per sale (before negotiation)\n    addictionPerSale  = 0.02,               -- 0-1, how much customer addiction increases\n    weight            = 1.0,                -- demand multiplier (relative to other drugs)\n}\n</code></pre>\n<h2><code>customers.lua</code> — customer AI</h2>\n<pre><code class=\"language-lua\">Config.SpawnIntervalMs = 30000             -- min ms between customer spawns per zone\nConfig.MaxConcurrent   = 3                 -- max active customers per zone\nConfig.SpawnRadius     = { min = 15, max = 40 }  -- spawn distance from dealer\n\nConfig.Behaviors = {\n    friendly = { weight = 70 },            -- pays + leaves\n    robber   = { weight = 15 },            -- attempts rob if dealer is alone\n    snitch   = { weight = 15 },            -- calls cops via dispatch\n}\n\nConfig.PedModels = { &#39;a_m_y_business_01&#39;, &#39;a_m_y_stbla_01&#39;, ... }\n</code></pre>\n<p>Behavior <code>weight</code>s are relative — they don&#39;t need to sum to 100.</p>\n<h2><code>police.lua</code> — dispatch</h2>\n<pre><code class=\"language-lua\">Config.PoliceJobs       = { &#39;police&#39;, &#39;sheriff&#39;, &#39;bcso&#39; }\nConfig.SnitchAlertText  = &#39;Posible venta de drogas reportada&#39;\nConfig.SnitchUseDispatch = true            -- true: ps-dispatch / pma-dispatch / qb-dispatch\nConfig.DispatchResource = &#39;ps-dispatch&#39;    -- which dispatch to use\n</code></pre>\n<h2><code>trapphone.lua</code> — lb-phone app</h2>\n<pre><code class=\"language-lua\">Config.Phone = {\n    AppName        = &#39;Trap&#39;,\n    AppIcon        = &#39;icon.png&#39;,           -- in html/trapphone/\n    AccentColor    = &#39;#16a34a&#39;,\n    AllowedJobs    = nil,                  -- nil = anyone; or { &#39;unemployed&#39;, &#39;cannabis&#39; }\n}\n</code></pre>\n<p>See <code>TRAPPHONE-GUIDE.md</code> in the resource folder for the full app feature list.</p>\n"
  },
  "suty-drugsell/commands": {
    "folderTitle": "Drug Sell System",
    "pageTitle": "Commands",
    "crumb": [
      "Drug Sell System",
      "Commands"
    ],
    "html": "<h1>Commands</h1>\n<h2>Player</h2>\n<table>\n<thead>\n<tr>\n<th>Command</th>\n<th>Description</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>/venderdrogas</code></td>\n<td>Toggle dealer mode ON. While ON and standing inside a zone, customers will approach to buy.</td>\n</tr>\n<tr>\n<td><code>/pararventa</code></td>\n<td>Toggle dealer mode OFF. Customers stop spawning for you.</td>\n</tr>\n</tbody></table>\n<p>Both commands work as a single toggle — running the same command twice flips the state.</p>\n<h2>Admin</h2>\n<p>All admin commands require <code>group.admin</code> ACE.</p>\n<table>\n<thead>\n<tr>\n<th>Command</th>\n<th>Description</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>/creardrugzone</code></td>\n<td>Opens the zone creator (default UI from <code>Config.DefaultCreator</code>)</td>\n</tr>\n<tr>\n<td><code>/creardrugzone map</code></td>\n<td>Forces the 2D Leaflet creator</td>\n</tr>\n<tr>\n<td><code>/creardrugzone freecam</code></td>\n<td>Forces the 3D in-world creator</td>\n</tr>\n<tr>\n<td><code>/toggledrugzone &lt;id&gt;</code></td>\n<td>Enables/disables a saved zone (drug economy on/off in that polygon)</td>\n</tr>\n</tbody></table>\n<h2>Trap-phone app</h2>\n<p>There are no chat commands for the trap-phone app — it&#39;s accessed through the <strong>lb-phone</strong> UI like any other app. Once installed and configured, players use it to:</p>\n<ul>\n<li>Track total sales by drug type</li>\n<li>View their current heat / police attention</li>\n<li>See active dealers in the area (if their job allows)</li>\n<li>Manage their territory presence</li>\n</ul>\n<h2>Map / freecam creator workflow</h2>\n<p>Same as suty-zonasegura — see its <a href=\"../suty-zonasegura/commands.md\">Commands page</a> for the full workflow (left-click vertices, right-click undo, drag to adjust, save with name).</p>\n"
  },
  "suty-drugsell/events": {
    "folderTitle": "Drug Sell System",
    "pageTitle": "Events",
    "crumb": [
      "Drug Sell System",
      "Events"
    ],
    "html": "<h1>🪝 Events &amp; Handlers</h1>\n<p><code>suty-drugsell</code> fires public events on key moments of the sale flow. Other\nresources can listen to these events to add their own behavior — XP systems,\nstat trackers, Discord webhooks, gang loyalty, achievements, leaderboards —\n<strong>without modifying or shipping the suty-drugsell source</strong>.</p>\n<p>Two events fire on a successful sale: one on the server, one on the seller&#39;s\nclient. Both deliver the same payload table.</p>\n<blockquote>\n<p>ℹ️ <strong>Today, only successful sales fire an event.</strong> Refused sales, failed\nvalidations, and &quot;sale started&quot; do not have hooks yet. See <a href=\"#what-does-not-fire\">What does NOT\nfire</a> at the bottom.</p>\n</blockquote>\n<hr>\n<h2>Server event — <code>suty-drugsell:sold</code></h2>\n<p>Fires once per successful sale. Listen from any server-side script.</p>\n<pre><code class=\"language-lua\">AddEventHandler(&#39;suty-drugsell:sold&#39;, function(src, payload)\n    -- src      : number    player server id (the seller)\n    -- payload  : table\n    --\n    --   payload.drug         : string  -- item name sold (e.g. &#39;cocaine_bag&#39;)\n    --   payload.quantity     : number  -- units sold this transaction\n    --   payload.totalPrice   : number  -- total money received\n    --   payload.pricePerUnit : number  -- random per-unit price used\n    --   payload.pedNet       : number  -- network id of the customer NPC\nend)\n</code></pre>\n<p>It&#39;s a plain <code>TriggerEvent</code> (not a <code>NetEvent</code>), so only <strong>other server-side\nresources</strong> can listen — players cannot spoof it from the client.</p>\n<hr>\n<h2>Client event — <code>suty-drugsell:client:sold</code></h2>\n<p>Fires on the <strong>seller&#39;s client</strong> right after the sale completes. Useful for\nfloating XP popups, sound effects, NUI notifications, screen flashes.</p>\n<pre><code class=\"language-lua\">RegisterNetEvent(&#39;suty-drugsell:client:sold&#39;, function(payload)\n    -- Same payload as the server event (no `src` — it&#39;s the local player).\n    --   payload.drug         : string\n    --   payload.quantity     : number\n    --   payload.totalPrice   : number\n    --   payload.pricePerUnit : number\nend)\n</code></pre>\n<hr>\n<h2>Examples</h2>\n<h3>1. Award XP per sale (server)</h3>\n<pre><code class=\"language-lua\">AddEventHandler(&#39;suty-drugsell:sold&#39;, function(src, p)\n    local xp = math.floor(p.totalPrice * 0.1)              -- 10% of revenue\n    exports[&#39;my-xp&#39;]:AddXp(src, &#39;dealing&#39;, xp)\nend)\n</code></pre>\n<h3>2. Per-drug XP rates (server)</h3>\n<pre><code class=\"language-lua\">local XP = {\n    weed_bag    = 4,\n    cocaine_bag = 12,\n    meth_bag    = 18,\n}\n\nAddEventHandler(&#39;suty-drugsell:sold&#39;, function(src, p)\n    local perUnit = XP[p.drug] or 5\n    exports[&#39;my-xp&#39;]:AddXp(src, &#39;dealing&#39;, perUnit * p.quantity)\nend)\n</code></pre>\n<h3>3. Track total dollars sold (server)</h3>\n<pre><code class=\"language-lua\">local total = {}   -- [citizenid] = number\n\nAddEventHandler(&#39;suty-drugsell:sold&#39;, function(src, p)\n    local Player = exports.qbx_core:GetPlayer(src)\n    if not Player then return end\n    local cid = Player.PlayerData.citizenid\n    total[cid] = (total[cid] or 0) + p.totalPrice\nend)\n</code></pre>\n<h3>4. Discord webhook log (server)</h3>\n<pre><code class=\"language-lua\">AddEventHandler(&#39;suty-drugsell:sold&#39;, function(src, p)\n    local name = GetPlayerName(src) or &#39;unknown&#39;\n    PerformHttpRequest(&#39;https://discord.com/api/webhooks/...&#39;, function() end, &#39;POST&#39;,\n        json.encode({\n            embeds = {{\n                title = &#39;Drug Sale&#39;,\n                color = 15844367,\n                fields = {\n                    { name = &#39;Player&#39;, value = (&#39;%s (src %d)&#39;):format(name, src), inline = false },\n                    { name = &#39;Drug&#39;,   value = p.drug,                            inline = true  },\n                    { name = &#39;Amount&#39;, value = (&#39;%dx&#39;):format(p.quantity),        inline = true  },\n                    { name = &#39;Income&#39;, value = (&#39;$%d&#39;):format(p.totalPrice),      inline = true  },\n                },\n            }},\n        }),\n        { [&#39;Content-Type&#39;] = &#39;application/json&#39; })\nend)\n</code></pre>\n<h3>5. Floating XP popup on the client</h3>\n<pre><code class=\"language-lua\">RegisterNetEvent(&#39;suty-drugsell:client:sold&#39;, function(p)\n    local xp = math.floor(p.totalPrice * 0.1)\n    lib.notify({\n        title = &#39;Dealing XP&#39;,\n        description = (&#39;+%d XP for selling %dx %s&#39;):format(xp, p.quantity, p.drug),\n        type = &#39;success&#39;,\n    })\nend)\n</code></pre>\n<h3>6. Gang loyalty (server)</h3>\n<pre><code class=\"language-lua\">AddEventHandler(&#39;suty-drugsell:sold&#39;, function(src, p)\n    local Player = exports.qbx_core:GetPlayer(src)\n    if not Player then return end\n    local gang = Player.PlayerData.gang and Player.PlayerData.gang.name\n    if not gang or gang == &#39;none&#39; then return end\n    exports[&#39;my-gangs&#39;]:AddLoyalty(gang, math.ceil(p.totalPrice / 50))\nend)\n</code></pre>\n<hr>\n<h2>Listening from a brand-new resource</h2>\n<p>A minimal add-on that listens to drug sales is just two files:</p>\n<p><code>fxmanifest.lua</code></p>\n<pre><code class=\"language-lua\">fx_version &#39;cerulean&#39;\ngame &#39;gta5&#39;\n\nserver_script &#39;server.lua&#39;\nclient_script &#39;client.lua&#39;   -- only if you need the client hook\n</code></pre>\n<p><code>server.lua</code></p>\n<pre><code class=\"language-lua\">AddEventHandler(&#39;suty-drugsell:sold&#39;, function(src, p)\n    -- your logic here\nend)\n</code></pre>\n<p><code>client.lua</code></p>\n<pre><code class=\"language-lua\">RegisterNetEvent(&#39;suty-drugsell:client:sold&#39;, function(p)\n    -- your UI here\nend)\n</code></pre>\n<p>That&#39;s it. <strong>No dependency on <code>suty-drugsell</code></strong>, no access to its source\nrequired, and the add-on keeps working across updates as long as payload\nfield names stay the same (they will — new fields may be added but existing\nones won&#39;t be renamed or removed).</p>\n<hr>\n<h2>What does NOT fire <a id=\"what-does-not-fire\"></a></h2>\n<p>For transparency, these flows have <strong>no public hook today</strong>. Open a ticket on\nDiscord if you need one and we&#39;ll add it:</p>\n<ul>\n<li>A sale that&#39;s <strong>refused</strong> (NPC undercover cop / police alert chance)</li>\n<li>A sale that <strong>fails validation</strong> (no drugs in inventory, not in zone, etc.)</li>\n<li>A player <strong>starting</strong> a sale conversation (before the deal lands)</li>\n<li><strong>Police alerts</strong> triggered by a sale</li>\n</ul>\n<hr>\n<h2>Compatibility note</h2>\n<p>The events have been stable since version <code>1.1.0</code>. If you&#39;re on an older\nbuild, update before relying on them.</p>\n"
  },
  "suty-drugsell/compatibility": {
    "folderTitle": "Drug Sell System",
    "pageTitle": "Compatibility",
    "crumb": [
      "Drug Sell System",
      "Compatibility"
    ],
    "html": "<h1>Compatibility</h1>\n<h2>Frameworks</h2>\n<table>\n<thead>\n<tr>\n<th>Framework</th>\n<th>Status</th>\n<th>Notes</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>QBX Core (Qbox)</td>\n<td>✅ Native</td>\n<td>Default. Uses <code>ox_inventory</code> for items.</td>\n</tr>\n<tr>\n<td>QB-Core</td>\n<td>✅ Native</td>\n<td>Set <code>Config.Framework = &#39;qb&#39;</code>. Uses QB-Core inventory.</td>\n</tr>\n<tr>\n<td>ESX (es_extended)</td>\n<td>✅ Native</td>\n<td>Set <code>Config.Framework = &#39;esx&#39;</code>. Uses ESX inventory.</td>\n</tr>\n</tbody></table>\n<h2>Inventories</h2>\n<p>The resource expects items to be defined in the active framework&#39;s inventory:</p>\n<ul>\n<li><strong>QBX</strong> → <code>ox_inventory/data/items.lua</code></li>\n<li><strong>QB-Core</strong> → <code>qb-core/shared/items.lua</code></li>\n<li><strong>ESX</strong> → <code>items</code> SQL table</li>\n</ul>\n<p>The <code>item</code> value in <code>config/drugs.lua</code> must match a name in your inventory&#39;s item list.</p>\n<h2>Dispatch systems</h2>\n<p><code>config/police.lua</code> → <code>Config.DispatchResource</code> accepts:</p>\n<ul>\n<li><code>ps-dispatch</code></li>\n<li><code>pma-dispatch</code></li>\n<li><code>qb-dispatch</code></li>\n<li><code>cd_dispatch</code></li>\n<li>Set <code>Config.SnitchUseDispatch = false</code> to fall back to direct cop notifications</li>\n</ul>\n<h2>Phone</h2>\n<ul>\n<li><strong>Required:</strong> <a href=\"https://docs.lbphone.com\">lb-phone</a></li>\n<li>The trap-phone app is a custom lb-phone app installed at boot</li>\n<li>Not compatible with qb-phone, qs-smartphone, or other phone systems</li>\n</ul>\n<h2>Optional integrations</h2>\n<table>\n<thead>\n<tr>\n<th>Resource</th>\n<th>Toggle</th>\n<th>Effect</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>op-crime / op-gangs</td>\n<td><code>Config.AdditionalScripts.op_Gangs = true</code></td>\n<td>Turf-based drug economy (prices/demand scale with gang control)</td>\n</tr>\n</tbody></table>\n<h2>Other suty resources</h2>\n<table>\n<thead>\n<tr>\n<th>Resource</th>\n<th>Interaction</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>suty-zonasegura</td>\n<td>Zone creator UI is the same family — no conflict</td>\n</tr>\n<tr>\n<td>suty-landmark</td>\n<td>No interaction needed</td>\n</tr>\n<tr>\n<td>All other suty-</td>\n<td>No conflicts</td>\n</tr>\n</tbody></table>\n<h2>Known incompatibilities</h2>\n<p>None reported. Open a ticket on <a href=\"https://discord.gg/c2RFDBU9pc\">Discord</a> if you find one.</p>\n"
  },
  "suty-drugsell/troubleshooting": {
    "folderTitle": "Drug Sell System",
    "pageTitle": "Troubleshooting",
    "crumb": [
      "Drug Sell System",
      "Troubleshooting"
    ],
    "html": "<h1>Troubleshooting</h1>\n<h2>Customers don&#39;t spawn</h2>\n<p>Three things to check, in order:</p>\n<ol>\n<li><strong>You&#39;re inside a zone</strong> — Walk into a saved drug zone and toggle <code>/venderdrogas</code>. Outside zones, no spawns happen (unless <code>Config.RestrictToZones = false</code>).</li>\n<li><strong>You carry a drug item</strong> — Customers approach dealers who hold at least one item matching <code>config/drugs.lua</code>. An empty-handed dealer triggers no spawns.</li>\n<li><strong>Spawn rate is sane</strong> — Open <code>config/customers.lua</code> and confirm <code>Config.SpawnIntervalMs</code> isn&#39;t set to an absurdly high value.</li>\n</ol>\n<h2>&quot;El UI personalizado no aparece&quot; / freecam HUD invisible</h2>\n<p>If the freecam creator opens but the HUD card doesn&#39;t show, you may be on an older build where <code>body { display: none }</code> in the zonecreator CSS was hiding sibling overlays. Updated versions ship with the body always laid out and a <code>body.active</code> class that toggles the dark backdrop only when the leaflet creator is open. Update the resource if you&#39;re on an old build.</p>\n<h2>Customers walk up but immediately call cops</h2>\n<p>This is the <code>snitch</code> behavior firing. Open <code>config/customers.lua</code> and lower the snitch weight:</p>\n<pre><code class=\"language-lua\">Config.Behaviors = {\n    friendly = { weight = 80 },\n    robber   = { weight = 15 },\n    snitch   = { weight = 5 },        -- was 15\n}\n</code></pre>\n<p>Weights are relative, not percentages.</p>\n<h2>Trap-phone app doesn&#39;t appear in lb-phone</h2>\n<p><code>lb-phone</code> must boot <strong>before</strong> <code>suty-drugsell</code>. Check your <code>server.cfg</code> ensure order. After fixing, restart both resources (<code>refresh; restart lb-phone; restart suty-drugsell</code>).</p>\n<h2>Dispatch alerts aren&#39;t reaching cops</h2>\n<p>Open <code>config/police.lua</code> and confirm:</p>\n<ul>\n<li><code>Config.SnitchUseDispatch = true</code></li>\n<li><code>Config.DispatchResource</code> matches your actual dispatch script (<code>ps-dispatch</code>, <code>pma-dispatch</code>, <code>qb-dispatch</code>)</li>\n<li>The dispatch resource is started</li>\n</ul>\n<p>If you don&#39;t use a dispatch script, set <code>Config.SnitchUseDispatch = false</code> — alerts will fall back to direct <code>TriggerClientEvent</code> to on-duty cops.</p>\n<h2>&quot;Item &#39;weed_bag&#39; is not configured in ox_inventory&quot;</h2>\n<p>The <code>item</code> value in <code>config/drugs.lua</code> references an inventory item that doesn&#39;t exist. Either:</p>\n<ol>\n<li>Add the missing item to <code>ox_inventory/data/items.lua</code>, or</li>\n<li>Change the <code>item</code> field in <code>drugs.lua</code> to an existing item name</li>\n</ol>\n<h2>OP-Crime/OP-Gangs turf integration not working</h2>\n<p><code>Config.AdditionalScripts.op_Gangs</code> must be <code>true</code> AND the <code>op-gangs</code> resource must be started <strong>before</strong> <code>suty-drugsell</code>. The integration hooks into op-gangs&#39; turf events at script load.</p>\n<h2>Still stuck?</h2>\n<p>Open a ticket on <a href=\"https://discord.gg/c2RFDBU9pc\">Discord</a> with:</p>\n<ul>\n<li>Your <code>Config.Framework</code> value</li>\n<li>Whether the issue is in the creator UI, dealing flow, or trap-phone app</li>\n<li>F8 console output (client) or server console errors</li>\n</ul>\n"
  },
  "suty-moneythrow/README": {
    "folderTitle": "Money Throwing",
    "pageTitle": "Overview",
    "crumb": [
      "Money Throwing",
      "Overview"
    ],
    "html": "<h1>💸 suty-moneythrow</h1>\n<p>Throw stacks of cash in the air with a make-it-rain animation, physics-based bill props, particle effects, and optional pickup mechanics. Bundled with a pole-dance system (MagicCity emotes) for strip-club locations — dancers earn the thrown bills.</p>\n<h2>✨ Key Features</h2>\n<ul>\n<li><strong><code>/tirardinero &lt;amount&gt;</code></strong> — throw cash with anim + sound + particles</li>\n<li><strong>Physics-based bill props</strong> that scatter on landing</li>\n<li><strong>Configurable pickup</strong> — uncollected bills despawn after a timer; can be picked up by anyone via ox_target</li>\n<li><strong>Pole dance integration</strong> — vanilla strip club poles wired with 10 MagicCity dance emotes</li>\n<li><strong><code>/pole</code> admin tool</strong> — position custom dance coords on any pole</li>\n<li><strong>Cancel mid-throw</strong> with a configurable key (refunds money)</li>\n<li><strong>Multi-framework</strong> — QBX / QB-Core / ESX</li>\n<li><strong>GTA-native poles always work</strong> (1-3) regardless of custom dance config</li>\n</ul>\n<h2>📦 Requirements</h2>\n<ul>\n<li>QBX Core / QB-Core / ESX</li>\n<li><a href=\"https://github.com/overextended/ox_target\">ox_target</a> — for the pickup interactions</li>\n<li><strong>Optional:</strong> MagicCity dance animations — if <code>Config.EnableCustomDances = true</code>, the dicts <code>pandora@magiccityv1.1</code> through <code>.10</code> must be streamed</li>\n</ul>\n<h2>🌐 Compatibility</h2>\n<table>\n<thead>\n<tr>\n<th>Framework</th>\n<th>Status</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>QBX Core (Qbox)</td>\n<td>✅</td>\n</tr>\n<tr>\n<td>QB-Core</td>\n<td>✅</td>\n</tr>\n<tr>\n<td>ESX (es_extended)</td>\n<td>✅</td>\n</tr>\n</tbody></table>\n<h2>🛍️ Get it</h2>\n<p><strong><a href=\"https://suty.dev\">Buy on suty.dev →</a></strong></p>\n<h2>📑 Next steps</h2>\n<ul>\n<li><a href=\"installation.md\">Installation</a></li>\n<li><a href=\"configuration.md\">Configuration</a></li>\n<li><a href=\"commands.md\">Commands</a></li>\n<li><a href=\"troubleshooting.md\">Troubleshooting</a></li>\n</ul>\n"
  },
  "suty-moneythrow/installation": {
    "folderTitle": "Money Throwing",
    "pageTitle": "Installation",
    "crumb": [
      "Money Throwing",
      "Installation"
    ],
    "html": "<h1>Installation</h1>\n<h2>1. Drop the resource</h2>\n<p>Extract <code>suty-moneythrow</code> into <code>resources/[suty]/suty-moneythrow</code>.</p>\n<h2>2. Ensure in <code>server.cfg</code></h2>\n<pre><code class=\"language-cfg\">ensure ox_lib\nensure ox_target\nensure suty-moneythrow\n</code></pre>\n<h2>3. Set framework</h2>\n<p><code>config.lua</code>:</p>\n<pre><code class=\"language-lua\">Config.Framework = &#39;qbx&#39;    -- &#39;qbx&#39; | &#39;qb&#39; | &#39;esx&#39;\n</code></pre>\n<h2>4. (Optional) Disable custom pole dances</h2>\n<p>If you don&#39;t have the MagicCity dance dicts (<code>pandora@magiccityv1.1</code> through <code>.10</code>) streamed, disable custom dances — vanilla GTA pole dances (1-3) still work:</p>\n<pre><code class=\"language-lua\">Config.EnableCustomDances = false\n</code></pre>\n<h2>5. Verify</h2>\n<p>In-game run:</p>\n<pre><code>/tirardinero 1000\n</code></pre>\n<p>Your character throws $1000 in the air (deducted from your cash account). Bills scatter on the ground and can be picked up via ox_target within <code>Config.PickupDistance</code> meters.</p>\n<p>For pole dancing, walk up to one of the configured pole positions (vanilla strip club poles are pre-set) and the ox_target interaction lets you pick a dance.</p>\n"
  },
  "suty-moneythrow/configuration": {
    "folderTitle": "Money Throwing",
    "pageTitle": "Configuration",
    "crumb": [
      "Money Throwing",
      "Configuration"
    ],
    "html": "<h1>Configuration</h1>\n<p><code>config.lua</code> at the resource root. Unencrypted and freely editable.</p>\n<h2>Globals</h2>\n<pre><code class=\"language-lua\">Config.Framework = &#39;qbx&#39;                -- &#39;qbx&#39; | &#39;qb&#39; | &#39;esx&#39;\nConfig.Command   = &#39;tirardinero&#39;        -- /tirardinero &lt;amount&gt;\n\nConfig.RestrictedGroup = false          -- false = anyone; or &#39;group.admin&#39; to gate the command\nConfig.CancelKey       = &#39;x&#39;            -- key to cancel mid-throw (refunds money)\n</code></pre>\n<h2>Money</h2>\n<pre><code class=\"language-lua\">Config.DefaultAmount = 500              -- default $ if no amount specified\nConfig.MaxAmount     = 5000             -- max $ per single throw\nConfig.BillThreshold = 1000             -- up to this: 1 bill per $1\nConfig.MaxBills      = 200              -- above threshold: cap visual bills at this\n</code></pre>\n<p>So a throw of $800 = 800 visual bills. A throw of $5000 = capped at 200 visual bills (still deducts the full $5000).</p>\n<h2>Timing</h2>\n<pre><code class=\"language-lua\">Config.ThrowDelay         = 200         -- ms between each bill thrown\nConfig.CleanupTime        = 120         -- seconds before bills despawn from ground\nConfig.MaxBillsOnGround   = 200         -- max bill props per client (oldest removed first)\nConfig.PickupDistance     = 2.5         -- ox_target distance\nConfig.DropExpireTime     = 300         -- seconds before uncollected money despawns\nConfig.PickupSettleTime   = 1500        -- ms wait after throw before bills can be picked up\n</code></pre>\n<h2>Physics</h2>\n<pre><code class=\"language-lua\">Config.ThrowForce   = { min = 2.0, max = 4.0 }   -- forward force\nConfig.ThrowUpForce = { min = 1.5, max = 3.5 }   -- upward force\nConfig.ThrowSpread  = 2.5                          -- random horizontal spread\n</code></pre>\n<h2>Animation + props</h2>\n<pre><code class=\"language-lua\">Config.MoneyProp = &#39;prop_anim_cash_note_b&#39;        -- single bill prop\nConfig.HandProp  = &#39;prop_anim_cash_pile_01&#39;       -- pile in hand during anim\n\nConfig.AnimDict = &#39;anim@mp_player_intupperraining_cash&#39;\nConfig.AnimName = &#39;idle_a&#39;\nConfig.AnimFlag = 49                              -- upper body + loop (walk while throwing)\n\nConfig.HandPropBone      = 60309                  -- left hand\nConfig.HandPropPlacement = {0.0, 0.0, 0.0, 180.0, 0.0, 70.0}\n\nConfig.FallbackAnimDict = &#39;anim@heists@ornate_bank@grab_cash_b&#39;\nConfig.FallbackAnimName = &#39;grab&#39;\n</code></pre>\n<h2>Particle effect</h2>\n<pre><code class=\"language-lua\">Config.PtfxAsset     = &#39;scr_xs_celebration&#39;\nConfig.PtfxName      = &#39;scr_xs_money_rain&#39;\nConfig.PtfxBone      = 60309\nConfig.PtfxPlacement = {0.0, 0.0, -0.09, -80.0, 0.0, 0.0, 1.0}\n</code></pre>\n<h2>Pole dances</h2>\n<pre><code class=\"language-lua\">Config.EnableCustomDances = true                  -- false = only vanilla GTA pole dances (1-3)\n\nConfig.Poles = {\n    [1] = {\n        position = vec3(112.58, -1287.04, 27.46),  -- box-zone center (pole location)\n        dances = {\n            { label = &#39;MagicCity 1&#39;, dict = &#39;pandora@magiccityv1.1&#39;, anim = &#39;pandora&#39;,\n              coords = vec4(112.56, -1287.06, 27.55, 194.37) },\n            -- ... 10 dances total\n        },\n    },\n    [2] = { ... },\n    [3] = { ... },\n}\n</code></pre>\n<p>Each pole entry has:</p>\n<ul>\n<li><code>position</code> — where ox_target detects &quot;I&#39;m at this pole&quot;</li>\n<li><code>dances</code> — list of custom dance emotes the player can pick</li>\n</ul>\n<p>Vanilla GTA pole dances (1-3) are always available on every pole regardless of this config.</p>\n<h2>Positioning custom dance coords</h2>\n<p>Use the in-game <code>/pole</code> command (admin-only). It captures your current ped position + heading so you can paste it into <code>Config.Poles[X].dances[Y].coords</code>.</p>\n"
  },
  "suty-moneythrow/commands": {
    "folderTitle": "Money Throwing",
    "pageTitle": "Commands",
    "crumb": [
      "Money Throwing",
      "Commands"
    ],
    "html": "<h1>Commands</h1>\n<h2>Player</h2>\n<table>\n<thead>\n<tr>\n<th>Command</th>\n<th>Description</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>/tirardinero [amount]</code></td>\n<td>Throw <code>amount</code> of cash. Without arg, uses <code>Config.DefaultAmount</code>. Must be ≤ <code>Config.MaxAmount</code>.</td>\n</tr>\n</tbody></table>\n<p>Press <strong>X</strong> (configurable via <code>Config.CancelKey</code>) during the throw to cancel and refund any unthrown bills.</p>\n<h2>Admin</h2>\n<table>\n<thead>\n<tr>\n<th>Command</th>\n<th>Description</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>/pole</code></td>\n<td>Print your current ped coords + heading to console — paste into <code>Config.Poles[X].dances[Y].coords</code> to position a custom dance</td>\n</tr>\n</tbody></table>\n<h2>ox_target interactions</h2>\n<table>\n<thead>\n<tr>\n<th>Target</th>\n<th>Action</th>\n<th>Result</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>Bill prop on the ground</td>\n<td>&quot;Recoger&quot;</td>\n<td>Picks up the bill, credits the player&#39;s cash account</td>\n</tr>\n<tr>\n<td>Pole position (box-zone)</td>\n<td>&quot;Bailar&quot;</td>\n<td>Opens dance menu — pick vanilla GTA (1-3) or custom MagicCity dances</td>\n</tr>\n</tbody></table>\n<h2>Bill lifecycle</h2>\n<ol>\n<li>Player runs <code>/tirardinero 500</code></li>\n<li>$500 is deducted from cash account</li>\n<li>500 bill props spawn (or <code>MaxBills</code> if higher than threshold)</li>\n<li>Bills fall with random physics + scatter</li>\n<li>After <code>PickupSettleTime</code> ms, bills become pickable via ox_target</li>\n<li>Any player can pick them up (no ownership)</li>\n<li>After <code>DropExpireTime</code> seconds, uncollected bills despawn</li>\n</ol>\n"
  },
  "suty-moneythrow/compatibility": {
    "folderTitle": "Money Throwing",
    "pageTitle": "Compatibility",
    "crumb": [
      "Money Throwing",
      "Compatibility"
    ],
    "html": "<h1>Compatibility</h1>\n<h2>Frameworks</h2>\n<table>\n<thead>\n<tr>\n<th>Framework</th>\n<th>Status</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>QBX Core (Qbox)</td>\n<td>✅ Native</td>\n</tr>\n<tr>\n<td>QB-Core</td>\n<td>✅ Native</td>\n</tr>\n<tr>\n<td>ESX (es_extended)</td>\n<td>✅ Native</td>\n</tr>\n</tbody></table>\n<p>The script reads/writes the <code>cash</code> (or framework-equivalent money) account.</p>\n<h2>Target system</h2>\n<p>Required: <strong>ox_target</strong> for bill pickup and pole interaction.</p>\n<h2>Optional integrations</h2>\n<table>\n<thead>\n<tr>\n<th>Resource</th>\n<th>Effect</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>MagicCity dance dicts (<code>pandora@magiccityv1.X</code>)</td>\n<td>Unlocks the 10 custom dances per pole. Without them, only GTA-native pole dances 1-3 work.</td>\n</tr>\n</tbody></table>\n<h2>Other suty resources</h2>\n<table>\n<thead>\n<tr>\n<th>Resource</th>\n<th>Interaction</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>All other suty-</td>\n<td>No conflicts</td>\n</tr>\n</tbody></table>\n<h2>Performance notes</h2>\n<ul>\n<li>Each thrown bill spawns a physics prop. <strong>Heavy throws (5000+ &quot;bills&quot; if you remove <code>MaxBills</code>) can hammer net sync</strong>.</li>\n<li><code>Config.MaxBills = 200</code> caps visual bills per throw to keep prop count sane.</li>\n<li><code>Config.MaxBillsOnGround = 200</code> is per-client — bills above this count get auto-despawned (oldest first).</li>\n<li>For a server with 64+ players, lowering <code>MaxBills</code> to 100 is reasonable.</li>\n</ul>\n<h2>Known incompatibilities</h2>\n<ul>\n<li>Some custom physics anti-cheats flag rapid <code>CreateObject</code> calls. Whitelist <code>suty-moneythrow</code> in your anti-cheat.</li>\n</ul>\n<p>Report compatibility issues on <a href=\"https://discord.gg/c2RFDBU9pc\">Discord</a>.</p>\n"
  },
  "suty-moneythrow/troubleshooting": {
    "folderTitle": "Money Throwing",
    "pageTitle": "Troubleshooting",
    "crumb": [
      "Money Throwing",
      "Troubleshooting"
    ],
    "html": "<h1>Troubleshooting</h1>\n<h2>&quot;No tienes suficiente dinero&quot;</h2>\n<p>Your <strong>cash account</strong> is below the throw amount. The script uses the framework&#39;s cash account, not bank. Top up cash or lower <code>Config.MaxAmount</code>.</p>\n<h2>Animation doesn&#39;t play</h2>\n<ul>\n<li><code>Config.AnimDict</code> failed to load — the script falls back to <code>FallbackAnimDict</code> automatically</li>\n<li>If both fail, check console for <code>RequestAnimDict</code> errors</li>\n</ul>\n<h2>Bills don&#39;t have physics / fall like dropped boxes</h2>\n<p><code>Config.ThrowForce</code> and <code>Config.ThrowUpForce</code> are too low or zero. Defaults should look &quot;make-it-rain&quot;-y. If you cranked them down, restore:</p>\n<pre><code class=\"language-lua\">Config.ThrowForce   = { min = 2.0, max = 4.0 }\nConfig.ThrowUpForce = { min = 1.5, max = 3.5 }\n</code></pre>\n<h2>Bills disappear too fast</h2>\n<p>Raise <code>Config.DropExpireTime</code> (default 300s) and <code>Config.CleanupTime</code> (default 120s). Don&#39;t push the per-client cap (<code>Config.MaxBillsOnGround</code>) too high — 200 bill props per client per session is the practical ceiling before performance suffers.</p>\n<h2>Custom pole dances do nothing</h2>\n<ul>\n<li><code>Config.EnableCustomDances = true</code></li>\n<li>The MagicCity dance dicts (<code>pandora@magiccityv1.1</code> through <code>.10</code>) must be streamed by some resource</li>\n<li>The pole position in <code>Config.Poles</code> must match the actual in-world pole</li>\n</ul>\n<p>Use <code>/pole</code> while standing/dancing at the position you want, then paste the printed coords.</p>\n<h2>Particle effect missing</h2>\n<p><code>Config.PtfxAsset</code> (<code>scr_xs_celebration</code>) is a vanilla asset and loads on demand. If the rain particles don&#39;t show, check the script&#39;s F8 log for <code>RequestNamedPtfxAsset</code> errors. Some custom particle replacements can break vanilla assets.</p>\n<h2>Pickup interaction doesn&#39;t appear</h2>\n<ul>\n<li><code>ox_target</code> must be started</li>\n<li><code>Config.PickupDistance = 2.5</code> — you must be within this distance of a bill prop</li>\n<li>Bills aren&#39;t pickable until <code>Config.PickupSettleTime</code> (default 1500ms) has passed after the throw</li>\n</ul>\n<h2>Still stuck?</h2>\n<p><a href=\"https://discord.gg/c2RFDBU9pc\">Discord</a> — include the failing command + amount + your <code>Config.Framework</code>.</p>\n"
  },
  "suty-queue/README": {
    "folderTitle": "Queue System",
    "pageTitle": "Overview",
    "crumb": [
      "Queue System",
      "Overview"
    ],
    "html": "<h1>🎟️ suty-queue</h1>\n<p>Production-grade FiveM connection queue. Discord-bot-driven priority tiers (Owners, Admins, VIPs, milestone-based player progression), silent-ban for cheaters who stay in queue forever without knowing it, reconnect grace, live Discord embed showing the full queue, and dynamic connection-delay tiers.</p>\n<h2>✨ Key Features</h2>\n<ul>\n<li><strong>Discord-role priority</strong> — fetch role membership via bot, weight queue position by role values</li>\n<li><strong>Stacked priority</strong> — multiple roles add up (e.g. VIP + Streamer + Booster)</li>\n<li><strong>Reconnect grace</strong> — disconnected players reconnect within window with bonus points (no re-queue)</li>\n<li><strong>Silent-ban / &quot;permanent queue&quot;</strong> — players with a flagged role NEVER leave the queue, even with empty slots. They see normal queue UI but never join.</li>\n<li><strong>Force-queue mode</strong> — all players go through queue regardless of server fullness (for welcome card branding)</li>\n<li><strong>Dynamic connection delay</strong> — <code>connectionDelay</code> scales by slot count (more slots free = faster cycle)</li>\n<li><strong>Live Discord embed</strong> — auto-updates a Discord channel embed with current queue state every 3s</li>\n<li><strong>Multi-language</strong> — Spanish + English locales</li>\n<li><strong>Whitelist gating</strong> — require Discord role to even enter the queue</li>\n</ul>\n<h2>📦 Requirements</h2>\n<ul>\n<li>Discord bot token + your Discord server (guild) ID</li>\n<li>Discord roles configured for each priority tier</li>\n<li>Any framework (queue runs server-wide, framework-agnostic for queue logic)</li>\n</ul>\n<h2>🌐 Compatibility</h2>\n<table>\n<thead>\n<tr>\n<th>Framework</th>\n<th>Status</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>Standalone</td>\n<td>✅ Queue is framework-agnostic</td>\n</tr>\n<tr>\n<td>QBX Core / QB-Core / ESX</td>\n<td>✅ Works alongside any</td>\n</tr>\n</tbody></table>\n<h2>🛍️ Get it</h2>\n<p><strong><a href=\"https://suty.dev\">Buy on suty.dev →</a></strong></p>\n<h2>📑 Next steps</h2>\n<ul>\n<li><a href=\"installation.md\">Installation</a></li>\n<li><a href=\"configuration.md\">Configuration</a></li>\n<li><a href=\"troubleshooting.md\">Troubleshooting</a></li>\n</ul>\n"
  },
  "suty-queue/installation": {
    "folderTitle": "Queue System",
    "pageTitle": "Installation",
    "crumb": [
      "Queue System",
      "Installation"
    ],
    "html": "<h1>Installation</h1>\n<h2>1. Drop the resource</h2>\n<p>Extract <code>suty-queue</code> into <code>resources/[suty]/suty-queue</code>.</p>\n<h2>2. Ensure FIRST in <code>server.cfg</code></h2>\n<p>The queue intercepts the connection handshake — it MUST boot before any framework or login systems.</p>\n<pre><code class=\"language-cfg\">ensure suty-queue\nensure qbx_core         # or qb-core / es_extended\n</code></pre>\n<h2>3. Set the slot count</h2>\n<p>In <code>server.cfg</code>:</p>\n<pre><code class=\"language-cfg\">sv_maxclients 64\n</code></pre>\n<p>The queue uses this to determine when the server is full.</p>\n<h2>4. Discord bot setup</h2>\n<p>You need a Discord application + bot token:</p>\n<ol>\n<li>Go to <a href=\"https://discord.com/developers/applications\">discord.com/developers/applications</a></li>\n<li>Create application → <strong>Bot</strong> → copy token</li>\n<li>Bot needs the <strong>Server Members</strong> privileged intent (toggle in Bot tab)</li>\n<li>Invite the bot to your Discord guild with <code>View Members</code> permission</li>\n<li>Right-click your guild → Copy Server ID (Developer Mode must be on)</li>\n</ol>\n<p>Then in <code>config.lua</code>:</p>\n<pre><code class=\"language-lua\">QueueConfig.DiscordBot = {\n    Active        = true,\n    RequireLink   = true,                   -- player must have Discord linked to FiveM\n    MustBeInServer = true,                  -- player must be IN your guild\n    UseWhitelist  = true,                   -- require WhitelistRole to enter queue\n    WhitelistRole = 1422487076003119114,    -- Discord role ID\n    Token         = &#39;YOUR_BOT_TOKEN&#39;,\n    ServerID      = &#39;YOUR_GUILD_ID&#39;,\n}\n</code></pre>\n<h2>5. Configure priority roles</h2>\n<pre><code class=\"language-lua\">QueueConfig.RoleHierarchy = {\n    {name = &#39;Owner&#39;,  discordRole = 1320233886214787072, value = 850000},\n    {name = &#39;Admins&#39;, discordRole = 1320234519873323082, value = 200000},\n    {name = &#39;VIP&#39;,    discordRole = 1320237013395443794, value = 150000},\n    -- ... add as many tiers as you need\n}\n</code></pre>\n<p>Higher <code>value</code> = better queue position. Role values stack if <code>CombinePoints = true</code>.</p>\n<h2>6. (Optional) Live Discord embed</h2>\n<pre><code class=\"language-lua\">QueueConfig.LiveEmbed = {\n    Enabled    = true,\n    WebhookURL = &#39;https://discord.com/api/webhooks/...&#39;,\n    UpdateInterval = 3,                     -- seconds (Discord rate-limits ~30/min)\n    ShowPlayerList = true,\n    MaxPlayersShown = 15,\n}\n</code></pre>\n<p>Posts a live-updating embed in a Discord channel showing who&#39;s in queue right now.</p>\n<h2>7. Verify</h2>\n<ul>\n<li>Fill the server past <code>sv_maxclients</code> (or set <code>ForceQueue.Enabled = true</code> to test with empty slots)</li>\n<li>New connections should see the welcome card with queue position</li>\n<li>Try with multiple roles to verify priority works</li>\n</ul>\n"
  },
  "suty-queue/configuration": {
    "folderTitle": "Queue System",
    "pageTitle": "Configuration",
    "crumb": [
      "Queue System",
      "Configuration"
    ],
    "html": "<h1>Configuration</h1>\n<p><code>config.lua</code> at the resource root. All settings use the new <code>QueueConfig.*</code> namespace — the bottom of the file maps to the legacy <code>Config.*</code> namespace for backwards compatibility (don&#39;t edit that section).</p>\n<h2>Locale</h2>\n<pre><code class=\"language-lua\">QueueConfig.Locale = &#39;es&#39;                   -- &#39;es&#39; (Spanish) | &#39;en&#39; (English)\n</code></pre>\n<p>Translations live in <code>locales/</code> files referenced by the locale helper.</p>\n<h2>Server behavior</h2>\n<pre><code class=\"language-lua\">QueueConfig.Server = {\n    StartupDelay     = 5,                   -- initial delay on server startup (seconds)\n    ConnectionSpeed  = 3,                   -- per-player connection speed\n    QueueSize        = 10,                  -- visible queue list size\n    RefreshRate      = 1000,                -- UI refresh interval (ms)\n    ShowInServerName = true,                -- show queue in server name\n    LogToConsole     = true,\n    DeveloperMode    = false,               -- debug spam\n    KeepAliveInterval = 10,                 -- keep-alive ping (seconds)\n    MaxWaitTime      = 2400,                -- 40 min before suggesting reconnect\n    AllowDuplicateDiscord = false,          -- testing only\n    PreQueueCheck    = 10,                  -- pre-queue check window (seconds)\n    ConnectionDelay  = 25,                  -- max delay between connections (when &lt;5 slots free)\n    MinQueueTime     = 10,                  -- minimum seconds in queue (for branding)\n\n    DynamicDelay     = true,                -- scale connection delay by slot count\n    DynamicDelayTiers = {\n        { slots = 20, delay = 3 },          -- 20+ slots free → 3s between connections\n        { slots = 10, delay = 8 },          -- 10-19 → 8s\n        { slots = 5,  delay = 15 },         -- 5-9 → 15s\n        -- &lt; 5 slots → ConnectionDelay (25s)\n    },\n\n    ChatEnabled      = true,                -- chat between players in queue\n    ChatMaxMessages  = 8,\n    ChatRateLimit    = 1,                   -- seconds between messages per player\n    ChatRefreshRate  = 5000,\n}\n</code></pre>\n<h2>Priority system</h2>\n<pre><code class=\"language-lua\">QueueConfig.Priority = {\n    CombinePoints = true,                   -- stack multiple roles together\n    ReconnectGrace = {\n        Enabled     = true,\n        BonusPoints = 40000,                -- higher than any role for instant reconnect\n        Duration    = 3000,                 -- grace duration (seconds) = 50 min\n    },\n    BaseLevel = {\n        Points = 10,                        -- base priority for non-role players\n        Name   = &quot;Player&quot;,\n    },\n\n    -- Silent ban — players with this role NEVER leave the queue\n    PermanentQueue = {\n        Enabled = true,\n        Roles   = {\n            1422487708571271178,             -- &quot;silent ban&quot; Discord role ID\n        },\n    },\n}\n</code></pre>\n<p><strong>Silent ban</strong> is the kill feature: tag a Discord role as &quot;permanent queue&quot; and assign suspected cheaters to it. They see a normal queue, position updates, never get angry-pings — but they NEVER enter the server. They eventually rage-quit thinking it&#39;s a slow server.</p>\n<h2>Discord bot</h2>\n<pre><code class=\"language-lua\">QueueConfig.DiscordBot = {\n    Active         = true,\n    RequireLink    = true,                  -- player must have Discord linked\n    MustBeInServer = true,                  -- player must be in your guild\n    UseWhitelist   = true,                  -- require WhitelistRole\n    WhitelistRole  = 1422487076003119114,\n    Token          = &#39;YOUR_BOT_TOKEN&#39;,\n    ServerID       = &#39;YOUR_GUILD_ID&#39;,\n    CacheUsers     = true,\n    CacheExpiry    = 120,                   -- seconds\n}\n</code></pre>\n<h2>Role hierarchy (priority tiers)</h2>\n<pre><code class=\"language-lua\">QueueConfig.RoleHierarchy = {\n    {name = &#39;Owner&#39;,     discordRole = 1320233886214787072, value = 850000},\n    {name = &#39;Admins&#39;,    discordRole = 1320234519873323082, value = 200000},\n    {name = &#39;Support&#39;,   discordRole = 1320235842580647967, value = 250000},\n    {name = &#39;VIP&#39;,       discordRole = 1320237013395443794, value = 150000},\n    {name = &#39;ByPass&#39;,    discordRole = 1435018023097602088, value = 100000},\n    {name = &#39;Streamer&#39;,  discordRole = 1320237171915096165, value = 65000},\n    -- Player progress milestones (5k miles, 10k, 20k, ...)\n    {name = &#39;5k Millas&#39;, discordRole = 1435018915427123270, value = 5000},\n    {name = &#39;10k Millas&#39;,discordRole = 1435018788830580816, value = 10000},\n    -- ...\n}\n</code></pre>\n<p>Higher value = better position. With <code>CombinePoints = true</code>, a VIP+Streamer+Booster player gets the SUM of all role values.</p>\n<h2>Welcome card / branding</h2>\n<pre><code class=\"language-lua\">QueueConfig.Interface = {\n    HeaderImage    = &#39;https://cdn.discordapp.com/.../Banner.gif&#39;,\n    WelcomeMessage = L(&#39;WelcomeMessage&#39;),\n    Elements = {\n        QueueList = {\n            show = true,\n            text = L(&#39;QueueList&#39;),\n            iconURL = &#39;https://cdn.n4.gg/queue.png&#39;,\n            style = &#39;destructive&#39;,\n        },\n        DiscordInvite = {\n            show = true,\n            text = &#39;Discord&#39;,\n            iconURL = &#39;https://cdn.n4.gg/discord.png&#39;,\n            link = &#39;https://discord.gg/yourinvite&#39;,\n            style = &#39;destructive&#39;,\n        },\n    },\n}\n</code></pre>\n<h2>Live embed</h2>\n<pre><code class=\"language-lua\">QueueConfig.LiveEmbed = {\n    Enabled         = true,\n    WebhookURL      = &#39;https://discord.com/api/webhooks/...&#39;,\n    UpdateInterval  = 3,                    -- min seconds between updates (Discord limit)\n    Color           = 0xFF0000,\n    Title           = L(&#39;LiveEmbedTitle&#39;),\n    FooterText      = &#39;Queue System&#39;,\n    ServerName      = &#39;HoodLife&#39;,\n    ShowPlayerList  = true,\n    MaxPlayersShown = 15,\n}\n</code></pre>\n<h2>Force queue</h2>\n<pre><code class=\"language-lua\">QueueConfig.ForceQueue = {\n    Enabled = false,                        -- true = ALL players go through queue even with empty slots\n}\n</code></pre>\n<p>Useful for showing the welcome card to every connection.</p>\n<h2>Test mode</h2>\n<pre><code class=\"language-lua\">QueueConfig.TestEnvironment = {\n    Activate       = false,                 -- DON&#39;T enable in prod\n    MaxTestPlayers = 1,                     -- forces queue at 1 player connected\n}\n</code></pre>\n"
  },
  "suty-queue/commands": {
    "folderTitle": "Queue System",
    "pageTitle": "Commands",
    "crumb": [
      "Queue System",
      "Commands"
    ],
    "html": "<h1>Commands</h1>\n<p>The queue is <strong>transparent to players</strong> — there are no chat commands. All interactions happen on the connection screen via the welcome card NUI.</p>\n<h2>Player UX</h2>\n<table>\n<thead>\n<tr>\n<th>Element</th>\n<th>Action</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>Welcome card</td>\n<td>Shows queue position, role/priority label, estimated wait</td>\n</tr>\n<tr>\n<td>Queue list button</td>\n<td>Opens a scrollable list of everyone currently in queue</td>\n</tr>\n<tr>\n<td>Discord button</td>\n<td>Opens your configured Discord invite link</td>\n</tr>\n<tr>\n<td>Chat panel (if <code>ChatEnabled = true</code>)</td>\n<td>Lets queued players chat while waiting</td>\n</tr>\n</tbody></table>\n<h2>Hidden mechanics</h2>\n<ul>\n<li><strong>Reconnect grace</strong> — if a player disconnects within <code>Priority.ReconnectGrace.Duration</code> seconds of being connected, their next connection attempt gets <code>BonusPoints</code> priority — they re-enter (almost) immediately. No action needed by the player.</li>\n<li><strong>Silent ban</strong> — players with any <code>Priority.PermanentQueue.Roles</code> Discord role go through normal queue UX but NEVER get connected. From their perspective, the server is &quot;very busy.&quot;</li>\n<li><strong>Force queue</strong> — when <code>ForceQueue.Enabled = true</code>, even on an empty server every connection still sees the welcome card for at least <code>Server.MinQueueTime</code> seconds.</li>\n</ul>\n<h2>Server logs</h2>\n<table>\n<thead>\n<tr>\n<th>Setting</th>\n<th>What it does</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>Server.LogToConsole = true</code></td>\n<td>Per-player connect/queue events in server console</td>\n</tr>\n<tr>\n<td><code>LiveEmbed.Enabled = true</code></td>\n<td>Live-updating Discord embed in your configured channel</td>\n</tr>\n</tbody></table>\n<h2>Programmatic</h2>\n<p>The queue doesn&#39;t expose <code>exports</code> because it operates at the deferrals layer before any framework loads.</p>\n"
  },
  "suty-queue/compatibility": {
    "folderTitle": "Queue System",
    "pageTitle": "Compatibility",
    "crumb": [
      "Queue System",
      "Compatibility"
    ],
    "html": "<h1>Compatibility</h1>\n<h2>Frameworks</h2>\n<table>\n<thead>\n<tr>\n<th>Framework</th>\n<th>Status</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>QBX Core (Qbox)</td>\n<td>✅</td>\n</tr>\n<tr>\n<td>QB-Core</td>\n<td>✅</td>\n</tr>\n<tr>\n<td>ESX (es_extended)</td>\n<td>✅</td>\n</tr>\n<tr>\n<td>Standalone / custom</td>\n<td>✅</td>\n</tr>\n</tbody></table>\n<p>The queue operates at the FiveM deferrals layer BEFORE any framework loads — it&#39;s framework-agnostic.</p>\n<h2>Discord bot</h2>\n<table>\n<thead>\n<tr>\n<th>Requirement</th>\n<th>Where</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>Bot token</td>\n<td><a href=\"https://discord.com/developers/applications\">discord.com/developers/applications</a> → Bot → Reset Token</td>\n</tr>\n<tr>\n<td>Bot in guild</td>\n<td>Invite link with <code>View Members</code> permission</td>\n</tr>\n<tr>\n<td>Server Members Intent</td>\n<td>Toggle in Bot tab → Privileged Gateway Intents</td>\n</tr>\n<tr>\n<td>Discord linked to FiveM</td>\n<td>Player runs FiveM with Discord open (CitizenFX picks up the Rich Presence ID)</td>\n</tr>\n</tbody></table>\n<h2>Other queue scripts</h2>\n<p><strong>Not compatible</strong> with any other queue script (connectqueue, etc.) — only one queue can intercept deferrals. Remove the other.</p>\n<h2>Other suty resources</h2>\n<table>\n<thead>\n<tr>\n<th>Resource</th>\n<th>Interaction</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>All other suty-</td>\n<td>No conflicts — queue is at the deferrals layer, fires before anything else</td>\n</tr>\n</tbody></table>\n<h2>Anti-cheats</h2>\n<p>Compatible with all major anti-cheats since the queue operates before player join. FiveGuard / Badger / Eulen-detect won&#39;t interact with queue logic.</p>\n<h2>Known incompatibilities</h2>\n<ul>\n<li>Other queue/whitelist scripts (only one can run)</li>\n<li>Custom deferral handlers that intercept connection at the same layer (rare)</li>\n</ul>\n<p>Report compatibility issues on <a href=\"https://discord.gg/c2RFDBU9pc\">Discord</a>.</p>\n"
  },
  "suty-queue/troubleshooting": {
    "folderTitle": "Queue System",
    "pageTitle": "Troubleshooting",
    "crumb": [
      "Queue System",
      "Troubleshooting"
    ],
    "html": "<h1>Troubleshooting</h1>\n<h2>Players see &quot;License not found&quot; or &quot;Discord not found&quot;</h2>\n<ul>\n<li><code>QueueConfig.DiscordBot.RequireLink = true</code> requires every player to have linked Discord on FiveM. Players with no Discord linked get rejected.</li>\n<li>For testing, set <code>RequireLink = false</code> to allow connections without Discord.</li>\n</ul>\n<h2>Players see &quot;Not whitelisted&quot; / &quot;Not in Discord&quot;</h2>\n<ul>\n<li>They&#39;re missing the <code>WhitelistRole</code> Discord role</li>\n<li>OR they&#39;re not in your Discord guild (<code>MustBeInServer = true</code>)</li>\n<li>Confirm by manually checking their Discord — do they have the role? Are they in the server?</li>\n<li>Verify the bot can READ members: bot has <code>View Members</code> permission AND <code>Server Members Intent</code> is enabled in the Developer Portal</li>\n</ul>\n<h2>Bot token invalid</h2>\n<p>Test by querying:</p>\n<pre><code>GET https://discord.com/api/v10/guilds/&lt;GUILD_ID&gt;/members/&lt;USER_ID&gt;\nAuthorization: Bot &lt;BOT_TOKEN&gt;\n</code></pre>\n<p>401 = bad token. 403 = bot lacks permissions. 404 = wrong guild or user.</p>\n<h2>Priority not stacking</h2>\n<p><code>QueueConfig.Priority.CombinePoints = true</code> — must be true for stacking. With <code>false</code>, only the highest single role&#39;s value counts.</p>\n<h2>Live embed isn&#39;t updating</h2>\n<ul>\n<li><code>QueueConfig.LiveEmbed.Enabled = true</code></li>\n<li><code>WebhookURL</code> is valid (paste in browser → should return JSON, not error)</li>\n<li><code>UpdateInterval &gt;= 3</code> — Discord rate-limits webhooks. Lower than 3 seconds will get rate-limited and silently drop updates</li>\n</ul>\n<h2>Reconnect grace doesn&#39;t work</h2>\n<ul>\n<li><code>Priority.ReconnectGrace.Enabled = true</code></li>\n<li><code>Duration</code> is in <strong>seconds</strong> — <code>3000</code> = 50 minutes (NOT milliseconds)</li>\n<li>The reconnecting player must use the SAME license — different account = no grace</li>\n</ul>\n<h2>&quot;Silent banned&quot; player still got in</h2>\n<ul>\n<li>Their Discord role must EXACTLY match an entry in <code>Priority.PermanentQueue.Roles</code></li>\n<li>The bot must be able to see the role assignment (members intent)</li>\n<li>Test by setting their role and checking <code>Server.LogToConsole = true</code> — you should see them queued but never connected</li>\n</ul>\n<h2>Server doesn&#39;t queue at all</h2>\n<ul>\n<li><code>suty-queue</code> must boot BEFORE the framework. Check <code>server.cfg</code> ensure order.</li>\n<li><code>sv_maxclients</code> must be set</li>\n<li>If <code>ForceQueue.Enabled = false</code> (default), queue only triggers when server is full</li>\n</ul>\n<h2>Dynamic delay seems wrong</h2>\n<p><code>DynamicDelayTiers</code> is sorted by <code>slots</code> descending (more slots = first match). Make sure your tier list is in the right order. The <code>delay</code> is in <strong>seconds</strong> between connection-cycle ticks.</p>\n<h2>Still stuck?</h2>\n<p><a href=\"https://discord.gg/c2RFDBU9pc\">Discord</a> — include <code>QueueConfig.DiscordBot.Active</code>, whether you&#39;re using whitelist, and a relevant server console snippet.</p>\n"
  },
  "suty-reportes/README": {
    "folderTitle": "Report System",
    "pageTitle": "Overview",
    "crumb": [
      "Report System",
      "Overview"
    ],
    "html": "<h1>📝 suty-reportes</h1>\n<p>A modern <code>/calladmin</code> system. Players file reports with optional screenshots and voice notes; admins manage a live queue with claim / teleport / revive actions. Persistent HUD badge shows pending count to admins only.</p>\n<h2>✨ Key Features</h2>\n<ul>\n<li><strong><code>/calladmin</code></strong> — player report dialog with subject + description + optional screenshot</li>\n<li><strong>Voice notes</strong> — hold-to-record audio replies (opus-encoded, ≤30s)</li>\n<li><strong>Screenshot attachments</strong> — auto-uploads via <code>screenshot-basic</code> to Discord webhook</li>\n<li><strong>Admin badge HUD</strong> — pulsing pending-reports counter (4 style variants)</li>\n<li><strong>Revive action</strong> — built-in integrations for qb-ambulancejob / qbx_medical / suty-ems / wasabi_ambulance, plus a custom export hook</li>\n<li><strong>Discord fallback</strong> — if no admins online, automatically pushes report to Discord with deep-link to player channel</li>\n<li><strong>Multi-language</strong> — Spanish and English locales</li>\n<li><strong>Multi-framework</strong> — QBX / QB-Core / ESX</li>\n</ul>\n<h2>📦 Requirements</h2>\n<ul>\n<li><a href=\"https://github.com/overextended/ox_lib\">ox_lib</a></li>\n<li><a href=\"https://github.com/overextended/oxmysql\">oxmysql</a></li>\n<li>QBX Core / QB-Core / ESX</li>\n<li><strong>Optional:</strong> <a href=\"https://github.com/citizenfx/screenshot-basic\">screenshot-basic</a> — for the screenshot attachment feature</li>\n<li><strong>Optional:</strong> any revive resource (qb-ambulancejob, qbx_medical, suty-ems, wasabi_ambulance)</li>\n</ul>\n<h2>🌐 Compatibility</h2>\n<table>\n<thead>\n<tr>\n<th>Framework</th>\n<th>Status</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>QBX Core (Qbox)</td>\n<td>✅</td>\n</tr>\n<tr>\n<td>QB-Core</td>\n<td>✅</td>\n</tr>\n<tr>\n<td>ESX (es_extended)</td>\n<td>✅</td>\n</tr>\n</tbody></table>\n<h2>🛍️ Get it</h2>\n<p><strong><a href=\"https://suty.dev\">Buy on suty.dev →</a></strong></p>\n<h2>📑 Next steps</h2>\n<ul>\n<li><a href=\"installation.md\">Installation</a></li>\n<li><a href=\"configuration.md\">Configuration</a></li>\n<li><a href=\"commands.md\">Commands</a></li>\n<li><a href=\"troubleshooting.md\">Troubleshooting</a></li>\n</ul>\n"
  },
  "suty-reportes/installation": {
    "folderTitle": "Report System",
    "pageTitle": "Installation",
    "crumb": [
      "Report System",
      "Installation"
    ],
    "html": "<h1>Installation</h1>\n<h2>1. Drop the resource</h2>\n<p>Extract <code>suty-reportes</code> into <code>resources/[suty]/suty-reportes</code>.</p>\n<h2>2. Ensure in <code>server.cfg</code></h2>\n<pre><code class=\"language-cfg\">ensure ox_lib\nensure oxmysql\nensure screenshot-basic    # optional, only if Config.Screenshot = true\nensure suty-reportes\n</code></pre>\n<h2>3. Database</h2>\n<p>Tables are created automatically on first start. Verify:</p>\n<pre><code class=\"language-sql\">SHOW TABLES LIKE &#39;suty_reports%&#39;;\n</code></pre>\n<h2>4. Set framework and locale</h2>\n<p><code>config.lua</code>:</p>\n<pre><code class=\"language-lua\">Config.Framework = &#39;qbx&#39;    -- &#39;qbx&#39; | &#39;qb&#39; | &#39;esx&#39;\nConfig.Locale    = &#39;es&#39;     -- &#39;es&#39; | &#39;en&#39;\n</code></pre>\n<h2>5. Grant admin permissions</h2>\n<p>Any group in <code>Config.AllowedGroups</code> can view + manage reports. Default:</p>\n<pre><code class=\"language-lua\">Config.AllowedGroups = { &#39;admin&#39;, &#39;god&#39;, &#39;mod&#39; }\n</code></pre>\n<p>Make sure your admins have one of these ACE groups.</p>\n<h2>6. Configure revive method</h2>\n<p><code>config.lua</code>:</p>\n<pre><code class=\"language-lua\">Config.ReviveMethod = &#39;suty-ems&#39;    -- &#39;qb-ambulancejob&#39; | &#39;qbx_medical&#39; | &#39;suty-ems&#39; | &#39;wasabi_ambulance&#39; | &#39;custom&#39;\n</code></pre>\n<p>For <code>custom</code>, fill out <code>Config.ReviveCustom</code> with your own export or event.</p>\n<h2>7. Discord webhook (optional but recommended)</h2>\n<pre><code class=\"language-lua\">Config.ScreenshotWebhook = &#39;https://discord.com/api/webhooks/...&#39;\nConfig.NoAdminDiscordUrl = &#39;https://discord.com/channels/SERVER/CHANNEL&#39;\n</code></pre>\n<p>The first uploads screenshot attachments. The second is shown to players when no admins are online (fallback to file a Discord ticket instead).</p>\n<h2>8. Verify</h2>\n<ul>\n<li>Player: <code>/calladmin</code> — opens the report dialog</li>\n<li>Admin: a pending-counter badge appears in the corner (style set by <code>Config.AdminBadge</code>); click or run the admin command to open the queue</li>\n</ul>\n"
  },
  "suty-reportes/configuration": {
    "folderTitle": "Report System",
    "pageTitle": "Configuration",
    "crumb": [
      "Report System",
      "Configuration"
    ],
    "html": "<h1>Configuration</h1>\n<p><code>config.lua</code> at the resource root. Unencrypted and freely editable.</p>\n<h2>Globals</h2>\n<pre><code class=\"language-lua\">Config.Framework = &#39;qbx&#39;                    -- &#39;qbx&#39; | &#39;qb&#39; | &#39;esx&#39;\nConfig.Locale    = &#39;es&#39;                     -- &#39;es&#39; | &#39;en&#39;\n\nConfig.Command       = &#39;calladmin&#39;          -- player + admin command name\nConfig.AllowedGroups = { &#39;admin&#39;, &#39;god&#39;, &#39;mod&#39; }    -- ACE groups for admin actions\n\nConfig.MaxSubjectLength      = 100\nConfig.MaxDescriptionLength  = 1000\nConfig.MaxMessageLength      = 500          -- per chat reply\n\nConfig.ReportCooldown   = 60                -- seconds between reports per player\nConfig.MaxHistoryResults = 50               -- pagination\n\nConfig.Logo = &#39;https://r2.fivemanage.com/.../hoodlife.png&#39;    -- shown in the admin queue UI\n</code></pre>\n<h2>Revive integration</h2>\n<pre><code class=\"language-lua\">Config.ReviveMethod = &#39;suty-ems&#39;            -- &#39;qb-ambulancejob&#39; | &#39;qbx_medical&#39; | &#39;suty-ems&#39; | &#39;wasabi_ambulance&#39; | &#39;custom&#39;\n</code></pre>\n<p>For <code>&#39;custom&#39;</code>:</p>\n<pre><code class=\"language-lua\">Config.ReviveCustom = {\n    type     = &#39;export&#39;,                    -- &#39;export&#39; or &#39;event&#39;\n    resource = &#39;my-resource&#39;,\n    name     = &#39;revivePlayer&#39;,              -- export function or event name\n}\n</code></pre>\n<h2>Admin badge HUD</h2>\n<p>Persistent counter showing pending reports — visible only to admins with notifications on.</p>\n<pre><code class=\"language-lua\">Config.AdminBadge = &#39;circle&#39;                -- &#39;minimal&#39; | &#39;icon&#39; | &#39;circle&#39; | &#39;topbar&#39; | false\n</code></pre>\n<table>\n<thead>\n<tr>\n<th>Style</th>\n<th>Look</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>&#39;minimal&#39;</code></td>\n<td>Pulsing red dot + number + &quot;reports&quot; label (top-right)</td>\n</tr>\n<tr>\n<td><code>&#39;icon&#39;</code></td>\n<td>Headset icon with red counter bubble (top-right)</td>\n</tr>\n<tr>\n<td><code>&#39;circle&#39;</code></td>\n<td>Floating red circle, just the number (top-right)</td>\n</tr>\n<tr>\n<td><code>&#39;topbar&#39;</code></td>\n<td>Centered pill at top: pulsing dot + &quot;pending reports&quot;</td>\n</tr>\n<tr>\n<td><code>false</code></td>\n<td>Disabled</td>\n</tr>\n</tbody></table>\n<h2>Screenshot attachments</h2>\n<pre><code class=\"language-lua\">Config.Screenshot         = true            -- show the screenshot button in the report dialog\nConfig.ScreenshotWebhook  = &#39;https://discord.com/api/webhooks/...&#39;\n</code></pre>\n<p>Requires <code>screenshot-basic</code> resource. The image is uploaded to your Discord webhook and the message link is attached to the report.</p>\n<h2>Admin-online gate</h2>\n<pre><code class=\"language-lua\">Config.RequireAdminOnline = true            -- if no admins online, refuse new reports + show Discord link\nConfig.NoAdminDiscordUrl  = &#39;https://discord.com/channels/SERVER/CHANNEL&#39;\n</code></pre>\n<p>When all admins are offline, the dialog shows the Discord URL instead of accepting the report. Helps avoid stale queues.</p>\n<h2>Voice notes</h2>\n<pre><code class=\"language-lua\">Config.VoiceMaxDurationMs = 30000           -- max ms per recording (30s default)\nConfig.VoiceMaxBytes      = 150 * 1024      -- hard cap (~120KB for 30s @ opus 24kbps)\nConfig.VoiceRateLimitSec  = 2               -- min seconds between voice notes from same sender\nConfig.VoiceCacheMaxBytes = 50 * 1024 * 1024  -- 50 MB total cache safety valve\n</code></pre>\n<p>Voice notes are opus-encoded base64 blobs stored in memory for the report&#39;s lifetime. Cleared on resolve / discard.</p>\n"
  },
  "suty-reportes/commands": {
    "folderTitle": "Report System",
    "pageTitle": "Commands",
    "crumb": [
      "Report System",
      "Commands"
    ],
    "html": "<h1>Commands</h1>\n<table>\n<thead>\n<tr>\n<th>Command</th>\n<th>Who</th>\n<th>Description</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>/calladmin</code></td>\n<td>Anyone</td>\n<td>If you&#39;re a player: opens the report dialog. If you&#39;re in <code>Config.AllowedGroups</code>: opens the admin queue UI.</td>\n</tr>\n</tbody></table>\n<p>The same command name does both — the script branches based on your ACE permissions.</p>\n<h2>Player report flow</h2>\n<ol>\n<li><code>/calladmin</code> → dialog opens with: subject (max 100 chars), description (max 1000), optional screenshot</li>\n<li>Submit → goes into the queue</li>\n<li>If no admins online + <code>RequireAdminOnline = true</code> → dialog refuses and shows the Discord URL instead</li>\n<li>Admin replies → notification on player&#39;s screen; can send back voice notes / text via the same UI</li>\n</ol>\n<h2>Admin queue actions</h2>\n<table>\n<thead>\n<tr>\n<th>Action</th>\n<th>Description</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><strong>Claim</strong></td>\n<td>Assign the report to yourself</td>\n</tr>\n<tr>\n<td><strong>Reply</strong></td>\n<td>Send a text message back to the reporter</td>\n</tr>\n<tr>\n<td><strong>Voice reply</strong></td>\n<td>Hold-to-record opus voice note (≤30s)</td>\n</tr>\n<tr>\n<td><strong>Teleport to reporter</strong></td>\n<td>TP to the player who filed the report</td>\n</tr>\n<tr>\n<td><strong>Teleport to subject</strong></td>\n<td>TP to the player being reported</td>\n</tr>\n<tr>\n<td><strong>Revive</strong></td>\n<td>Use the configured <code>ReviveMethod</code> on the player</td>\n</tr>\n<tr>\n<td><strong>Resolve</strong></td>\n<td>Close the report with resolution notes</td>\n</tr>\n<tr>\n<td><strong>Discard</strong></td>\n<td>Dismiss as invalid (logs the dismissal)</td>\n</tr>\n</tbody></table>\n<h2>Cooldown</h2>\n<p>Players can file a new report once every <code>Config.ReportCooldown</code> seconds (default 60). The cooldown isn&#39;t shown until they try a second submission.</p>\n"
  },
  "suty-reportes/compatibility": {
    "folderTitle": "Report System",
    "pageTitle": "Compatibility",
    "crumb": [
      "Report System",
      "Compatibility"
    ],
    "html": "<h1>Compatibility</h1>\n<h2>Frameworks</h2>\n<table>\n<thead>\n<tr>\n<th>Framework</th>\n<th>Status</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>QBX Core (Qbox)</td>\n<td>✅ Native</td>\n</tr>\n<tr>\n<td>QB-Core</td>\n<td>✅ Native</td>\n</tr>\n<tr>\n<td>ESX (es_extended)</td>\n<td>✅ Native</td>\n</tr>\n</tbody></table>\n<h2>Revive integrations (built-in)</h2>\n<table>\n<thead>\n<tr>\n<th>Resource</th>\n<th><code>Config.ReviveMethod</code> value</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>qb-ambulancejob</td>\n<td><code>&#39;qb-ambulancejob&#39;</code></td>\n</tr>\n<tr>\n<td>qbx_medical</td>\n<td><code>&#39;qbx_medical&#39;</code></td>\n</tr>\n<tr>\n<td>suty-ems</td>\n<td><code>&#39;suty-ems&#39;</code></td>\n</tr>\n<tr>\n<td>wasabi_ambulance</td>\n<td><code>&#39;wasabi_ambulance&#39;</code></td>\n</tr>\n<tr>\n<td>(Your own export/event)</td>\n<td><code>&#39;custom&#39;</code> + fill out <code>Config.ReviveCustom</code></td>\n</tr>\n</tbody></table>\n<h2>Screenshots</h2>\n<ul>\n<li><strong>Required when <code>Config.Screenshot = true</code>:</strong> <a href=\"https://github.com/citizenfx/screenshot-basic\">screenshot-basic</a></li>\n<li>The script uploads the captured PNG to your Discord webhook (<code>Config.ScreenshotWebhook</code>)</li>\n<li>You can keep the feature disabled — the rest of the script works fine without it</li>\n</ul>\n<h2>Voice notes</h2>\n<ul>\n<li>Uses the browser&#39;s native MediaRecorder API (Chrome embedded in FiveM) — no extra resource required</li>\n<li>Opus encoding @ 24kbps — ~3KB/sec → ~90KB for a 30s clip</li>\n<li>Stored in memory until the report is resolved/discarded</li>\n</ul>\n<h2>Other suty resources</h2>\n<table>\n<thead>\n<tr>\n<th>Resource</th>\n<th>Interaction</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>suty-ems</td>\n<td>Direct revive integration (<code>Config.ReviveMethod = &#39;suty-ems&#39;</code>)</td>\n</tr>\n<tr>\n<td>All other suty-</td>\n<td>No conflicts</td>\n</tr>\n</tbody></table>\n<h2>Known incompatibilities</h2>\n<ul>\n<li>Some custom notification scripts that hijack <code>ox_lib:notify</code> events can swallow report-status notifications. Disable other notify overrides if you&#39;re not seeing reply alerts.</li>\n</ul>\n<p>Report compatibility issues on <a href=\"https://discord.gg/c2RFDBU9pc\">Discord</a>.</p>\n"
  },
  "suty-reportes/troubleshooting": {
    "folderTitle": "Report System",
    "pageTitle": "Troubleshooting",
    "crumb": [
      "Report System",
      "Troubleshooting"
    ],
    "html": "<h1>Troubleshooting</h1>\n<h2>&quot;No admins online — file a Discord ticket instead&quot;</h2>\n<p>This is intentional behavior when <code>Config.RequireAdminOnline = true</code> AND no players with <code>Config.AllowedGroups</code> ACE are connected. Either:</p>\n<ul>\n<li>Wait for an admin to log in</li>\n<li>Use the Discord link shown in the dialog</li>\n<li>Disable the gate: <code>Config.RequireAdminOnline = false</code></li>\n</ul>\n<h2>Admin badge HUD doesn&#39;t appear</h2>\n<p>Three checks:</p>\n<ol>\n<li>You have one of the ACE groups in <code>Config.AllowedGroups</code></li>\n<li><code>Config.AdminBadge</code> is set to a style name (not <code>false</code>)</li>\n<li>You have at least one <strong>pending</strong> report (badge auto-hides at 0)</li>\n</ol>\n<h2>Screenshot button missing / does nothing</h2>\n<ul>\n<li><code>Config.Screenshot = true</code></li>\n<li><code>screenshot-basic</code> is started in <code>server.cfg</code></li>\n<li><code>Config.ScreenshotWebhook</code> is a valid Discord webhook URL</li>\n</ul>\n<p>If the button is there but uploads fail, paste the webhook URL in a browser. You should see a JSON response, not an error.</p>\n<h2>Revive button does nothing</h2>\n<p><code>Config.ReviveMethod</code> doesn&#39;t match an installed resource. Verify the resource is actually started:</p>\n<pre><code>restart suty-ems     # or qbx_medical, wasabi_ambulance, etc.\n</code></pre>\n<p>For custom systems, double-check <code>Config.ReviveCustom.resource</code> matches the actual resource name and <code>name</code> matches an export or event.</p>\n<h2>Voice notes don&#39;t record</h2>\n<ul>\n<li><code>screenshot-basic</code> is unrelated — voice uses the browser&#39;s MediaRecorder API directly</li>\n<li>The player must accept the microphone permission prompt the first time</li>\n<li>Check <code>Config.VoiceMaxDurationMs &gt; 0</code></li>\n</ul>\n<h2>&quot;Voice rate limit exceeded&quot;</h2>\n<p><code>Config.VoiceRateLimitSec</code> defaults to 2 seconds between notes per sender. Lower it if you want faster back-and-forth, or raise it to prevent voice-spam.</p>\n<h2>Collation / &quot;Illegal mix of collations&quot; error</h2>\n<p>The script&#39;s tables use <code>utf8mb4_general_ci</code>. If your DB defaults to <code>utf8mb4_uca1400_ai_ci</code> (MariaDB 10.10+), joins against <code>players</code> may fail. Convert the players table:</p>\n<pre><code class=\"language-sql\">ALTER TABLE players CONVERT TO CHARACTER SET utf8mb4 COLLATE utf8mb4_general_ci;\n</code></pre>\n<h2>Still stuck?</h2>\n<p><a href=\"https://discord.gg/c2RFDBU9pc\">Discord</a> — include your <code>Config.Framework</code>, <code>Config.ReviveMethod</code>, and the error from F8 / server console.</p>\n"
  },
  "suty-scalemenu/README": {
    "folderTitle": "Character Scale Menu",
    "pageTitle": "Overview",
    "crumb": [
      "Character Scale Menu",
      "Overview"
    ],
    "html": "<h1>📏 suty-scalemenu</h1>\n<p>Let players (and admins) customize their character&#39;s height with smooth per-frame scaling. Includes an NPC self-service location (configurable price), admin scale-bypass for special jobs, a slow auto-growth mechanic for bypassed players who pick the lowest height, and barber-chair reset zones.</p>\n<h2>✨ Key Features</h2>\n<ul>\n<li><strong>Per-player scale</strong> (0.80 – 1.10 default range)</li>\n<li><strong>NPC self-service</strong> — paid scale changes at configured peds (ox_target)</li>\n<li><strong>Bypass system</strong> — admins + specific jobs can go down to <code>BypassMinScale</code> (0.50 default)</li>\n<li><strong>Auto-growth</strong> — bypass players who hit StartHeight slowly grow back over weeks of online time</li>\n<li><strong>Barber-chair reset zones</strong> — auto-reset to default scale at fixed locations (e.g. for haircuts)</li>\n<li><strong>Hide-when conditions</strong> — scale auto-hides during swim / fall / ragdoll / climb / death</li>\n<li><strong>Sync distance</strong> — only renders scale for players within range (perf)</li>\n<li><strong>Discord webhook logging</strong></li>\n<li><strong>Multi-language</strong> — Spanish + English</li>\n</ul>\n<h2>📦 Requirements</h2>\n<ul>\n<li><a href=\"https://github.com/overextended/ox_lib\">ox_lib</a></li>\n<li><a href=\"https://github.com/overextended/ox_target\">ox_target</a></li>\n<li>QBX Core / QB-Core / ESX</li>\n<li><strong>Optional:</strong> <a href=\"#\">suty-ems</a> — for the death-state integration (<code>Config.UseSutyEMS = true</code>)</li>\n</ul>\n<h2>🌐 Compatibility</h2>\n<table>\n<thead>\n<tr>\n<th>Framework</th>\n<th>Status</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>QBX Core (Qbox)</td>\n<td>✅</td>\n</tr>\n<tr>\n<td>QB-Core</td>\n<td>✅</td>\n</tr>\n<tr>\n<td>ESX (es_extended)</td>\n<td>✅</td>\n</tr>\n</tbody></table>\n<h2>🛍️ Get it</h2>\n<p><strong><a href=\"https://suty.dev\">Buy on suty.dev →</a></strong></p>\n<h2>📑 Next steps</h2>\n<ul>\n<li><a href=\"installation.md\">Installation</a></li>\n<li><a href=\"usage.md\">Usage / Exports</a></li>\n<li><a href=\"configuration.md\">Configuration</a></li>\n<li><a href=\"commands.md\">Commands</a></li>\n<li><a href=\"troubleshooting.md\">Troubleshooting</a></li>\n</ul>\n"
  },
  "suty-scalemenu/installation": {
    "folderTitle": "Character Scale Menu",
    "pageTitle": "Installation",
    "crumb": [
      "Character Scale Menu",
      "Installation"
    ],
    "html": "<h1>Installation</h1>\n<h2>1. Drop the resource</h2>\n<p>Extract <code>suty-scalemenu</code> into <code>resources/[suty]/suty-scalemenu</code>.</p>\n<h2>2. Ensure in <code>server.cfg</code></h2>\n<pre><code class=\"language-cfg\">ensure ox_lib\nensure ox_target\nensure suty-scalemenu\n</code></pre>\n<h2>3. Set framework + language</h2>\n<p><code>config.lua</code>:</p>\n<pre><code class=\"language-lua\">Config.Framework = &#39;qbx&#39;    -- &#39;qbx&#39; | &#39;qb&#39; | &#39;esx&#39;\nConfig.Lang      = &#39;es&#39;     -- &#39;es&#39; | &#39;en&#39;\n</code></pre>\n<h2>4. (Optional) suty-ems death integration</h2>\n<pre><code class=\"language-lua\">Config.UseSutyEMS = true    -- false = use vanilla GTA death natives only\n</code></pre>\n<p>When <code>true</code>, the script uses suty-ems&#39; state-bag (<code>IsDeadOrDowned</code>) to hide the scale during executions / downed states — preventing visual glitches.</p>\n<h2>5. Configure self-service NPC</h2>\n<pre><code class=\"language-lua\">Config.Price = 20000        -- cost per scale change\n\nConfig.Locations = {\n    {\n        coords = vector4(1136.41, -1529.71, 34.37, 272.23),\n        ped    = &#39;s_m_m_doctor_01&#39;,\n        label  = &#39;Cambiar Altura&#39;,\n        icon   = &#39;fas fa-up-down&#39;,\n        job    = nil,\n    },\n}\n</code></pre>\n<p>Add more entries for multiple NPC locations.</p>\n<h2>6. (Optional) Discord webhook</h2>\n<pre><code class=\"language-lua\">Config.Webhook = {\n    Enabled = true,\n    URL     = &#39;https://discord.com/api/webhooks/...&#39;,\n}\n</code></pre>\n<h2>7. Verify</h2>\n<ul>\n<li>As a regular player, visit one of the NPCs → ox_target prompt should appear</li>\n<li>As an admin, run <code>/scalemenu</code> — opens the height picker for yourself</li>\n<li>As an admin, run <code>/scalemenu &lt;id&gt;</code> — opens the picker for another player</li>\n</ul>\n"
  },
  "suty-scalemenu/configuration": {
    "folderTitle": "Character Scale Menu",
    "pageTitle": "Configuration",
    "crumb": [
      "Character Scale Menu",
      "Configuration"
    ],
    "html": "<h1>Configuration</h1>\n<p><code>config.lua</code> at the resource root. Unencrypted and freely editable.</p>\n<h2>Globals</h2>\n<pre><code class=\"language-lua\">Config.Framework  = &#39;qbx&#39;           -- &#39;qbx&#39; | &#39;qb&#39; | &#39;esx&#39;\nConfig.Lang       = &#39;es&#39;            -- &#39;es&#39; | &#39;en&#39;\nConfig.UseSutyEMS = true            -- use suty-ems exports/state-bags for death state\n</code></pre>\n<h2>Scale range</h2>\n<pre><code class=\"language-lua\">Config.DefaultScale = 1.0\nConfig.MinScale     = 0.80          -- lowest scale for normal players\nConfig.MaxScale     = 1.10\n</code></pre>\n<h2>Bypass system</h2>\n<pre><code class=\"language-lua\">Config.BypassMinScale = 0.50        -- bypass players can go down to this\nConfig.BypassJobs = {\n    [&#39;ambulance&#39;] = true,           -- on-duty ambulance gets bypass\n}\n</code></pre>\n<p>Bypass is <strong>also</strong> automatically granted to admins (<code>group.admin</code> by default — see <code>BypassPermission</code> below). You can grant per-player bypass via <code>/scalebypass &lt;id&gt; grant</code>.</p>\n<h2>Auto-growth (for bypass players)</h2>\n<pre><code class=\"language-lua\">Config.Growth = {\n    Enabled         = true,\n    StartHeight     = 0.50,         -- only players AT this height grow\n    EndHeight       = 0.55,         -- they grow until reaching this\n    Step            = 0.01,         -- height added per interval\n    IntervalSeconds = 14 * 24 * 3600,   -- 2 weeks of online time per +0.01\n    TickSeconds     = 60 * 60,      -- check every 1 hour while online\n    NotifyOnGrow    = true,         -- notify the player on each growth step\n}\n</code></pre>\n<p>Designed so toddler/baby characters slowly grow back to &quot;shorter adult&quot; over weeks of real play. Catches up the moment they reconnect after being offline.</p>\n<h2>Commands</h2>\n<pre><code class=\"language-lua\">Config.AdminCommand     = &#39;scalemenu&#39;           -- /scalemenu [id]\nConfig.AdminGroups      = { &#39;admin&#39;, &#39;god&#39;, &#39;mod&#39; }\nConfig.BypassCommand    = &#39;scalebypass&#39;         -- /scalebypass &lt;id&gt; &lt;grant|revoke&gt;\nConfig.SetHeightCommand = &#39;scaleset&#39;            -- /scaleset &lt;id&gt; &lt;altura&gt;\nConfig.BypassPermission = &#39;group.admin&#39;         -- ACE for bypass-related commands\n</code></pre>\n<h2>NPC self-service</h2>\n<pre><code class=\"language-lua\">Config.Price = 20000\n\nConfig.Locations = {\n    {\n        coords = vector4(1136.41, -1529.71, 34.37, 272.23),\n        ped    = &#39;s_m_m_doctor_01&#39;,\n        label  = &#39;Cambiar Altura&#39;,\n        icon   = &#39;fas fa-up-down&#39;,\n        job    = nil,                           -- optional job lock\n    },\n}\n</code></pre>\n<h2>Discord role gate (optional)</h2>\n<pre><code class=\"language-lua\">Config.DiscordRoleRestriction = {\n    Enabled       = false,\n    BotToken      = &#39;&#39;,                         -- same token as suty-queue if you use it\n    GuildId       = &#39;&#39;,\n    RoleId        = &#39;&#39;,\n    DeniedMessage = &#39;No tienes el rol de Discord necesario para cambiar tu altura&#39;,\n}\n</code></pre>\n<p>When enabled, only players with the specified Discord role can use the NPC locations.</p>\n<h2>Reset zones (e.g. barber chairs)</h2>\n<pre><code class=\"language-lua\">Config.UseScaleResetZones = false\nConfig.ScaleResetZones = {\n    {\n        coords = {\n            vector3(-830.58, -1089.7, 10.74),\n            vector3(-832.4,  -1090.74, 10.74),\n            -- ...\n        },\n        radius = 1.5,\n    },\n}\n</code></pre>\n<p>When a player enters one of these radii, their scale is auto-reset to <code>DefaultScale</code>. Used for barber chairs or other locations where scale would break animations.</p>\n<h2>Hide-when conditions</h2>\n<pre><code class=\"language-lua\">Config.HideWhen = {\n    Swimming  = true,\n    Falling   = true,\n    Ragdoll   = true,\n    Climbing  = true,\n    Invisible = true,\n    Dead      = true,\n}\n</code></pre>\n<p>When any active condition matches, scale is temporarily reset to 1.0 (re-applied on exit) — prevents weird visuals during these states.</p>\n<h2>Sync distance</h2>\n<pre><code class=\"language-lua\">Config.SyncDistance = 25.0          -- only apply visual scale to players within this range\n</code></pre>\n<p>Performance optimization — players outside this range render at default scale.</p>\n<h2>Webhook logging</h2>\n<pre><code class=\"language-lua\">Config.Webhook = {\n    Enabled = true,\n    URL     = &#39;&#39;,                   -- paste webhook URL\n    Title   = &#39;Scale Menu Log&#39;,\n    Footer  = &#39;suty-scalemenu&#39;,\n    Color   = 0xdc2626,\n}\n</code></pre>\n<p>Logs scale changes, bypass grants/revokes, and admin-set heights.</p>\n"
  },
  "suty-scalemenu/commands": {
    "folderTitle": "Character Scale Menu",
    "pageTitle": "Commands",
    "crumb": [
      "Character Scale Menu",
      "Commands"
    ],
    "html": "<h1>Commands</h1>\n<h2>Admin</h2>\n<table>\n<thead>\n<tr>\n<th>Command</th>\n<th>Description</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>/scalemenu</code></td>\n<td>Opens the height picker for yourself</td>\n</tr>\n<tr>\n<td><code>/scalemenu &lt;id&gt;</code></td>\n<td>Opens the height picker for another player</td>\n</tr>\n<tr>\n<td><code>/scalebypass &lt;id&gt; &lt;grant|revoke&gt;</code></td>\n<td>Toggle bypass permission per player (lets them go below <code>MinScale</code>)</td>\n</tr>\n<tr>\n<td><code>/scaleset &lt;id&gt; &lt;altura&gt;</code></td>\n<td>Set a player&#39;s height directly (e.g. <code>/scaleset 5 0.85</code>)</td>\n</tr>\n</tbody></table>\n<p>All commands require an ACE group in <code>Config.AdminGroups</code> (default <code>admin</code>, <code>god</code>, <code>mod</code>). The bypass-related commands additionally require <code>Config.BypassPermission</code> (<code>group.admin</code>).</p>\n<h2>Player</h2>\n<p>There&#39;s no chat command for players. They interact via:</p>\n<ul>\n<li><strong>NPC self-service</strong> — visit any pedestrian in <code>Config.Locations</code>, use the ox_target option &quot;Cambiar Altura&quot;, pay <code>Config.Price</code>, pick height</li>\n<li><strong>Auto-growth</strong> (passive) — if you&#39;re a bypass player AT <code>StartHeight</code>, you slowly grow each <code>IntervalSeconds</code> of online time</li>\n<li><strong>Reset zones</strong> (passive) — entering a configured radius (barber chair, etc.) snaps you to <code>DefaultScale</code></li>\n</ul>\n<h2>Bypass behavior</h2>\n<p>Bypass is granted to:</p>\n<ol>\n<li>Players with ACE in <code>Config.AdminGroups</code></li>\n<li>On-duty members of any job in <code>Config.BypassJobs</code></li>\n<li>Players manually granted via <code>/scalebypass &lt;id&gt; grant</code></li>\n</ol>\n<p>Bypass players see additional lower options in the picker (down to <code>Config.BypassMinScale</code>).</p>\n"
  },
  "suty-scalemenu/compatibility": {
    "folderTitle": "Character Scale Menu",
    "pageTitle": "Compatibility",
    "crumb": [
      "Character Scale Menu",
      "Compatibility"
    ],
    "html": "<h1>Compatibility</h1>\n<h2>Frameworks</h2>\n<table>\n<thead>\n<tr>\n<th>Framework</th>\n<th>Status</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>QBX Core (Qbox)</td>\n<td>✅</td>\n</tr>\n<tr>\n<td>QB-Core</td>\n<td>✅</td>\n</tr>\n<tr>\n<td>ESX (es_extended)</td>\n<td>✅</td>\n</tr>\n</tbody></table>\n<h2>Optional integrations</h2>\n<table>\n<thead>\n<tr>\n<th>Resource</th>\n<th>Flag</th>\n<th>Effect</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><a href=\"#\">suty-ems</a></td>\n<td><code>Config.UseSutyEMS = true</code></td>\n<td>Uses suty-ems&#39; death/downed state-bag for the Dead hide-condition. Without it, falls back to vanilla GTA death natives.</td>\n</tr>\n<tr>\n<td><a href=\"#\">fadetrap-plus</a> (or any fixed-chair barber)</td>\n<td><code>Config.UseScaleResetZones = true</code></td>\n<td>Pre-configured for FadeTrap barber chair coords — extend <code>Config.ScaleResetZones</code> for other barbers.</td>\n</tr>\n</tbody></table>\n<h2>Anti-cheats</h2>\n<p>The scale is applied via <code>SetPedConfigFlag</code> / <code>SetPedScale</code> (or equivalent). Most anti-cheats accept this:</p>\n<ul>\n<li><strong>FiveGuard</strong> — whitelist <code>suty-scalemenu</code></li>\n<li><strong>Badger</strong> — usually no action</li>\n<li><strong>Eulen-detect</strong> — whitelist for ped-mod checks</li>\n</ul>\n<h2>Multi-character / persistence</h2>\n<p>Scale is keyed by citizenid (QBX/QB) or identifier (ESX). Per-character on multi-character servers. Persists across reconnects, restarts, and character switches.</p>\n<h2>Other suty resources</h2>\n<table>\n<thead>\n<tr>\n<th>Resource</th>\n<th>Interaction</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>suty-ems</td>\n<td>Direct death-state integration</td>\n</tr>\n<tr>\n<td>All other suty-</td>\n<td>No conflicts</td>\n</tr>\n</tbody></table>\n<h2>Known incompatibilities</h2>\n<ul>\n<li>Custom death scripts that don&#39;t expose a state-bag won&#39;t work with the Dead hide-condition. Set <code>Config.HideWhen.Dead = false</code> if you see scale glitching during your death/respawn flow.</li>\n<li>Two scale scripts running at once will fight each other — remove any other ped scale resource (e.g. <code>nation-pedscale</code>).</li>\n</ul>\n<p>Report compatibility issues on <a href=\"https://discord.gg/c2RFDBU9pc\">Discord</a>.</p>\n"
  },
  "suty-scalemenu/troubleshooting": {
    "folderTitle": "Character Scale Menu",
    "pageTitle": "Troubleshooting",
    "crumb": [
      "Character Scale Menu",
      "Troubleshooting"
    ],
    "html": "<h1>Troubleshooting</h1>\n<h2>Scale doesn&#39;t apply / player stays normal size</h2>\n<ul>\n<li><code>Config.SyncDistance</code> may be too low — players outside this range render at default. Default 25.0m.</li>\n<li><code>Config.HideWhen.*</code> might be triggering — if the player is in swim/fall/ragdoll/climbing state, scale is hidden by design.</li>\n<li><code>Config.UseSutyEMS = true</code> but suty-ems isn&#39;t installed — set <code>UseSutyEMS = false</code> to fall back to vanilla death natives.</li>\n</ul>\n<h2>Scale resets during execution / downed states</h2>\n<p>This is intentional — <code>Config.HideWhen.Dead = true</code> resets the scale to 1.0 during downed states to prevent broken animations. If you WANT scale during executions, set <code>Dead = false</code> — but expect visual glitches.</p>\n<h2>NPC self-service won&#39;t work</h2>\n<ul>\n<li><code>ox_target</code> must be started</li>\n<li>The <code>Locations[X].coords</code> ped model must be valid (e.g. <code>s_m_m_doctor_01</code>)</li>\n<li><code>Config.DiscordRoleRestriction.Enabled = true</code> requires the player to have the Discord role — disable for testing</li>\n</ul>\n<h2>&quot;No tienes el rol de Discord necesario&quot;</h2>\n<p><code>Config.DiscordRoleRestriction.Enabled = true</code> and the player lacks the role. Either:</p>\n<ul>\n<li>Give them the role</li>\n<li>Disable the check: <code>Config.DiscordRoleRestriction.Enabled = false</code></li>\n</ul>\n<h2>Bypass doesn&#39;t grant lower options</h2>\n<ul>\n<li>Admin ACE check: do you have <code>group.admin</code> (or one in <code>Config.AdminGroups</code>)?</li>\n<li>Job-based: are you actually on-duty for a job in <code>Config.BypassJobs</code>?</li>\n<li>Manual grant: did you run <code>/scalebypass &lt;id&gt; grant</code>? Check by running <code>/scalebypass &lt;id&gt;</code> (without grant/revoke) to see current state.</li>\n</ul>\n<h2>Auto-growth not progressing</h2>\n<ul>\n<li><code>Config.Growth.Enabled = true</code></li>\n<li>The player must be EXACTLY at <code>Config.Growth.StartHeight</code> (default 0.50) — slightly different heights don&#39;t trigger growth</li>\n<li>They must be ONLINE for <code>IntervalSeconds</code> (default 14 days)</li>\n<li>The <code>TickSeconds</code> (default 1 hour) is the check interval — growth doesn&#39;t apply instantly</li>\n</ul>\n<p>To force a growth step for testing: temporarily set <code>IntervalSeconds = 60</code> and wait one tick.</p>\n<h2>Barber chair resets aren&#39;t working</h2>\n<ul>\n<li><code>Config.UseScaleResetZones = true</code></li>\n<li>The player must be within <code>radius</code> of one of the listed coords</li>\n<li>Check that the coords match your actual barber chair positions</li>\n</ul>\n<h2>Webhook doesn&#39;t fire</h2>\n<ul>\n<li><code>Config.Webhook.Enabled = true</code></li>\n<li><code>URL</code> is valid (paste in browser → JSON response)</li>\n</ul>\n<h2>Still stuck?</h2>\n<p><a href=\"https://discord.gg/c2RFDBU9pc\">Discord</a> — include <code>Config.Framework</code>, <code>Config.UseSutyEMS</code>, and the issue category.</p>\n"
  },
  "suty-scalemenu/usage": {
    "folderTitle": "Character Scale Menu",
    "pageTitle": "Usage",
    "crumb": [
      "Character Scale Menu",
      "Usage"
    ],
    "html": "<h1>Usage / Exports</h1>\n<p><code>suty-scalemenu</code> exposes both <strong>scale-management exports</strong> for external scripts AND <strong>scale natives</strong> for advanced integration. Most users won&#39;t need to call these — the in-game NPC and admin commands cover the standard flows.</p>\n<h2>Common exports</h2>\n<pre><code class=\"language-lua\">-- Get the player&#39;s current scale\nlocal scale = exports[&#39;suty-scalemenu&#39;]:GetMyScale()\n\n-- Set your own scale (subject to permission checks)\nexports[&#39;suty-scalemenu&#39;]:SetMyScale(0.85)\n\n-- Server-side: set any player&#39;s scale\nexports[&#39;suty-scalemenu&#39;]:SetPlayerScale(targetSrc, 0.85)\n\n-- Check if a player has bypass\nlocal hasBypass = exports[&#39;suty-scalemenu&#39;]:HasBypass(targetSrc)\n\n-- Grant / revoke bypass per player\nexports[&#39;suty-scalemenu&#39;]:GrantBypass(targetSrc)\nexports[&#39;suty-scalemenu&#39;]:RevokeBypass(targetSrc)\n</code></pre>\n<h2>Integration hooks</h2>\n<p>If your custom script needs to <strong>temporarily reset</strong> a player&#39;s scale (e.g. a custom barber chair animation that needs default ped size):</p>\n<pre><code class=\"language-lua\">exports[&#39;suty-scalemenu&#39;]:ForceDefaultScale(targetSrc, true)\n-- ... do animation ...\nexports[&#39;suty-scalemenu&#39;]:ForceDefaultScale(targetSrc, false)\n</code></pre>\n<p>The script&#39;s own <code>ScaleResetZones</code> feature uses this internally for barber chairs.</p>\n<h2>State bags</h2>\n<p>The current effective scale is published via player state-bag:</p>\n<pre><code class=\"language-lua\">local scale = Player(serverId).state.scale\n</code></pre>\n<p>Read-only — to change it, use the exports above (the server-side write also handles persistence + sync).</p>\n<h2>When to use exports vs commands</h2>\n<table>\n<thead>\n<tr>\n<th>Use case</th>\n<th>Use</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>Player-driven UI in your own script</td>\n<td>Exports</td>\n</tr>\n<tr>\n<td>Admin action / debugging</td>\n<td><code>/scalemenu</code> <code>/scaleset</code></td>\n</tr>\n<tr>\n<td>Temporary disable for an anim</td>\n<td><code>ForceDefaultScale</code> export</td>\n</tr>\n<tr>\n<td>Reading current scale (read-only)</td>\n<td>State-bag</td>\n</tr>\n</tbody></table>\n"
  },
  "suty-sv_sync/README": {
    "folderTitle": "Community Service",
    "pageTitle": "Overview",
    "crumb": [
      "Community Service",
      "Overview"
    ],
    "html": "<h1>⛓️ suty-sv_sync — Servicio Comunitario</h1>\n<p>Send rule-breakers to community service. They&#39;re routing-bucketed (no friend-bailout), teleported to a work area, and must complete configurable tasks with ox_lib skill-checks before being released. On completion every weapon is stripped from their inventory. Other resources hook in via exports.</p>\n<h2>✨ Key Features</h2>\n<ul>\n<li><code>/comserv &lt;id&gt; &lt;count&gt; [reason]</code> — admin command</li>\n<li><strong>Routing bucket isolation</strong> — punished players are bucketed alone, can&#39;t be helped by friends</li>\n<li><strong>Skill-checks per task</strong> — ox_lib mini-game gate (configurable difficulty)</li>\n<li><strong>Max-distance enforcement</strong> — auto-teleport back if they wander</li>\n<li><strong>Discord-role permissions</strong> (recommended) or ACE-group permissions</li>\n<li><strong>Discord bot integration</strong> — checks role membership via Discord API</li>\n<li><strong>Auto-strip weapons on completion</strong> — keeps the punishment meaningful after release</li>\n<li><strong>Custom NUI panel</strong> — themed via CSS variables</li>\n<li><strong>Chat announcements</strong> (optional) — broadcast assignments/completions</li>\n<li><strong>Multi-framework</strong> — QBX / QB-Core / ESX</li>\n</ul>\n<h2>📦 Requirements</h2>\n<ul>\n<li><a href=\"https://github.com/overextended/ox_lib\">ox_lib</a></li>\n<li><a href=\"https://github.com/overextended/oxmysql\">oxmysql</a></li>\n<li><a href=\"https://github.com/overextended/ox_inventory\">ox_inventory</a> — for the weapon-strip on completion</li>\n<li>QBX Core / QB-Core / ESX</li>\n<li><strong>Optional:</strong> Discord bot token + guild ID (if using Discord-role permissions)</li>\n</ul>\n<h2>🌐 Compatibility</h2>\n<table>\n<thead>\n<tr>\n<th>Framework</th>\n<th>Status</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>QBX Core (Qbox)</td>\n<td>✅</td>\n</tr>\n<tr>\n<td>QB-Core</td>\n<td>✅</td>\n</tr>\n<tr>\n<td>ESX (es_extended)</td>\n<td>✅</td>\n</tr>\n</tbody></table>\n<h2>🛍️ Get it</h2>\n<p><strong><a href=\"https://suty.dev\">Buy on suty.dev →</a></strong></p>\n<h2>📑 Next steps</h2>\n<ul>\n<li><a href=\"installation.md\">Installation</a></li>\n<li><a href=\"configuration.md\">Configuration</a></li>\n<li><a href=\"commands.md\">Commands</a></li>\n<li><a href=\"troubleshooting.md\">Troubleshooting</a></li>\n</ul>\n"
  },
  "suty-sv_sync/installation": {
    "folderTitle": "Community Service",
    "pageTitle": "Installation",
    "crumb": [
      "Community Service",
      "Installation"
    ],
    "html": "<h1>Installation</h1>\n<h2>1. Drop the resource</h2>\n<p>Extract <code>suty-sv_sync</code> into <code>resources/[suty]/suty-sv_sync</code>.</p>\n<h2>2. Ensure in <code>server.cfg</code></h2>\n<pre><code class=\"language-cfg\">ensure ox_lib\nensure oxmysql\nensure ox_inventory\nensure suty-sv_sync\n</code></pre>\n<h2>3. Database</h2>\n<p><code>Config.Database.autoInstall = true</code> (default) creates the tables on first start. If you prefer manual SQL, set it to <code>false</code> and run the SQL in <code>[INSTALL SQL]/</code> against your DB.</p>\n<h2>4. Set framework + locations</h2>\n<p><code>config.lua</code>:</p>\n<pre><code class=\"language-lua\">Config.Framework = &#39;qbx&#39;    -- &#39;qbx&#39; | &#39;qb&#39; | &#39;esx&#39;\n\nConfig.ServiceLocation = vector3(3083.0, -4722.0, 15.26)    -- where players spawn on assignment\nConfig.ReturnLocation  = vector3(276.72, -346.45, 44.92)    -- where they spawn on release\n</code></pre>\n<p>The default coordinates put players at a remote desert area — change to match wherever you want service to happen.</p>\n<h2>5. Permissions</h2>\n<p>Choose <strong>Discord roles</strong> (recommended) or <strong>ACE groups</strong>:</p>\n<pre><code class=\"language-lua\">Config.Permissions = {\n    useDiscordWhitelist = true,         -- true = Discord, false = ACE\n    discordRoles = { &#39;1320234519873323082&#39; },   -- Discord role IDs\n    discordIDs   = { &#39;878077871443288086&#39; },    -- specific Discord user IDs\n    allowedGroups = { &#39;mod&#39;, &#39;admin&#39;, &#39;god&#39; },  -- used when useDiscordWhitelist = false\n}\n</code></pre>\n<p>If using Discord, also fill out:</p>\n<pre><code class=\"language-lua\">Config.Discord = {\n    botToken = &#39;YOUR_DISCORD_BOT_TOKEN&#39;,\n    guildId  = &#39;YOUR_DISCORD_SERVER_ID&#39;,\n}\n</code></pre>\n<p>The Discord bot needs <code>View Server Members</code> permission to look up roles.</p>\n<h2>6. Configure task locations</h2>\n<pre><code class=\"language-lua\">Config.TaskLocations = {\n    vector3(3084.5, -4725.5, 15.26),\n    vector3(3092.0, -4722.0, 15.26),\n    ...\n}\n</code></pre>\n<p>Add as many coordinates as you want — the script picks one randomly per task.</p>\n<h2>7. Verify</h2>\n<pre><code>/comserv &lt;serverid&gt; 10 Testing\n</code></pre>\n<p>The target player should teleport to the service location, get a routing bucket, and see the panel with 10 pending tasks.</p>\n"
  },
  "suty-sv_sync/configuration": {
    "folderTitle": "Community Service",
    "pageTitle": "Configuration",
    "crumb": [
      "Community Service",
      "Configuration"
    ],
    "html": "<h1>Configuration</h1>\n<p><code>config.lua</code> at the resource root. Unencrypted and freely editable.</p>\n<h2>Globals</h2>\n<pre><code class=\"language-lua\">Config.Framework = &#39;qbx&#39;                    -- &#39;qbx&#39; | &#39;qb&#39; | &#39;esx&#39;\nConfig.Debug     = false                    -- enable console debug\n\nConfig.Database = {\n    autoInstall = true,                     -- auto-create tables on resource start\n}\n\nConfig.Commands = {\n    assign      = &#39;comserv&#39;,                -- /comserv &lt;id&gt; &lt;count&gt; [reason]\n    end_service = &#39;endcoms&#39;,                -- /endcoms &lt;id&gt; — release early\n}\n\nConfig.Webhook = &#39;https://discord.com/api/webhooks/...&#39;    -- assign/release/complete logs\n</code></pre>\n<h2>Permissions</h2>\n<pre><code class=\"language-lua\">Config.Permissions = {\n    useDiscordWhitelist = false,            -- true = Discord roles, false = ACE groups\n\n    -- Option A — Discord role IDs\n    discordRoles = { &#39;1320234519873323082&#39; },\n\n    -- Option B — specific Discord user IDs\n    discordIDs = { &#39;878077871443288086&#39; },\n\n    -- Option C — ACE groups (used only if useDiscordWhitelist = false)\n    allowedGroups = { &#39;mod&#39;, &#39;admin&#39;, &#39;god&#39; },\n}\n\nConfig.Discord = {\n    botToken = &#39;YOUR_BOT_TOKEN&#39;,\n    guildId  = &#39;YOUR_GUILD_ID&#39;,\n}\n</code></pre>\n<h2>Notification system</h2>\n<pre><code class=\"language-lua\">Config.NotifySystem = &#39;ox_lib&#39;              -- &#39;qbcore&#39; | &#39;esx&#39; | &#39;ox_lib&#39; | &#39;mythic&#39; | &#39;okokNotify&#39; | &#39;custom&#39;\n</code></pre>\n<p>For <code>&#39;custom&#39;</code>, edit the <code>Notify</code> function in <code>client.lua</code>.</p>\n<h2>Locations</h2>\n<pre><code class=\"language-lua\">Config.ServiceLocation = vector3(3083.0, -4722.0, 15.26)\nConfig.ReturnLocation  = vector3(276.72, -346.45, 44.92)\nConfig.MaxDistance     = 60.0                -- max meters from service location\n\nConfig.TaskLocations = {\n    vector3(3084.5, -4725.5, 15.26),\n    vector3(3092.0, -4722.0, 15.26),\n    -- add more...\n}\n</code></pre>\n<h2>Penalty + routing</h2>\n<pre><code class=\"language-lua\">Config.EscapePenalty    = 5                 -- extra tasks added if player tries to leave the area\nConfig.PunishmentBucket = 999               -- routing bucket ID — isolates punished players\n</code></pre>\n<p>The routing bucket prevents friends from session-hopping in to help the punished player.</p>\n<h2>Auto-strip weapons on completion</h2>\n<pre><code class=\"language-lua\">Config.RemoveWeaponsOnCompletion = true     -- strip every ox_inventory weapon item on release\n</code></pre>\n<p>Mirrors qbx_adminmenu&#39;s <code>/removeweapons</code> logic. Keeps the punishment meaningful after release.</p>\n<h2>Skill-checks</h2>\n<pre><code class=\"language-lua\">Config.SkillCheck = {\n    enabled    = true,\n    difficulty = { &#39;medium&#39;, &#39;medium&#39; },    -- sequence: &#39;easy&#39; | &#39;medium&#39; | &#39;hard&#39;\n    inputs     = { &#39;w&#39;, &#39;a&#39;, &#39;s&#39;, &#39;d&#39; },\n    failMessage = &#39;Fallaste! Intenta de nuevo.&#39;,\n}\n</code></pre>\n<p>Each task is gated by ox_lib skill-check. Multiple difficulty values mean multiple consecutive checks per task.</p>\n<h2>Chat announcements</h2>\n<pre><code class=\"language-lua\">Config.ChatAnnouncement = {\n    enabled       = false,                  -- default false to avoid admin-action spam\n\n    assignmentMessage = &#39;^3[COMMUNITY SERVICE]^7 %s has been sentenced to ^1%d^7 community service tasks. ^3Reason:^7 %s&#39;,\n    completionMessage = &#39;^3[COMMUNITY SERVICE]^7 %s has completed their community service.&#39;,\n    releaseMessage    = &#39;^3[COMMUNITY SERVICE]^7 %s has been released from community service by an admin.&#39;,\n}\n</code></pre>\n<h2>UI theming</h2>\n<pre><code class=\"language-lua\">Config.UIColors = {\n    primary       = &#39;#dc2626&#39;,              -- titles, buttons, gradients\n    secondary     = &#39;#991b1b&#39;,              -- hover/gradient end\n    success       = &#39;#10b981&#39;,\n    warning       = &#39;#f59e0b&#39;,\n    danger        = &#39;#ef4444&#39;,\n    textPrimary   = &#39;#ffffff&#39;,\n    textSecondary = &#39;#fca5a5&#39;,\n    textMuted     = &#39;#f87171&#39;,\n    cardBorder    = &#39;rgba(220, 38, 38, 0.2)&#39;,\n}\n\nConfig.TaskMarkerColor = {\n    r = 255, g = 0, b = 0, a = 200,        -- 0-255 per channel\n}\n</code></pre>\n"
  },
  "suty-sv_sync/commands": {
    "folderTitle": "Community Service",
    "pageTitle": "Commands",
    "crumb": [
      "Community Service",
      "Commands"
    ],
    "html": "<h1>Commands</h1>\n<h2>Admin</h2>\n<table>\n<thead>\n<tr>\n<th>Command</th>\n<th>Description</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>/comserv &lt;id&gt; &lt;count&gt; [reason]</code></td>\n<td>Sentence a player to community service. <code>count</code> = number of tasks. <code>reason</code> = optional free-text logged + announced.</td>\n</tr>\n<tr>\n<td><code>/endcoms &lt;id&gt;</code></td>\n<td>Release a player from community service immediately. Stripping weapons (if enabled) still happens.</td>\n</tr>\n</tbody></table>\n<p>Permissions depend on <code>Config.Permissions</code> — Discord roles, Discord IDs, or ACE groups.</p>\n<h2>Programmatic API (other resources)</h2>\n<p>Other resources can sentence a player via export:</p>\n<pre><code class=\"language-lua\">exports[&#39;suty-sv_sync&#39;]:SendToCommunityService(playerSrc, taskCount, reason)\n</code></pre>\n<p>Used by:</p>\n<ul>\n<li><code>hl-perfmon</code> — to punish caught vehicle spawners</li>\n<li><code>suty-chainsnatch</code> — to punish detected exploiters</li>\n<li>Your own scripts when you need a non-chat punishment hook</li>\n</ul>\n<pre><code class=\"language-lua\">exports[&#39;suty-sv_sync&#39;]:ReleaseFromCommunityService(playerSrc)\nexports[&#39;suty-sv_sync&#39;]:IsInCommunityService(playerSrc)    -- returns bool, remainingTasks\n</code></pre>\n<h2>Player flow (involuntary)</h2>\n<ol>\n<li>Player gets <code>/comserv</code>&#39;d → teleported to <code>Config.ServiceLocation</code></li>\n<li>Routing-bucketed (alone — friends can&#39;t join)</li>\n<li>NUI panel shows total task count</li>\n<li>Walk to a task marker → ox_lib skill-check sequence → task completes</li>\n<li>Repeat until count reaches 0</li>\n<li>Released to <code>Config.ReturnLocation</code>, weapons stripped (if enabled)</li>\n</ol>\n<h2>Escape attempts</h2>\n<p>If the player walks more than <code>Config.MaxDistance</code> meters from <code>Config.ServiceLocation</code>, they get auto-teleported back AND <code>Config.EscapePenalty</code> tasks are added to their sentence.</p>\n"
  },
  "suty-sv_sync/compatibility": {
    "folderTitle": "Community Service",
    "pageTitle": "Compatibility",
    "crumb": [
      "Community Service",
      "Compatibility"
    ],
    "html": "<h1>Compatibility</h1>\n<h2>Frameworks</h2>\n<table>\n<thead>\n<tr>\n<th>Framework</th>\n<th>Status</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>QBX Core (Qbox)</td>\n<td>✅ Native</td>\n</tr>\n<tr>\n<td>QB-Core</td>\n<td>✅ Native</td>\n</tr>\n<tr>\n<td>ESX (es_extended)</td>\n<td>✅ Native</td>\n</tr>\n</tbody></table>\n<h2>Inventory</h2>\n<p>Required: <strong>ox_inventory</strong> for the auto-strip-weapons feature. Without it, set <code>Config.RemoveWeaponsOnCompletion = false</code> and the rest of the script works normally.</p>\n<h2>Discord bot</h2>\n<p>Optional but recommended. If you use <code>Config.Permissions.useDiscordWhitelist = true</code>:</p>\n<ul>\n<li>Need a bot token (Bot must be in your Discord server)</li>\n<li>Bot needs <code>View Server Members</code> privileged intent enabled in the Discord Developer Portal</li>\n<li><code>Config.Discord.guildId</code> is your server&#39;s snowflake ID (right-click server icon → Copy Server ID, with Developer Mode on)</li>\n</ul>\n<h2>Notification systems</h2>\n<p><code>Config.NotifySystem</code> accepts:</p>\n<table>\n<thead>\n<tr>\n<th>Value</th>\n<th>Notes</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>&#39;qbcore&#39;</code></td>\n<td>QB / QBX <code>Notify</code></td>\n</tr>\n<tr>\n<td><code>&#39;esx&#39;</code></td>\n<td>ESX <code>ShowNotification</code></td>\n</tr>\n<tr>\n<td><code>&#39;ox_lib&#39;</code></td>\n<td>ox_lib <code>lib.notify</code> (recommended)</td>\n</tr>\n<tr>\n<td><code>&#39;mythic&#39;</code></td>\n<td>Mythic Notify</td>\n</tr>\n<tr>\n<td><code>&#39;okokNotify&#39;</code></td>\n<td>OKOK Notify</td>\n</tr>\n<tr>\n<td><code>&#39;custom&#39;</code></td>\n<td>Edit <code>Notify</code> function in <code>client.lua</code></td>\n</tr>\n</tbody></table>\n<h2>Routing bucket awareness</h2>\n<p>If another resource manages routing buckets (e.g. custom shell systems, instance scripts), make sure <code>Config.PunishmentBucket = 999</code> doesn&#39;t collide. Pick any unique integer.</p>\n<h2>Caller integrations</h2>\n<table>\n<thead>\n<tr>\n<th>Resource</th>\n<th>How it uses suty-sv_sync</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><a href=\"#\">hl-perfmon</a> (internal)</td>\n<td>Calls <code>SendToCommunityService</code> when catching illegal vehicle spawns</td>\n</tr>\n<tr>\n<td><a href=\"#\">suty-chainsnatch</a></td>\n<td>Calls <code>SendToCommunityService</code> on detected exploits</td>\n</tr>\n</tbody></table>\n<p>Any external script can hook in via the exported <code>SendToCommunityService(playerSrc, taskCount, reason)</code> function.</p>\n<h2>Other suty resources</h2>\n<table>\n<thead>\n<tr>\n<th>Resource</th>\n<th>Interaction</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>All other suty-</td>\n<td>No conflicts</td>\n</tr>\n</tbody></table>\n<h2>Known incompatibilities</h2>\n<ul>\n<li>Routing bucket conflicts with other punishment / instance scripts using the same bucket ID — pick a unique <code>Config.PunishmentBucket</code>.</li>\n</ul>\n<p>Open a ticket on <a href=\"https://discord.gg/c2RFDBU9pc\">Discord</a> if you find one.</p>\n"
  },
  "suty-sv_sync/troubleshooting": {
    "folderTitle": "Community Service",
    "pageTitle": "Troubleshooting",
    "crumb": [
      "Community Service",
      "Troubleshooting"
    ],
    "html": "<h1>Troubleshooting</h1>\n<h2>&quot;No tienes permisos&quot; — admins can&#39;t run <code>/comserv</code></h2>\n<p>If <code>useDiscordWhitelist = true</code>:</p>\n<ol>\n<li>The admin&#39;s Discord ID must be linked to their FiveM account (license)</li>\n<li>They must be IN the guild specified by <code>Config.Discord.guildId</code></li>\n<li>Their Discord role ID must appear in <code>Config.Permissions.discordRoles</code></li>\n<li>The bot token must have <code>View Members</code> permission on the guild</li>\n</ol>\n<p>If <code>useDiscordWhitelist = false</code>, they need one of <code>Config.Permissions.allowedGroups</code> ACE groups.</p>\n<h2>Discord role check always fails</h2>\n<p>The bot token is invalid or the guild ID is wrong. Test by querying:</p>\n<pre><code>GET https://discord.com/api/v10/guilds/&lt;GUILD_ID&gt;/members/&lt;USER_ID&gt;\nAuthorization: Bot &lt;BOT_TOKEN&gt;\n</code></pre>\n<p>If you get a 401/403, the token or permissions are wrong.</p>\n<h2>Player escapes — auto-teleport doesn&#39;t fire</h2>\n<ul>\n<li><code>Config.MaxDistance</code> is too high — drop it (default 60.0 meters)</li>\n<li>The service area is overlapping a no-collision zone — pick a different <code>Config.ServiceLocation</code></li>\n</ul>\n<h2>Routing bucket isn&#39;t isolating</h2>\n<p><code>Config.PunishmentBucket = 999</code> — make sure no other resource is using bucket 999. If you have a custom routing system, pick a unique bucket ID.</p>\n<h2>Tasks never complete / skill-check broken</h2>\n<ul>\n<li><code>Config.SkillCheck.enabled = true</code></li>\n<li><code>Config.SkillCheck.difficulty</code> must contain at least one valid difficulty (<code>&#39;easy&#39;</code>, <code>&#39;medium&#39;</code>, <code>&#39;hard&#39;</code>)</li>\n<li><code>Config.SkillCheck.inputs</code> must be valid key letters</li>\n</ul>\n<p>Disable temporarily to confirm: <code>Config.SkillCheck.enabled = false</code> — tasks then complete on touch.</p>\n<h2>Weapons aren&#39;t stripped on release</h2>\n<ul>\n<li><code>Config.RemoveWeaponsOnCompletion = true</code></li>\n<li><code>ox_inventory</code> is started (the strip uses ox_inventory exports)</li>\n<li>The release happened via task completion OR <code>/endcoms</code> (manual escape via routing bucket bypass won&#39;t trigger it)</li>\n</ul>\n<h2>Chat announcements don&#39;t show</h2>\n<ul>\n<li><code>Config.ChatAnnouncement.enabled = true</code></li>\n<li>The specific event toggle (<code>onAssignment</code>, <code>onCompletion</code>, <code>onRelease</code>) is also <code>true</code></li>\n</ul>\n<h2>Still stuck?</h2>\n<p><a href=\"https://discord.gg/c2RFDBU9pc\">Discord</a> — include your <code>Config.Framework</code>, whether Discord or ACE permissions are in use, and which step in the flow is failing.</p>\n"
  },
  "suty-zonasegura/README": {
    "folderTitle": "Safe Zone Creator",
    "pageTitle": "Overview",
    "crumb": [
      "Safe Zone Creator",
      "Overview"
    ],
    "html": "<h1>🛡️ suty-zonasegura</h1>\n<div class=\"docs-embed\"><iframe src=\"https://www.youtube.com/embed/sCHengqqiIo\" allow=\"accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture\" allowfullscreen loading=\"lazy\"></iframe></div><p>Create safezones with <strong>any polygon shape</strong> where players cannot draw weapons or take damage. Admins draw zones on an interactive 2D Leaflet map of GTA V or directly in the world with a freecam.</p>\n<h2>✨ Key Features</h2>\n<ul>\n<li><strong>Two creators included</strong> — interactive 2D Leaflet map or 3D in-world freecam</li>\n<li><strong>Draggable vertices</strong> — adjust zone shape after drawing</li>\n<li><strong>Weapon lockout + invincibility</strong> — fully configurable per server</li>\n<li><strong>Anti-cheat compatible</strong> — <code>inSafeZone</code> statebag prevents godmode false positives</li>\n<li><strong>HUD badge</strong> — &quot;You are in a safezone&quot; with shield icon</li>\n<li><strong>Optimized for hundreds of zones</strong> — AABB + centroid + point-in-polygon two-tier check</li>\n<li><strong>Mid-session sync</strong> — new players load zones on join</li>\n<li><strong>Discord webhook</strong> (optional) — logs zone create/delete</li>\n<li><strong>Multi-language</strong> — Spanish and English locales included</li>\n<li><strong>Multi-framework</strong> — auto-detect QBX Core / QB-Core / ESX</li>\n</ul>\n<h2>📦 Requirements</h2>\n<ul>\n<li><a href=\"https://github.com/overextended/ox_lib\">ox_lib</a></li>\n<li><a href=\"https://github.com/overextended/oxmysql\">oxmysql</a></li>\n<li><a href=\"https://github.com/overextended/ox_target\">ox_target</a></li>\n<li>MariaDB / MySQL</li>\n</ul>\n<h2>🌐 Compatibility</h2>\n<table>\n<thead>\n<tr>\n<th>Framework</th>\n<th>Status</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>QBX Core (Qbox)</td>\n<td>✅</td>\n</tr>\n<tr>\n<td>QB-Core</td>\n<td>✅</td>\n</tr>\n<tr>\n<td>ESX (es_extended)</td>\n<td>✅</td>\n</tr>\n</tbody></table>\n<h2>🛍️ Get it</h2>\n<p><strong><a href=\"https://suty.dev\">Buy on suty.dev →</a></strong></p>\n<h2>📑 Next steps</h2>\n<ul>\n<li><a href=\"installation.md\">Installation</a></li>\n<li><a href=\"configuration.md\">Configuration</a></li>\n<li><a href=\"commands.md\">Commands</a></li>\n<li><a href=\"compatibility.md\">Compatibility details</a></li>\n<li><a href=\"troubleshooting.md\">Troubleshooting</a></li>\n</ul>\n"
  },
  "suty-zonasegura/installation": {
    "folderTitle": "Safe Zone Creator",
    "pageTitle": "Installation",
    "crumb": [
      "Safe Zone Creator",
      "Installation"
    ],
    "html": "<h1>Installation</h1>\n<h2>1. Drop the resource</h2>\n<p>Extract the <code>suty-zonasegura</code> folder into your server&#39;s <code>resources</code> directory (typically <code>resources/[suty]/suty-zonasegura</code>).</p>\n<div class=\"docs-hint docs-hint-warning\"><p>The folder <strong>must</strong> be named exactly <code>suty-zonasegura</code>. The script has an anti-rename guard that will refuse to start otherwise.</p>\n</div><h2>2. Ensure it in <code>server.cfg</code></h2>\n<pre><code class=\"language-cfg\">ensure ox_lib\nensure oxmysql\nensure ox_target\nensure suty-zonasegura\n</code></pre>\n<p>Start order matters — <code>ox_lib</code>, <code>oxmysql</code>, and <code>ox_target</code> must boot <strong>before</strong> <code>suty-zonasegura</code>.</p>\n<h2>3. Database</h2>\n<p>The resource creates its own table automatically on first start. No manual SQL required. You can verify the table exists with:</p>\n<pre><code class=\"language-sql\">SHOW CREATE TABLE suty_safezones;\n</code></pre>\n<h2>4. Grant admin permission</h2>\n<p>Only ACE-allowed admins can open the creator. Add this to your <code>server.cfg</code> if you haven&#39;t already:</p>\n<pre><code class=\"language-cfg\">add_principal identifier.license:YOUR_LICENSE_HERE group.admin\n</code></pre>\n<p>The script accepts both <code>admin</code> and <code>group.admin</code> ACE forms, so any standard QBX/QB/ESX admin setup works.</p>\n<h2>5. Verify</h2>\n<p>Restart the server, join in-game, and run <code>/zonasegura</code> as an admin. The map creator should open.</p>\n<h2>6. (Optional) Discord webhook</h2>\n<p>Open <code>config.lua</code> and set:</p>\n<pre><code class=\"language-lua\">Config.Discord = {\n    Webhook = &#39;https://discord.com/api/webhooks/XXX/YYY&#39;,\n    BotName = &#39;HoodLife Zonas Seguras&#39;,\n}\n</code></pre>\n<p>Logs are sent on zone create and delete.</p>\n"
  },
  "suty-zonasegura/configuration": {
    "folderTitle": "Safe Zone Creator",
    "pageTitle": "Configuration",
    "crumb": [
      "Safe Zone Creator",
      "Configuration"
    ],
    "html": "<h1>Configuration</h1>\n<p>All settings live in <code>config.lua</code> at the resource root. The file is <strong>not encrypted</strong> and can be edited freely.</p>\n<h2>Language</h2>\n<pre><code class=\"language-lua\">Config.Locale = &#39;es&#39;  -- &#39;es&#39; (Spanish) | &#39;en&#39; (English)\n</code></pre>\n<p>Add new languages by creating <code>locales/&lt;code&gt;.lua</code> following the same pattern as <code>es.lua</code> / <code>en.lua</code>.</p>\n<h2>Framework</h2>\n<pre><code class=\"language-lua\">Config.Framework = &#39;auto&#39;  -- &#39;auto&#39; | &#39;qbx&#39; | &#39;qb&#39; | &#39;esx&#39;\n</code></pre>\n<p><code>auto</code> checks for <code>qbx_core</code> → <code>qb-core</code> → <code>es_extended</code> in that order and picks the first one started. Force a specific framework only if you have multiple installed.</p>\n<h2>Command &amp; Permissions</h2>\n<pre><code class=\"language-lua\">Config.Command    = &#39;zonasegura&#39;\nConfig.AdminGroup = &#39;group.admin&#39;  -- ACE permission\n</code></pre>\n<h2>Default Creator</h2>\n<pre><code class=\"language-lua\">Config.DefaultCreator = &#39;map&#39;  -- &#39;map&#39; | &#39;freecam&#39;\n</code></pre>\n<p>Determines what opens when an admin runs plain <code>/zonasegura</code> with no sub-argument. Both creators stay available via:</p>\n<ul>\n<li><code>/zonasegura map</code> — force the 2D Leaflet creator</li>\n<li><code>/zonasegura freecam</code> — force the 3D in-world creator</li>\n</ul>\n<h2>Zone Validation</h2>\n<pre><code class=\"language-lua\">Config.MinPolygonPoints = 3   -- minimum vertices required to save\nConfig.MaxZoneName      = 64  -- character limit\n</code></pre>\n<h2>Runtime Enforcement</h2>\n<pre><code class=\"language-lua\">Config.NearZoneBufferM = 5.0  -- performance gate buffer in meters\nConfig.OuterLoopMs     = 500  -- outer &quot;near any zone&quot; tick interval\n</code></pre>\n<div class=\"docs-hint docs-hint-info\"><p><code>NearZoneBufferM</code> is a <strong>performance gate only</strong> — it widens the area where the precise point-in-polygon check runs, but doesn&#39;t expand the actual protection area. Protection is decided by point-in-polygon against the raw polygon vertices, so the protected area is exactly what you drew.</p>\n</div><h2>Protection Behavior</h2>\n<pre><code class=\"language-lua\">Config.Invincible = true\n</code></pre>\n<p>When <code>true</code>, players inside a safezone get <code>SetEntityInvincible(true)</code> — bullets and melee from outside the zone bounce off. A replicated statebag <code>inSafeZone = true</code> is also set so anti-cheats (like suty-landmark) skip the protected player.</p>\n<p>Set to <code>false</code> to keep <strong>only</strong> the weapon-draw lockout and allow damage from outside shooters.</p>\n<h2>On-screen Badge</h2>\n<pre><code class=\"language-lua\">Config.TextUI = {\n    text     = &#39;ESTÁS EN ZONA SEGURA&#39;,  -- leave nil to fall back to Locale.inside_safezone\n    icon     = &#39;shield-halved&#39;,\n    position = &#39;right-center&#39;,\n    style    = {\n        backgroundColor = &#39;#0a3d62&#39;,\n        color           = &#39;#ffffff&#39;,\n        borderColor     = &#39;#3498db&#39;,\n    },\n}\n</code></pre>\n<div class=\"docs-hint docs-hint-info\"><p>If <code>Config.TextUI.text</code> is <code>nil</code>, the badge falls back to the active locale&#39;s <code>inside_safezone</code> string — so switching <code>Config.Locale = &#39;en&#39;</code> automatically changes the badge text.</p>\n</div><h2>Discord Logging (optional)</h2>\n<pre><code class=\"language-lua\">Config.Discord = {\n    Webhook = false,                         -- false = disabled, or set a webhook URL\n    BotName = &#39;HoodLife Zonas Seguras&#39;,\n}\n</code></pre>\n<h2>Debug</h2>\n<pre><code class=\"language-lua\">Config.Debug = false\n</code></pre>\n<p>When <code>true</code>, prints exact enter/exit coords to the F8 client console every time a player crosses a zone boundary. Useful for verifying the protection trigger fires precisely at the polygon edge — walk the perimeter and watch the log.</p>\n"
  },
  "suty-zonasegura/commands": {
    "folderTitle": "Safe Zone Creator",
    "pageTitle": "Commands",
    "crumb": [
      "Safe Zone Creator",
      "Commands"
    ],
    "html": "<h1>Commands</h1>\n<p>All commands are restricted to ACE-allowed admins (<code>group.admin</code> by default — see <a href=\"configuration.md\">Configuration</a>).</p>\n<table>\n<thead>\n<tr>\n<th>Command</th>\n<th>Description</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>/zonasegura</code></td>\n<td>Opens the default creator (set by <code>Config.DefaultCreator</code>)</td>\n</tr>\n<tr>\n<td><code>/zonasegura map</code></td>\n<td>Forces the 2D Leaflet creator</td>\n</tr>\n<tr>\n<td><code>/zonasegura freecam</code></td>\n<td>Forces the 3D in-world creator</td>\n</tr>\n</tbody></table>\n<h2>Map creator workflow</h2>\n<ol>\n<li>Run <code>/zonasegura</code> (or <code>/zonasegura map</code>).</li>\n<li><strong>Left-click</strong> on the map to add vertices.</li>\n<li><strong>Right-click</strong> to undo the last vertex.</li>\n<li><strong>Drag</strong> existing vertices to fine-tune the shape.</li>\n<li>Type a name in the side panel.</li>\n<li>Click <strong>Guardar Zona</strong> (Save Zone) — the zone becomes active immediately for all online players.</li>\n<li>Click <strong>×</strong> next to a zone in the list to delete it.</li>\n</ol>\n<h2>Freecam creator workflow</h2>\n<ol>\n<li>Run <code>/zonasegura freecam</code>.</li>\n<li><strong>LMB</strong> — add a point at the cursor&#39;s world position.</li>\n<li><strong>RMB</strong> — undo the last point.</li>\n<li><strong>Scroll</strong> — adjust the zone&#39;s vertical thickness.</li>\n<li><strong>G</strong> — finish drawing and enter preview mode.</li>\n<li><strong>Enter</strong> — save the zone (name prompt appears).</li>\n<li><strong>Backspace</strong> — cancel / restart.</li>\n</ol>\n<h2>Keyboard shortcuts (map creator)</h2>\n<table>\n<thead>\n<tr>\n<th>Key</th>\n<th>Action</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>ESC</code></td>\n<td>Close creator</td>\n</tr>\n</tbody></table>\n"
  },
  "suty-zonasegura/compatibility": {
    "folderTitle": "Safe Zone Creator",
    "pageTitle": "Compatibility",
    "crumb": [
      "Safe Zone Creator",
      "Compatibility"
    ],
    "html": "<h1>Compatibility</h1>\n<h2>Frameworks</h2>\n<table>\n<thead>\n<tr>\n<th>Framework</th>\n<th>Status</th>\n<th>Notes</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>QBX Core (Qbox)</td>\n<td>✅ Native</td>\n<td>Auto-detected first</td>\n</tr>\n<tr>\n<td>QB-Core</td>\n<td>✅ Native</td>\n<td>Auto-detected after QBX</td>\n</tr>\n<tr>\n<td>ESX (es_extended)</td>\n<td>✅ Native</td>\n<td>Auto-detected last</td>\n</tr>\n</tbody></table>\n<p>Auto-detection picks the first framework whose resource is <code>started</code>. Override with <code>Config.Framework = &#39;qbx&#39; | &#39;qb&#39; | &#39;esx&#39;</code> if you have multiple installed.</p>\n<h2>Anti-cheat</h2>\n<p>The script sets a replicated statebag on each protected player:</p>\n<pre><code class=\"language-lua\">LocalPlayer.state:set(&#39;inSafeZone&#39;, true, true)\n</code></pre>\n<h3>suty-landmark</h3>\n<p><strong>Native integration.</strong> suty-landmark&#39;s anti-godmode detector reads the <code>inSafeZone</code> statebag and skips the player while it&#39;s <code>true</code>. No extra setup needed.</p>\n<h3>Other anti-cheats</h3>\n<p>Most reputable anti-cheats (Badger, FiveGuard, eulen-detect, etc.) either:</p>\n<ul>\n<li>Honor <code>SetEntityInvincible</code> calls from whitelisted resources, <strong>or</strong></li>\n<li>Read player statebags before flagging.</li>\n</ul>\n<p>If your anti-cheat <strong>does not</strong> honor statebags, set <code>Config.Invincible = false</code> and the script will only block weapon-draw (no invincibility). Players inside the zone will still be killable from outside in that mode.</p>\n<h2>Other suty resources</h2>\n<table>\n<thead>\n<tr>\n<th>Resource</th>\n<th>Interaction</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>suty-landmark</td>\n<td>Statebag-aware (no godmode false positives)</td>\n</tr>\n<tr>\n<td>All other suty-</td>\n<td>No conflicts</td>\n</tr>\n</tbody></table>\n<h2>Known incompatibilities</h2>\n<p>None reported. If you find one, open a ticket on <a href=\"https://discord.gg/c2RFDBU9pc\">Discord</a>.</p>\n"
  },
  "suty-zonasegura/troubleshooting": {
    "folderTitle": "Safe Zone Creator",
    "pageTitle": "Troubleshooting",
    "crumb": [
      "Safe Zone Creator",
      "Troubleshooting"
    ],
    "html": "<h1>Troubleshooting</h1>\n<h2>&quot;You do not have permission to use this command&quot;</h2>\n<p>You don&#39;t have the required ACE permission. Add yourself to <code>group.admin</code>:</p>\n<pre><code class=\"language-cfg\">add_principal identifier.license:YOUR_LICENSE_HERE group.admin\n</code></pre>\n<p>The script checks both <code>admin</code> and <code>group.admin</code> ACE forms, so any standard admin setup works.</p>\n<h2>The script refuses to start: &quot;ERROR — resource must be named &#39;suty-zonasegura&#39;&quot;</h2>\n<p>The folder is renamed. Rename it back to exactly <code>suty-zonasegura</code> and restart. This is an anti-piracy guard, not a bug.</p>\n<h2>Players are still taking damage inside the zone</h2>\n<p>Check three things:</p>\n<ol>\n<li><code>Config.Invincible = true</code> in <code>config.lua</code></li>\n<li>The player actually entered the zone — turn on <code>Config.Debug = true</code> and watch F8 for <code>ENTER</code>/<code>EXIT</code> log lines</li>\n<li>No other resource is overriding <code>SetEntityInvincible</code> on a faster loop</li>\n</ol>\n<h2>Anti-cheat is kicking players for godmode inside zones</h2>\n<p>The script writes a replicated <code>inSafeZone</code> statebag. If your anti-cheat doesn&#39;t honor it:</p>\n<ul>\n<li>For <strong>suty-landmark</strong> — make sure it&#39;s updated to a version that reads the statebag (built-in support).</li>\n<li>For <strong>other anti-cheats</strong> — set <code>Config.Invincible = false</code> and only the weapon-draw lockout will apply.</li>\n</ul>\n<h2>The map creator opens but the GTA V map is blank</h2>\n<p>The map tiles ship with the resource at <code>html/zonecreator/mapStyles/styleAtlas/</code>. If the folder is missing, redownload from Tebex. Don&#39;t manually exclude <code>mapStyles/**</code> in the <code>files {}</code> block.</p>\n<h2>Zones save but disappear on server restart</h2>\n<p>Check that the MySQL connection is healthy:</p>\n<pre><code class=\"language-sql\">SELECT COUNT(*) FROM suty_safezones;\n</code></pre>\n<p>If the count matches what you saved, the issue is on the load side — check the server console for any <code>^1[suty-zonasegura]</code> errors on startup.</p>\n<h2>Hidden scrollbar in the side panel — is that a bug?</h2>\n<p>No, it&#39;s intentional. The scroll behavior still works (mousewheel inside the side panel) — only the visible scrollbar is hidden for cleaner UI.</p>\n<h2>&quot;Acércate al sospechoso para capturar la foto&quot; / target ped not streamed</h2>\n<p>This message is from <strong>suty-andaimer</strong>, not suty-zonasegura. You&#39;re in the wrong docs page.</p>\n<h2>Still stuck?</h2>\n<p>Open a ticket on <a href=\"https://discord.gg/c2RFDBU9pc\">Discord</a> with:</p>\n<ul>\n<li>Your <code>Config.Framework</code> value</li>\n<li>The exact error from F8 console (client) or server console</li>\n<li>What you were doing when it happened</li>\n</ul>\n"
  }
};

const DOCS_DEFAULT_ID = "suty-billing/README";

Object.assign(window, { DOCS_TREE, DOCS_CONTENT, DOCS_DEFAULT_ID });
